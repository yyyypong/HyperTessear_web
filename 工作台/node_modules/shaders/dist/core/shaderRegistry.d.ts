import { ComponentDefinition } from './types';
import { GpuShaderDefinition } from './gpu/contract';

export interface ShaderRegistryEntry {
    name: string;
    fileName: string;
    category: string;
    definition: ComponentDefinition<any> | GpuShaderDefinition<any>;
    propsMetadata: Record<string, {
        ui?: any;
        default?: any;
        description?: string;
    }>;
}
export declare const shaderRegistry: Record<string, ShaderRegistryEntry>;
export declare const deprecatedShaderNames: Record<string, string>;
/** Canonical shader name for `name` — resolves deprecated names, passes everything else through. */
export declare function resolveShaderName(name: string): string;
export declare function getAllShaders(): ShaderRegistryEntry[];
export declare function getShaderByName(name: string): ShaderRegistryEntry | undefined;
export declare function getShadersByCategory(category: string): ShaderRegistryEntry[];
export declare function getShaderCategories(): string[];
//# sourceMappingURL=shaderRegistry.d.ts.map