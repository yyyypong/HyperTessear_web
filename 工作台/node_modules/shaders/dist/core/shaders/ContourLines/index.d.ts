import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

declare const transformSource: (value: string) => number;
declare const transformColorMode: (value: string) => number;
export interface ComponentProps {
    /**
     * Number of contour levels
     *
     * @default 5
     */
    levels: number;
    /**
     * Width of the contour lines in pixels
     *
     * @default 2
     */
    lineWidth: number;
    /**
     * Edge softness of the lines (0 = sharp, 1 = soft)
     *
     * Accepts a number between 0 and 1.
     * @default 0
     */
    softness: number;
    /**
     * Contour distribution. <1 clusters in bright, >1 clusters in dark
     *
     * @default 0.5
     */
    gamma: number;
    /**
     * Invert the source values
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    invert: boolean;
    /**
     * Use luminance or alpha channel for contours
     *
     * Accepts one of: `"luminance"`, `"alpha"`.
     * @default "luminance"
     */
    source: Parameters<typeof transformSource>[0];
    /**
     * Use source image colors or custom colors
     *
     * Accepts one of: `"source"`, `"custom"`.
     * @default "source"
     */
    colorMode: Parameters<typeof transformColorMode>[0];
    /**
     * Color of the contour lines (custom mode)
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    lineColor: Parameters<typeof transformColor>[0];
    /**
     * Background color (custom mode)
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "transparent"
     */
    backgroundColor: Parameters<typeof transformColor>[0];
}
export declare const contourLuma: import('typegpu').TgpuFn<(color: d.Vec4f) => d.F32>;
export declare const contourLineMask: import('typegpu').TgpuFn<(sourceValue: d.F32, levels: d.F32, lineWidth: d.F32, softness: d.F32, gamma: d.F32, invert: d.F32) => d.F32>;
export declare const contourCompose: import('typegpu').TgpuFn<(bg: d.Vec4f, line: d.Vec4f, mask: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map