import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Dithering pattern algorithm
     *
     * Accepts one of: `"bayer2"`, `"bayer4"`, `"bayer8"`, `"clusteredDot"`, `"blueNoise"`, `"whiteNoise"`, `"floydSteinberg"`.
     * @default "bayer4"
     */
    pattern: string;
    /**
     * Size of dithering pixels
     *
     * Accepts a number between 1 and 20.
     * @default 4
     */
    pixelSize: number;
    /**
     * Luminance threshold for dithering
     *
     * @default 0.5
     */
    threshold: number;
    /**
     * How much of the luminance range participates in dithering (lower = more solid areas)
     *
     * @default 1
     */
    spread: number;
    /**
     * How colors are determined
     *
     * Accepts one of: `"custom"`, `"source"`.
     * @default "custom"
     */
    colorMode: string;
    /**
     * Dark color for dithering
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "transparent"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Light color for dithering
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    colorB: Parameters<typeof transformColor>[0];
}
export declare const ditherCoord: import('typegpu').TgpuFn<(uv: d.Vec2f, gridRes: d.Vec2f, pixelSize: d.F32) => d.Vec2f>;
export declare const ditherPixUV: import('typegpu').TgpuFn<(coord: d.Vec2f, pixelSize: d.F32, gridRes: d.Vec2f) => d.Vec2f>;
export declare const ditherLuma: import('typegpu').TgpuFn<(c: d.Vec4f) => d.F32>;
export declare const ditherBayerQuad: import('typegpu').TgpuFn<(a: d.F32, b: d.F32) => d.F32>;
export declare const ditherPeriodic: import('typegpu').TgpuFn<(coord: d.Vec2f) => d.Vec4f>;
export declare const ditherBlueNoise: import('typegpu').TgpuFn<(coord: d.Vec2f) => d.F32>;
export declare const ditherWhiteNoise: import('typegpu').TgpuFn<(coord: d.Vec2f) => d.F32>;
export declare const ditherOrderedResult: import('typegpu').TgpuFn<(ditherValue: d.F32, luminance: d.F32, threshold: d.F32, spread: d.F32) => d.F32>;
export declare const ditherBlockOrigin: import('typegpu').TgpuFn<(coord: d.Vec2f) => d.Vec2f>;
export declare const ditherLocalCellIndex: import('typegpu').TgpuFn<(coord: d.Vec2f, blockOrigin: d.Vec2f) => d.F32>;
export declare const ditherCellUV: import('typegpu').TgpuFn<(blockOrigin: d.Vec2f, lx: d.F32, ly: d.F32, pixelSize: d.F32, gridRes: d.Vec2f) => d.Vec2f>;
export declare const ditherFloydSteinberg: import('typegpu').TgpuFn<(lum: d.WgslArray<d.F32>, lic: d.F32, threshold: d.F32, spread: d.F32) => d.F32>;
export declare const ditherComposeCustom: import('typegpu').TgpuFn<(colorA: d.Vec4f, colorB: d.Vec4f, ditherResult: d.F32) => d.Vec4f>;
export declare const ditherComposeSource: import('typegpu').TgpuFn<(sourceColor: d.Vec4f, ditherResult: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map