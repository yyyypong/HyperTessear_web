import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * The color of the grid lines
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    color: Parameters<typeof transformColor>[0];
    /**
     * Fill color of the cells (transparent = lines only). Pair with Variation for a tiled look.
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "transparent"
     */
    cellColor: Parameters<typeof transformColor>[0];
    /**
     * Number of cells along the shortest canvas edge (creates square cells)
     *
     * @default 10
     */
    cells: number;
    /**
     * Thickness of grid lines (normalized, 0.0-1.0)
     *
     * @default 1
     */
    thickness: number;
    /**
     * Softness of the line edges (0 = crisp, 1 = very soft)
     *
     * @default 0
     */
    softness: number;
    /**
     * Per-cell random lightening/darkening of the cell fill color
     *
     * @default 0
     */
    variation: number;
    /**
     * Rotation of the grid in degrees. At 45° this produces a crosshatch/diamond pattern.
     *
     * @default 0
     */
    rotation: number;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const gridCellCenterUV: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, cells: d.F32, rotation: d.F32) => d.Vec2f>;
export declare const gridFilterAxis: import('typegpu').TgpuFn<(px: d.F32, wx: d.F32, N: d.F32) => d.F32>;
export declare const gridField: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, cells: d.F32, thickness: d.F32, rotation: d.F32, softness: d.F32, variation: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map