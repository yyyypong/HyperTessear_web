import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformPosition, transformColor } from '../../utilities/transformations';
import { BoundingBoxOrigin } from '../../types';

export declare const liquidStudio: import('typegpu').TgpuFn<(rx: d.F32, ry: d.F32, envC: d.F32, envS: d.F32, keyRad: d.F32, drift: d.F32, envStr: d.F32) => d.F32>;
export declare const liquidMetalComposite: import('typegpu').TgpuFn<(surf0: d.Vec4f, surfX: d.Vec4f, surfY: d.Vec4f, sdfUV: d.Vec2f, viewportSize: d.Vec2f, lightColor: d.Vec3f, darkColor: d.Vec3f, p0: d.Vec4f, p1: d.Vec4f, p2: d.Vec4f, p3: d.Vec4f) => d.Vec4f>;
export interface ComponentProps {
    /**
     * Reference edge the center position is measured from (center default). Lets you pin the shape relative to a corner or the canvas centre.
     *
     * @default "center"
     */
    origin: BoundingBoxOrigin;
    /**
     * Center position of the metal shape
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Scale of the metal shape (1 = default size)
     *
     * Accepts a number between 0.1 and 3.
     * @default 1
     */
    scale: number;
    /**
     * Rotation of the metal shape in degrees
     *
     * Accepts a number between 0 and 360.
     * @default 0
     */
    rotation: number;
    /**
     * The bright tone of the chrome highlights
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#eef0f6"
     */
    lightColor: Parameters<typeof transformColor>[0];
    /**
     * The dark tone the reflection falls to in shadow — recolor for tinted metal (gold, steel, copper…)
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#141414"
     */
    darkColor: Parameters<typeof transformColor>[0];
    /**
     * How molten the surface is — the depth of the flowing folds that bend the reflection
     *
     * Accepts a number between 0 and 1.5.
     * @default 1
     */
    turbulence: number;
    /**
     * Scale of the molten folds — higher = smaller, busier ripples
     *
     * Accepts a number between 0.5 and 8.
     * @default 4
     */
    ripple: number;
    /**
     * Swirl of the flow — domain-warps the folds into liquid curls
     *
     * Accepts a number between 0 and 1.5.
     * @default 1.5
     */
    warp: number;
    /**
     * Tightness of the specular highlights — 1 = razor chrome glints, 0 = soft satin
     *
     * Accepts a number between 0 and 1.
     * @default 0.6
     */
    sharpness: number;
    /**
     * Strength of the reflected studio lighting — the softboxes seen across the metal
     *
     * Accepts a number between 0 and 2.
     * @default 1.5
     */
    environment: number;
    /**
     * Rotates the whole reflected studio — spins where the bright reflections fall
     *
     * Accepts a number between 0 and 360.
     * @default 0
     */
    envRotation: number;
    /**
     * Prismatic color fringing along the reflection edges
     *
     * Accepts a number between 0 and 1.
     * @default 0.25
     */
    dispersion: number;
    /**
     * Direction of the key light for the specular glints, in degrees
     *
     * Accepts a number between 0 and 360.
     * @default 265
     */
    lightAngle: number;
    /**
     * Speed of the molten flow and the slowly drifting reflection. 0 pauses.
     *
     * Accepts a number between 0 and 4.
     * @default 0.5
     */
    speed: number;
    /**
     * Softness of the shape boundary edge
     *
     * Accepts a number between 0 and 1.
     * @default 0.05
     */
    edgeSoftness: number;
    /**
     * Serialized shape configuration (JSON)
     *
     * @default "DEFAULT_SHAPE_CONFIG"
     */
    shape: string;
    /**
     * URL to a pre-generated SDF .bin file
     *
     * @default ""
     */
    shapeSdfUrl: string;
    /**
     * Active SDF shape type
     *
     * @default ""
     */
    shapeType: string;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map