import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Center point of rotation
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Horizontal rotation (left/right)
     *
     * @default 0
     */
    pan: number;
    /**
     * Vertical rotation (up/down)
     *
     * @default 0
     */
    tilt: number;
    /**
     * Field of view - controls perspective intensity
     *
     * @default 60
     */
    fov: number;
    /**
     * Zoom in to fill the frame after rotation
     *
     * @default 1
     */
    zoom: number;
    /**
     * Shift the result in X/Y
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    offset: Parameters<typeof transformPosition>[0];
    /**
     * How to handle edges
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "transparent"
     */
    edges: string;
}
export declare const perspectiveUV: import('typegpu').TgpuFn<(center: d.Vec2f, pan: d.F32, tilt: d.F32, fov: d.F32, zoom: d.F32, offset: d.Vec2f, uv: d.Vec2f) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map