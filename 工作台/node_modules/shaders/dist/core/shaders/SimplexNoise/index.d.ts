import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { ColorStop } from '../../utilities/colorStops';

export type { ColorStop };
export interface ComponentProps {
    /**
     * First color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    colorA: string;
    /**
     * Second color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    colorB: string;
    stops: ColorStop[] | null;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
    /**
     * Pattern scale (higher = larger patterns)
     *
     * Accepts a number between -2 and 5.
     * @default 2
     */
    scale: number;
    /**
     * Balance between colors (negative = more colorB, positive = more colorA)
     *
     * @default 0
     */
    balance: number;
    /**
     * Pattern contrast (higher = sharper transitions)
     *
     * @default 0
     */
    contrast: number;
    /**
     * Random seed for pattern variation
     *
     * Accepts a number between 0 and 100.
     * @default 0
     */
    seed: number;
    /**
     * Animation speed
     *
     * Accepts a number between 0 and 5.
     * @default 1
     */
    speed: number;
}
export declare const simplexNoiseField: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, scale: d.F32, seed: d.F32, contrast: d.F32, balance: d.F32, animTime: d.F32) => d.F32>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map