import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Background color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Spiral stroke color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Thickness of spiral stroke
     *
     * @default 0.5
     */
    strokeWidth: number;
    /**
     * Stroke losing width further from center
     *
     * @default 0
     */
    strokeFalloff: number;
    /**
     * Color transition sharpness (0 = hard edge, 1 = smooth fade)
     *
     * @default 0
     */
    softness: number;
    /**
     * Animation speed (negative values reverse direction)
     *
     * Accepts a number between -3 and 3.
     * @default 1
     */
    speed: number;
    /**
     * The center point of the spiral
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Scale factor for spiral bands (higher = more bands, lower = fewer bands)
     *
     * @default 1
     */
    scale: number;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const spiralOffset: import('typegpu').TgpuFn<(center: d.Vec2f, scale: d.F32, animTime: d.F32, uv: d.Vec2f, viewport: d.Vec2f) => d.F32>;
export declare const spiralMask: import('typegpu').TgpuFn<(center: d.Vec2f, scale: d.F32, strokeWidth: d.F32, strokeFalloff: d.F32, softness: d.F32, animTime: d.F32, uv: d.Vec2f, viewport: d.Vec2f) => d.F32>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map