import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Ray color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffdd88"
     */
    color: Parameters<typeof transformColor>[0];
    /**
     * Background color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    background: Parameters<typeof transformColor>[0];
    /**
     * Center point of the sunburst
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Number of rays
     *
     * Accepts a number between 3 and 64.
     * @default 12
     */
    rayCount: number;
    /**
     * Softness of ray edges
     *
     * @default 0.3
     */
    softness: number;
    /**
     * How far the rays extend from the center
     *
     * @default 0.8
     */
    radius: number;
    /**
     * How gradually the rays fade at their outer edge
     *
     * @default 0.5
     */
    feather: number;
    /**
     * Rotation speed — positive values rotate clockwise
     *
     * Accepts a number between -2 and 2.
     * @default 0.2
     */
    speed: number;
}
export declare const sunBurstAlpha: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, center: d.Vec2f, rayCount: d.F32, softness: d.F32, radius: d.F32, feather: d.F32, animTime: d.F32) => d.F32>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map