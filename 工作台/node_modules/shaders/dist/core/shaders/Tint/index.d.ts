import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Tint color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ff8800"
     */
    color: Parameters<typeof transformColor>[0];
    /**
     * Tint amount (0 = no tint, 1 = full tint)
     *
     * @default 0.5
     */
    amount: number;
    /**
     * Preserve original brightness
     *
     * Accepts a boolean value (`true` or `false`).
     * @default true
     */
    preserveLuminosity: boolean;
}
export declare const tintColorPlain: import('typegpu').TgpuFn<(color: d.Vec4f, tintColor: d.Vec4f, amount: d.F32) => d.Vec4f>;
export declare const tintColorPreserve: import('typegpu').TgpuFn<(color: d.Vec4f, tintColor: d.Vec4f, amount: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map