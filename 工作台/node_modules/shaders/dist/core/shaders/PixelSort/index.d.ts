import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Size of the cursor brush that sorts pixels
     *
     * @default 0.4
     */
    radius: number;
    /**
     * Softness of the brush edge (0 = hard circle, 1 = very soft)
     *
     * @default 1
     */
    falloff: number;
    /**
     * How fast pixels sort — runs more sorting passes per frame
     *
     * @default 0.1
     */
    strength: number;
    /**
     * How quickly sorted pixels melt back to their original position (0 = permanent)
     *
     * @default 0.1
     */
    decay: number;
    /**
     * Axis pixels are sorted along
     *
     * Accepts one of: `"horizontal"`, `"vertical"`.
     * @default "vertical"
     */
    axis: string;
    /**
     * Sort order by brightness
     *
     * Accepts one of: `"ascending"`, `"descending"`.
     * @default "descending"
     */
    direction: string;
}
/** Aspect-corrected brush weight: 1 at the cursor centre → 0 past the radius. */
export declare const brushWeight: import('typegpu').TgpuFn<(cxf: d.F32, cyf: d.F32, mouseX: d.F32, mouseY: d.F32, radius: d.F32, falloff: d.F32, aspect: d.F32) => d.F32>;
/** Per-cell dither random (sin-fract hash → GPU-only). */
export declare const cellRand: import('typegpu').TgpuFn<(xf: d.F32, yf: d.F32, seed: d.F32) => d.F32>;
/** Luma prepass kernel: cache each cell's child luma once per frame (swap passes read the cache). */
export declare const lumaKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
/**
 * Compare-swap kernel factory (odd-even transposition sort). `parity`, `vertical`, and `dir` are
 * compile-time constants baked into the WGSL. Both members of a
 * pair derive the same exchange decision from the same two cached keys → lock-free consistent swap.
 */
export declare function makeSwapKernel(vertical: boolean, dir: number, parity: number): import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
/** Output kernel factory: per cell, write the normalised source coordinate along the sort axis. */
export declare function makeOutputKernel(vertical: boolean): import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
/** Child sample UV from the sorted source coordinate (one body per axis; axis is compile-time). */
export declare const pixelSortUVVertical: import('typegpu').TgpuFn<(uv: d.Vec2f, srcCoord: d.F32) => d.Vec2f>;
export declare const pixelSortUVHorizontal: import('typegpu').TgpuFn<(uv: d.Vec2f, srcCoord: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map