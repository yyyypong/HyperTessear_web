import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformPosition, transformAngle } from '../../utilities/transformations';
import { ColorStop } from '../../utilities/colorStops';

export type { ColorStop };
export declare const gradientStopsInSpace: import('typegpu').TgpuFn<(t: d.F32, colors: d.WgslArray<d.Vec4f>, positions: d.WgslArray<d.Vec4f>, converted: d.WgslArray<d.Vec3f>, stopCount: d.F32) => d.WgslStruct<{
    conv: d.Vec3f;
    alpha: d.F32;
}>>;
export interface ComponentProps {
    /**
     * The starting color of the gradient
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#1aff00"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * The ending color of the gradient
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#0000ff"
     */
    colorB: Parameters<typeof transformColor>[0];
    stops: ColorStop[] | null;
    /**
     * The starting point of the gradient
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0,"y":0.5}
     */
    start: Parameters<typeof transformPosition>[0];
    /**
     * The ending point of the gradient
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":1,"y":0.5}
     */
    end: Parameters<typeof transformPosition>[0];
    /**
     * Additional rotation angle of the gradient (in degrees)
     *
     * @default 0
     */
    angle: Parameters<typeof transformAngle>[0];
    /**
     * How to handle areas beyond the gradient endpoints
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "stretch"
     */
    edges: string;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const gradientRawT: import('typegpu').TgpuFn<(start: d.Vec2f, end: d.Vec2f, angle: d.F32, uv: d.Vec2f, viewport: d.Vec2f) => d.F32>;
/** Mirror (mode 2): reflect at boundaries — `m = mod(abs(t), 2); m > 1 ? 2 - m : m`. */
export declare const gradientEdgeMirror: import('typegpu').TgpuFn<(t: d.F32) => d.F32>;
/** Wrap (mode 3): tile via fract. */
export declare const gradientEdgeWrap: import('typegpu').TgpuFn<(t: d.F32) => d.F32>;
/** Clamp `t` to [0,1] — the color-lookup parameter (and the stretch edge mode). */
export declare const gradientClamp01: import('typegpu').TgpuFn<(t: d.F32) => d.F32>;
/** Transparent (mode 1) coverage: 1 when the raw `t` is inside [0,1], else 0. */
export declare const gradientTransparentAlpha: import('typegpu').TgpuFn<(t: d.F32) => d.F32>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map