import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Scale of the fabric displacement effect
     *
     * @default 10
     */
    intensity: number;
    /**
     * Fabric rigidity (higher = stiffer canvas, lower = stretchy silk)
     *
     * Accepts a number between 1 and 30.
     * @default 3
     */
    stiffness: number;
    /**
     * How quickly fabric motion settles
     *
     * Accepts a number between 0 and 10.
     * @default 3
     */
    damping: number;
    /**
     * Cursor influence area
     *
     * Accepts a number between 0.1 and 1.5.
     * @default 1
     */
    radius: number;
    /**
     * How to handle edges when distortion pushes content out of bounds
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "stretch"
     */
    edges: string;
}
/** Spring-mass cloth step (4 structural + 4 shear springs + return-to-rest, semi-implicit Euler,
 *  cursor injection). 2D-dispatched with the interior border guard. */
export declare const springKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
/** Copy the displacement (xy) of the current buffer into the RG displacement texture. */
export declare const outputKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
/** Shared by the fragment + uvRemap: the intensity-scaled, clamped displaced UV. */
export declare const liquifyOffsetUV: import('typegpu').TgpuFn<(uv: d.Vec2f, disp: d.Vec2f, intensity: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map