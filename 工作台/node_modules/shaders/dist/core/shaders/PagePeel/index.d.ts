import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Which corner the page peels from
     *
     * Accepts one of: `"top-left"`, `"top-right"`, `"bottom-left"`, `"bottom-right"`.
     * @default "bottom-right"
     */
    corner: string;
    /**
     * How far the peel has progressed across the page (0 = flat, 1 = fully peeled)
     *
     * @default 0.2
     */
    amount: number;
    /**
     * Tightness of the curl (fraction of the page diagonal)
     *
     * @default 0.2
     */
    radius: number;
    /**
     * How much the underside of the curl is shaded for depth
     *
     * @default 0.55
     */
    shading: number;
    /**
     * Strength of the specular sheen running along the curl
     *
     * @default 0.4
     */
    highlight: number;
    /**
     * Width of the specular highlight band (low = tight gloss, high = broad satin)
     *
     * @default 0.2
     */
    highlightSoftness: number;
    /**
     * Strength of the contact shadow the curl casts on the page
     *
     * @default 1
     */
    shadow: number;
}
export declare const PagePeelGeom: d.WgslStruct<{
    curlUV: d.Vec2f;
    curlBright: d.F32;
    spec: d.F32;
    flatShade: d.F32;
    peelAlpha: d.F32;
    foldBlend: d.F32;
    showCurl: d.F32;
}>;
export declare const pagePeelGeom: import('typegpu').TgpuFn<(uv: d.Vec2f, aspect: d.F32, cornerX: d.F32, cornerY: d.F32, oppX: d.F32, oppY: d.F32, amount: d.F32, radius: d.F32, shading: d.F32, highlight: d.F32, highlightSoftness: d.F32, shadow: d.F32) => d.WgslStruct<{
    curlUV: d.Vec2f;
    curlBright: d.F32;
    spec: d.F32;
    flatShade: d.F32;
    peelAlpha: d.F32;
    foldBlend: d.F32;
    showCurl: d.F32;
}>>;
export declare const pagePeelCompose: import('typegpu').TgpuFn<(curlSample: d.Vec4f, flatSample: d.Vec4f, g: d.WgslStruct<{
    curlUV: d.Vec2f;
    curlBright: d.F32;
    spec: d.F32;
    flatShade: d.F32;
    peelAlpha: d.F32;
    foldBlend: d.F32;
    showCurl: d.F32;
}>) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map