import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Maximum rotation angle per ring
     *
     * @default 20
     */
    intensity: number;
    /**
     * Number of concentric rings
     *
     * @default 8
     */
    rings: number;
    /**
     * Softness of transitions between rings
     *
     * @default 0.03
     */
    smoothness: number;
    /**
     * Randomization seed for per-ring rotation variation
     *
     * Accepts a number between 0 and 100.
     * @default 0
     */
    seed: number;
    /**
     * Speed of continuous ring rotation
     *
     * Accepts a number between -5 and 5.
     * @default 0.1
     */
    speed: number;
    /**
     * How much each ring varies in rotation speed and direction
     *
     * @default 0.5
     */
    speedRandomness: number;
    /**
     * How to handle edges when distortion pushes content out of bounds
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "mirror"
     */
    edges: string;
    /**
     * Center point of the concentric rings
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: {
        x: number;
        y: number;
    } | string;
}
export declare const csHash: import('typegpu').TgpuFn<(x: d.F32) => d.F32>;
export declare const csRotateUV: import('typegpu').TgpuFn<(uv: d.Vec2f, aspect: d.F32, center: d.Vec2f, angle: d.F32) => d.Vec2f>;
export declare const concentricSpinUV: import('typegpu').TgpuFn<(center: d.Vec2f, intensity: d.F32, rings: d.F32, smoothness: d.F32, seed: d.F32, speedRandomness: d.F32, animTime: d.F32, uv: d.Vec2f, aspect: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map