import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * How far the wipe has progressed (0 = fully visible, 1 = fully wiped away)
     *
     * @default 0.5
     */
    progress: number;
    /**
     * Size of each square as a fraction of the frame width
     *
     * @default 0.1
     */
    blockSize: number;
    /**
     * How softly each square fades out (0 = hard-edged squares)
     *
     * @default 0.15
     */
    softness: number;
    /**
     * Reverse the order the squares wipe in
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    invert: boolean;
}
export declare const checkerWipeMask: import('typegpu').TgpuFn<(color: d.Vec4f, uv: d.Vec2f, aspect: d.F32, blockSize: d.F32, softness: d.F32, progress: d.F32, invert: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map