import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Horizontal thread color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#c4c4c4"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Vertical thread color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#4d4d4d"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Number of threads across the shortest canvas edge
     *
     * @default 10
     */
    cells: number;
    /**
     * Gap between threads (0 = no gap, 0.5 = maximum gap)
     *
     * @default 0.25
     */
    gap: number;
    /**
     * Rotation of the weave pattern in degrees
     *
     * @default 0
     */
    rotation: number;
}
export declare const weaveColor: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, cells: d.F32, gap: d.F32, rotation: d.F32, colorA: d.Vec4f, colorB: d.Vec4f) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map