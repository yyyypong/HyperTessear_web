import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * First stripe color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Second stripe color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Angle of stripes in degrees
     *
     * @default 45
     */
    angle: Parameters<typeof transformAngle>[0];
    /**
     * Number of stripe pairs visible
     *
     * @default 5
     */
    density: number;
    /**
     * Ratio of the two colors
     *
     * @default 0.5
     */
    balance: number;
    /**
     * Edge softness
     *
     * @default 0
     */
    softness: number;
    /**
     * Animation speed
     *
     * Accepts a number between -1 and 1.
     * @default 0.2
     */
    speed: number;
    /**
     * Phase offset for pattern positioning
     *
     * @default 0
     */
    offset: number;
    /**
     * Color space for interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const stripeCoordP: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, angle: d.F32, density: d.F32, animTime: d.F32, offset: d.F32) => d.F32>;
export declare const stripeMaskValue: import('typegpu').TgpuFn<(p: d.F32, w: d.F32, balance: d.F32) => d.F32>;
export declare const stripesMask: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, angle: d.F32, density: d.F32, animTime: d.F32, offset: d.F32, balance: d.F32, softness: d.F32) => d.F32>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map