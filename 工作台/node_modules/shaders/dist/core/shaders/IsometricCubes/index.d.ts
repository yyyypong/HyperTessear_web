import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Base cube color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#7c5cff"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Second cube color, mixed in per-cube for variation
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ff5c9d"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Color of the cube edge lines
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    lineColor: Parameters<typeof transformColor>[0];
    /**
     * Number of cubes across the shortest canvas edge
     *
     * @default 6
     */
    cells: number;
    /**
     * Thickness of the cube edge lines (0 = no edges)
     *
     * @default 1
     */
    thickness: number;
    /**
     * Rotation of the tiling in degrees
     *
     * @default 0
     */
    rotation: Parameters<typeof transformAngle>[0];
    /**
     * Softness of the edge lines (0 = crisp, 1 = very soft)
     *
     * @default 0
     */
    softness: number;
    /**
     * Per-cube random blend between the two colors (0 = uniform cubes, the clean 3D look)
     *
     * @default 1
     */
    colorVariation: number;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const isoCubeField: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, cells: d.F32, thickness: d.F32, rotation: d.F32, softness: d.F32) => d.Vec3f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map