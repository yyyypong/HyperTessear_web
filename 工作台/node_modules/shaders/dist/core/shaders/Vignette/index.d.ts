import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Color of the vignette at the edges
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    color: Parameters<typeof transformColor>[0];
    /**
     * Center of the clear area where the vignette begins
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Distance from center where the vignette begins to fade in
     *
     * @default 0.5
     */
    radius: number;
    /**
     * Width of the transition zone from clear to full vignette
     *
     * @default 0.5
     */
    falloff: number;
    /**
     * Strength of the vignette effect
     *
     * @default 1
     */
    intensity: number;
}
export declare const vignetteColor: import('typegpu').TgpuFn<(color: d.Vec4f, uv: d.Vec2f, aspect: d.F32, center: d.Vec2f, vigColor: d.Vec4f, radius: d.F32, falloff: d.F32, intensity: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map