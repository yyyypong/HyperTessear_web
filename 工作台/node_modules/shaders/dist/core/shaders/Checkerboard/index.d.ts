import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * First color of the checkerboard pattern
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#cccccc"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Second color of the checkerboard pattern
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#999999"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Number of cells along the canvas height (creates square cells). Switch the unit to px in the editor to set an absolute cell size instead.
     *
     * @default 8
     */
    cells: number;
    /**
     * Smoothness of the transition between colors (0 = hard edges, 1 = very soft)
     *
     * @default 0
     */
    softness: number;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const checkerCoord: import('typegpu').TgpuFn<(uv: d.Vec2f, aspect: d.F32, cells: d.F32) => d.Vec2f>;
export declare const checkerFilterValue: import('typegpu').TgpuFn<(p: d.Vec2f, w: d.Vec2f) => d.F32>;
export declare const checkerBlend: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, cells: d.F32, softness: d.F32) => d.F32>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map