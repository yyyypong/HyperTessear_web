import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Blur intensity for red channel
     *
     * @default 0
     */
    redIntensity: number;
    /**
     * Blur intensity for green channel
     *
     * @default 20
     */
    greenIntensity: number;
    /**
     * Blur intensity for blue channel
     *
     * @default 40
     */
    blueIntensity: number;
}
/**
 * Per-channel UI intensity (0–100) → Gaussian pixel radius via the `× 0.1` factor. A single fixed
 * Gaussian runs at the MAX per-channel radius; the fragment then mixes each channel between the
 * sharp source and that one blurred buffer by `channelRadius / maxRadius` (an approximation that
 * avoids 6 dispatches / 6 storage textures).
 */
export declare const INTENSITY_TO_RADIUS = 0.1;
export declare const channelIntensityToRadius: (intensity: number) => number;
/**
 * Fragment composite: per-channel `mix(sharp, blurred, channelRadius / maxRadius)`. The blur ran at
 * `maxRadius = max(r, g, b, 0.01)`, so the max-intensity channel takes the full blur, a zero channel
 * stays exactly sharp, and intermediate channels linearly interpolate. Alpha comes from the sharp
 * child.
 */
export declare const channelBlurCompose: import('typegpu').TgpuFn<(sharp: d.Vec4f, blurred: d.Vec4f, redIntensity: d.F32, greenIntensity: d.F32, blueIntensity: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map