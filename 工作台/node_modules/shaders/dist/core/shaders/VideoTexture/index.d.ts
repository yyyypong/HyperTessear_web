import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Upload a video or provide a URL
     *
     * @default "https://shaders.com/sample.mp4"
     */
    url: string;
    /**
     * How the video should be sized within the viewport
     *
     * Accepts one of: `"cover"`, `"contain"`, `"fill"`, `"scale-down"`.
     * @default "fill"
     */
    objectFit: string;
    /**
     * Loop the video playback
     *
     * Accepts a boolean value (`true` or `false`).
     * @default true
     */
    loop: boolean;
}
/** cover — scale to cover the viewport (never smaller than the viewport). */
export declare const videoScaleCover: import('typegpu').TgpuFn<(videoDims: d.Vec2f, vp: d.Vec2f) => d.Vec2f>;
/** contain — scale to fit within the viewport (never larger than the viewport). */
export declare const videoScaleContain: import('typegpu').TgpuFn<(videoDims: d.Vec2f, vp: d.Vec2f) => d.Vec2f>;
/** fill — stretch to fill the viewport (uvScale = 1,1). */
export declare const videoScaleFill: import('typegpu').TgpuFn<() => d.Vec2f>;
/** scale-down — contain, but never scale up past the video's natural pixel size. */
export declare const videoScaleScaleDown: import('typegpu').TgpuFn<(videoDims: d.Vec2f, vp: d.Vec2f) => d.Vec2f>;
export declare const videoSampleUV: import('typegpu').TgpuFn<(uv: d.Vec2f, uvScale: d.Vec2f) => d.Vec2f>;
/**
 * sRGB → linear (inverse of kit `linearToSrgb`). External-texture samples come back gamma-encoded
 * (importExternalTexture returns display-referred sRGB), but the composition works in linear light
 * and the final pass re-encodes with `linearToSrgb`; decoding here avoids a double-encode that
 * washes the video out.
 */
export declare const srgbToLinear: import('typegpu').TgpuFn<(color: d.Vec3f) => d.Vec3f>;
/** Decode the sampled sRGB colour to linear, and cut alpha to 0 outside [0,1]² (contain/scale-down letterbox). */
export declare const videoAlphaCut: import('typegpu').TgpuFn<(color: d.Vec4f, uv: d.Vec2f) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map