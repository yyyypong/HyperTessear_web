import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * The center point of the stretch effect
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: {
        x: number;
        y: number;
    } | string;
    /**
     * The intensity of the stretch effect
     *
     * @default 1
     */
    strength: number;
    /**
     * The direction of the stretch in degrees
     *
     * @default 0
     */
    angle: number;
    /**
     * Controls the sharpness of the transition (0 = sharp edge, 1 = gradual transition)
     *
     * @default 0
     */
    falloff: number;
    /**
     * How to handle edges when distortion pushes content out of bounds
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "stretch"
     */
    edges: string;
}
export declare const stretchUV: import('typegpu').TgpuFn<(center: d.Vec2f, strength: d.F32, angle: d.F32, falloff: d.F32, uv: d.Vec2f, aspect: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map