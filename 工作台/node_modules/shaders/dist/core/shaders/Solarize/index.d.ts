import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Luminance level above which colors are inverted. Pixels brighter than this threshold get flipped.
     *
     * @default 0.5
     */
    threshold: number;
    /**
     * Blend between original (0) and fully solarized (1)
     *
     * @default 1
     */
    strength: number;
}
export declare const solarizeColor: import('typegpu').TgpuFn<(color: d.Vec4f, threshold: d.F32, strength: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map