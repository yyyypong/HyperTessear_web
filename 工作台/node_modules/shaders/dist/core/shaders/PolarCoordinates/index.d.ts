import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * The center point for polar coordinate conversion
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: {
        x: number;
        y: number;
    } | string;
    /**
     * Controls how much of the angular range to use (1 = full 360°, 0.5 = 180°)
     *
     * @default 1
     */
    wrap: number;
    /**
     * Controls how much of the radius range to use (affects the radial mapping)
     *
     * @default 1
     */
    radius: number;
    /**
     * Blends between original UVs (0) and polar coordinates (1)
     *
     * @default 1
     */
    intensity: number;
    /**
     * How to handle edges when distortion pushes content out of bounds
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "transparent"
     */
    edges: string;
}
export declare const polarCoordsUV: import('typegpu').TgpuFn<(center: d.Vec2f, wrap: d.F32, radius: d.F32, uv: d.Vec2f, aspect: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map