import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Strength of the ripple distortion
     *
     * @default 10
     */
    intensity: number;
    /**
     * How quickly ripples fade (higher = faster)
     *
     * Accepts a number between 0 and 20.
     * @default 10
     */
    decay: number;
    /**
     * Radius of cursor influence
     *
     * Accepts a number between 0.1 and 1.
     * @default 0.5
     */
    radius: number;
    /**
     * RGB channel separation along ripple edges
     *
     * @default 1
     */
    chromaticSplit: number;
    /**
     * How to handle edges when distortion pushes content out of bounds
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "stretch"
     */
    edges: string;
}
/**
 * Wave-equation propagation + cursor injection kernel (damped `avg − prev`).
 * 2D-dispatched (cx=col, cy=row); the interior guard drops the 1-cell border (neighbours are ±1/±gs).
 */
export declare const propagateKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
/** Central-difference gradient of the height field → RG displacement texel. */
export declare const gradientKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
/** The three per-channel sample UVs from the displacement gradient (chromatic split). */
export declare const cursorRipplesUVs: import('typegpu').TgpuFn<(uv: d.Vec2f, disp: d.Vec2f, intensity: d.F32, chromaticSplit: d.F32) => d.WgslStruct<{
    rUV: d.Vec2f;
    gUV: d.Vec2f;
    bUV: d.Vec2f;
}>>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map