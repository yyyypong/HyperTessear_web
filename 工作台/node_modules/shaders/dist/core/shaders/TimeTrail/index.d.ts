import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * What sheds the trail — Motion trails whatever changes frame-to-frame (works on opaque images and video), Alpha trails the child
     *
     * Accepts one of: `"motion"`, `"alpha"`.
     * @default "motion"
     */
    trailSource: string;
    /**
     * How much a pixel must change between frames to shed a trail (Motion source only) — lower catches subtle movement, higher only fast motion
     *
     * @default 0.06
     */
    motionThreshold: number;
    /**
     * How long the trail lingers, in seconds — the time constant of the fade (the star control)
     *
     * @default 0.6
     */
    trailLength: number;
    /**
     * How much the trail smears and softens as it fades — 0 keeps crisp ghost frames, higher melts them together
     *
     * @default 1
     */
    diffusion: number;
    /**
     * Sideways drift of the trail per second — smears the trail in a direction
     *
     * @default 0
     */
    driftX: number;
    /**
     * Vertical drift of the trail per second
     *
     * @default 0
     */
    driftY: number;
    /**
     * Per-frame zoom of the trail around the centre — above 1 pulls the trail into an outward tunnel, below 1 sucks it inward
     *
     * @default 1
     */
    zoom: number;
    /**
     * Overall strength of the trail behind the live frame
     *
     * @default 1
     */
    trailOpacity: number;
    /**
     * Colour the trail — Color tints it a single hue, Rainbow cycles hue as the trail ages
     *
     * Accepts one of: `"none"`, `"color"`, `"rainbow"`.
     * @default "none"
     */
    tintMode: string;
    /**
     * Trail tint colour (used in Color mode)
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#33aaff"
     */
    tint: Parameters<typeof transformColor>[0];
    /**
     * How the trail combines with the frame — Add / Screen build glowing light-painting trails, Multiply lays down dark ink ghosts, plus the full standard blend set
     *
     * Accepts one of: `"normal"`, `"add"`, `"screen"`, `"multiply"`, `"overlay"`, `"softLight"`, `"hardLight"`, `"darken"`, `"lighten"`, `"colorDodge"`, `"colorBurn"`, `"linearBurn"`, `"difference"`, `"exclusion"`, `"hue"`, `"saturation"`, `"color"`, `"luminosity"`.
     * @default "normal"
     */
    trailBlend: string;
    /**
     * Simulation speed. 0 freezes the trail in place.
     *
     * Accepts a number between 0 and 4.
     * @default 1
     */
    speed: number;
}
/** Bilinear tap of the previous history at a normalized UV (clamp-to-edge). */
export declare const timeTrailSampleHistory: import('typegpu').TgpuFn<(uvx: d.F32, uvy: d.F32) => d.Vec4f>;
/**
 * Frame-difference motion mask: how much this pixel changed since last frame, smoothstepped over
 * [threshold, 2·threshold] into a 0–1 stamp weight. Premultiplied RGB luma difference + alpha
 * difference, so both colour motion (opaque footage) and coverage motion (moving shapes) trigger.
 */
export declare const timeTrailMotionMask: import('typegpu').TgpuFn<(live: d.Vec4f, prev: d.Vec4f, threshold: d.F32) => d.F32>;
/** Rotate an RGB colour's hue by `amount` turns (via HSL) — the rainbow-trail primitive. */
export declare const timeTrailRotateHue: import('typegpu').TgpuFn<(rgb: d.Vec3f, amount: d.F32) => d.Vec3f>;
export declare const timeTrailKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const timeTrailColorize: import('typegpu').TgpuFn<(trail: d.Vec4f, tint: d.Vec4f) => d.Vec4f>;
/** Premultiplied scale — trailOpacity applied to the trail before the Alpha-order composite. */
export declare const timeTrailScaleTrail: import('typegpu').TgpuFn<(trail: d.Vec4f, k: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map