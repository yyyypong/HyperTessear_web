import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * The intensity of the saturation effect (1 being no change)
     *
     * @default 1
     */
    intensity: number;
}
export declare const saturationColor: import('typegpu').TgpuFn<(color: d.Vec4f, intensity: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map