import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * How far the sweep has travelled (0 = fully visible, 1 = fully wiped away)
     *
     * @default 0.5
     */
    progress: number;
    /**
     * Angle in degrees where the sweep begins
     *
     * @default 0
     */
    startAngle: number;
    /**
     * Which way the sweep rotates
     *
     * Accepts one of: `"cw"`, `"ccw"`, `"both"`.
     * @default "cw"
     */
    direction: string;
    /**
     * Point the sweep rotates around
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Softness of the sweeping edge
     *
     * @default 0.1
     */
    feather: number;
    /**
     * Reverse which side of the sweep is wiped
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    invert: boolean;
}
export declare const radialWipeMask: import('typegpu').TgpuFn<(color: d.Vec4f, uv: d.Vec2f, aspect: d.F32, center: d.Vec2f, startDeg: d.F32, progress: d.F32, feather: d.F32, mode: d.F32, invert: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map