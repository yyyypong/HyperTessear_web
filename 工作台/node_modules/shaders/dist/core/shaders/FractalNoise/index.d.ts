import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformAngle } from '../../utilities/transformations';
import { ColorStop } from '../../utilities/colorStops';

export type { ColorStop };
export interface ComponentProps {
    /**
     * First color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Second color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    colorB: Parameters<typeof transformColor>[0];
    stops: ColorStop[] | null;
    /**
     * Number of noise octaves (more = more detail)
     *
     * Accepts a number between 1 and 8.
     * @default 4
     */
    octaves: number;
    /**
     * How much finer each successive octave becomes
     *
     * @default 2
     */
    detail: number;
    /**
     * How strongly finer octaves contribute — higher values create more texture contrast
     *
     * @default 0.5
     */
    contrast: number;
    /**
     * Speed at which the noise pattern evolves in place
     *
     * Accepts a number between 0 and 1.
     * @default 0.15
     */
    speed: number;
    /**
     * Rotation angle in degrees
     *
     * @default 0
     */
    angle: Parameters<typeof transformAngle>[0];
    /**
     * Random seed for pattern variation
     *
     * @default 0
     */
    seed: number;
    /**
     * Color space for interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const fractalNoiseValue: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, angle: d.F32, animTime: d.F32, seed: d.F32, detail: d.F32, contrast: d.F32, octaveCount: d.F32) => d.F32>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map