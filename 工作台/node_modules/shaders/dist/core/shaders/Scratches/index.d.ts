import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    colorA: string;
    colorB: string;
    /**
     * Pattern scale (higher = more, finer scratches)
     *
     * Accepts a number between -2 and 5.
     * @default 2
     */
    scale: number;
    /**
     * Thickness of the scratches (higher = bolder streaks)
     *
     * Accepts a number between 0.2 and 5.
     * @default 1
     */
    thickness: number;
    /**
     * Random seed for pattern variation
     *
     * Accepts a number between 0 and 100.
     * @default 0
     */
    seed: number;
    speed: number;
}
export declare const scratchesField: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, scale: d.F32, seed: d.F32, animTime: d.F32, thickness: d.F32) => d.F32>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map