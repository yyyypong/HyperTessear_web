import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformAngle, transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Halftone rendering style
     *
     * Accepts one of: `"classic"`, `"cmyk"`.
     * @default "classic"
     */
    style: string;
    /**
     * Frequency of the halftone dots
     *
     * @default 100
     */
    frequency: number;
    /**
     * Rotation angle of the pattern (in degrees)
     *
     * @default 45
     */
    angle: Parameters<typeof transformAngle>[0];
    /**
     * Screen angle for the cyan plate (in degrees)
     *
     * @default 15
     */
    cyanAngle: Parameters<typeof transformAngle>[0];
    /**
     * Screen angle for the magenta plate (in degrees)
     *
     * @default 75
     */
    magentaAngle: Parameters<typeof transformAngle>[0];
    /**
     * Screen angle for the yellow plate (in degrees)
     *
     * @default 0
     */
    yellowAngle: Parameters<typeof transformAngle>[0];
    /**
     * Screen angle for the black plate (in degrees)
     *
     * @default 45
     */
    blackAngle: Parameters<typeof transformAngle>[0];
    /**
     * Simulated mis-registration between plates. Plates are offset around the misprint angle, producing colour fringing at the edges of inked regions.
     *
     * @default 0
     */
    misprint: number;
    /**
     * Direction the plates drift apart. Rotating this rotates the colour-fringing pattern.
     *
     * @default 0
     */
    misprintAngle: Parameters<typeof transformAngle>[0];
    /**
     * Paper/substrate color shown where no ink lands
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    paperColor: Parameters<typeof transformColor>[0];
    /**
     * Cyan ink color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#00ffff"
     */
    cyanColor: Parameters<typeof transformColor>[0];
    /**
     * Magenta ink color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ff00ff"
     */
    magentaColor: Parameters<typeof transformColor>[0];
    /**
     * Yellow ink color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffff00"
     */
    yellowColor: Parameters<typeof transformColor>[0];
    /**
     * Black (key) ink color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    blackColor: Parameters<typeof transformColor>[0];
}
export declare const halftoneClassic: import('typegpu').TgpuFn<(childColor: d.Vec4f, uv: d.Vec2f, aspect: d.F32, angleDeg: d.F32, frequency: d.F32) => d.Vec4f>;
export declare const halftonePlateUV: import('typegpu').TgpuFn<(uv: d.Vec2f, misprintAngleDeg: d.F32, plateIndexDeg: d.F32, misprint: d.F32, aspect: d.F32) => d.Vec2f>;
export declare const halftoneChannelK: import('typegpu').TgpuFn<(s: d.Vec4f) => d.F32>;
export declare const halftoneChannelC: import('typegpu').TgpuFn<(s: d.Vec4f) => d.F32>;
export declare const halftoneChannelM: import('typegpu').TgpuFn<(s: d.Vec4f) => d.F32>;
export declare const halftoneChannelY: import('typegpu').TgpuFn<(s: d.Vec4f) => d.F32>;
export declare const halftonePlateGrid: import('typegpu').TgpuFn<(uv: d.Vec2f, aspect: d.F32, angleDeg: d.F32, intensity: d.F32, frequency: d.F32) => d.F32>;
export declare const halftoneTransmission: import('typegpu').TgpuFn<(inkColor: d.Vec4f, inkMask: d.F32) => d.Vec3f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map