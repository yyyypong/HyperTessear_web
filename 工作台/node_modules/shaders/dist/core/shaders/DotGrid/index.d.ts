import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * The color of the dot
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    color: Parameters<typeof transformColor>[0];
    /**
     * The number of dots on the longest canvas edge
     *
     * Accepts a number between 1 and 200.
     * @default 30
     */
    density: number;
    /**
     * The size of each dot, zero (0) being invisible, one (1) filled the grid with no gaps
     *
     * @default 0.3
     */
    dotSize: number;
    /**
     * Horizontal stagger of alternating rows (0.5 = classic polka-dot brick offset)
     *
     * @default 0
     */
    offset: number;
    /**
     * Animates the rows drifting horizontally (0 = static)
     *
     * Accepts a number between -3 and 3.
     * @default 0
     */
    speed: number;
    /**
     * Per-row random speed variance for irregular drifting motion
     *
     * Accepts a number between 0 and 1.
     * @default 0.3
     */
    speedVariance: number;
    /**
     * Intensity of the twinkle effect (0 = off, 1 = full twinkle)
     *
     * @default 0
     */
    twinkle: number;
}
export declare const dotGridCellCenterUV: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, density: d.F32) => d.Vec2f>;
export declare const dotGridAlpha: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, density: d.F32, dotSize: d.F32, offset: d.F32, speedVariance: d.F32, twinkle: d.F32, animTime: d.F32, time: d.F32) => d.F32>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map