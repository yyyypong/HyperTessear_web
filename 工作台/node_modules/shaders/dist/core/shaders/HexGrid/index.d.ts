import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Cell fill color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Grid line color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Number of hexagons across the shortest canvas edge
     *
     * @default 8
     */
    cells: number;
    /**
     * Thickness of the hex grid lines
     *
     * @default 1
     */
    thickness: number;
    /**
     * Rotation of the grid in degrees
     *
     * @default 0
     */
    rotation: Parameters<typeof transformAngle>[0];
    /**
     * Softness of the line edges (0 = crisp, 1 = very soft)
     *
     * @default 0
     */
    softness: number;
    /**
     * Per-cell random lightening/darkening of the cell fill color
     *
     * @default 0
     */
    variation: number;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const hexGridField: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, cells: d.F32, thickness: d.F32, rotation: d.F32, softness: d.F32, variation: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map