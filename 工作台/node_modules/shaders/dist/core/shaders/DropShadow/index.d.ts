import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

/** The shadow's screen UV: offset the sample by the compass-angle direction × distance (X divided by
 *  aspect so the offset covers equal pixel distance in both axes). Pure. */
export declare const dropShadowUV: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, angle: d.F32, distance: d.F32) => d.Vec2f>;
/** `uv + (ox, oy) · blurRadius / viewport` — one separable-blur tap offset in UV space. Pure. */
export declare const dropShadowTapUV: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, blurRadius: d.F32, ox: d.F32, oy: d.F32) => d.Vec2f>;
/**
 * The final drop-shadow composite in PREMULTIPLIED space (the child RTT is premultiplied) — the
 * builder unpremultiplies the return. `cutout` is a runtime 0/1 flag selected via `std.select`.
 * Normal: original content OVER the shadow. Cutout: shadow only, with the original silhouette punched
 * out. `shadowColor` is the vec4 rgba tint. Pure.
 */
export declare const dropShadowComposite: import('typegpu').TgpuFn<(originalColor: d.Vec4f, shadowColor: d.Vec4f, shadowAlpha: d.F32, cutout: d.F32) => d.Vec4f>;
export interface ComponentProps {
    /**
     * Shadow color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    color: Parameters<typeof transformColor>[0];
    /**
     * How far the shadow is offset from the content
     *
     * @default 0.1
     */
    distance: number;
    /**
     * Direction the shadow is cast (compass degrees: 0=up, 90=right, 135=lower-right, 180=down)
     *
     * @default 135
     */
    angle: number;
    /**
     * Shadow softness (blur radius in pixels)
     *
     * @default 5
     */
    blur: number;
    /**
     * Shadow intensity — how strong/visible the shadow is
     *
     * @default 0.5
     */
    intensity: number;
    /**
     * Hide the original layer and show only the shadow
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    cutout: boolean;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map