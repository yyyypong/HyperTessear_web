import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Position of the top-left corner
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0,"y":0}
     */
    topLeft: Parameters<typeof transformPosition>[0];
    /**
     * Position of the top-right corner
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":1,"y":0}
     */
    topRight: Parameters<typeof transformPosition>[0];
    /**
     * Position of the bottom-left corner
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0,"y":1}
     */
    bottomLeft: Parameters<typeof transformPosition>[0];
    /**
     * Position of the bottom-right corner
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":1,"y":1}
     */
    bottomRight: Parameters<typeof transformPosition>[0];
    /**
     * Blends the warp in and out — 0 returns the content to its original rectangle, 1 fully pins the corners
     *
     * @default 1
     */
    amount: number;
    /**
     * How to handle areas outside the pinned quad
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "transparent"
     */
    edges: string;
}
export declare const cornerPinSafeDiv: import('typegpu').TgpuFn<(n: d.F32, dv: d.F32) => d.F32>;
export declare const cornerSideOf: import('typegpu').TgpuFn<(p: d.Vec2f, a: d.Vec2f, b: d.Vec2f) => d.F32>;
export declare const cornerInsideTri: import('typegpu').TgpuFn<(p: d.Vec2f, a: d.Vec2f, b: d.Vec2f, c: d.Vec2f) => d.F32>;
export declare const cornerProjectToDiagonal: import('typegpu').TgpuFn<(p: d.Vec2f, a: d.Vec2f, b: d.Vec2f, refSide: d.F32) => d.Vec2f>;
export declare const cornerConvexify: import('typegpu').TgpuFn<(p: d.Vec2f, a: d.Vec2f, b: d.Vec2f, o: d.Vec2f) => d.Vec2f>;
export declare const cornerPinSample: import('typegpu').TgpuFn<(coord: d.Vec2f, amount: d.F32, c0: d.Vec2f, c1: d.Vec2f, c2: d.Vec2f, c3: d.Vec2f) => d.Vec3f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map