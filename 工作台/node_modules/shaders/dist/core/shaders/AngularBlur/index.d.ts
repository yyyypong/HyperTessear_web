import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Intensity of the angular blur effect
     *
     * @default 20
     */
    intensity: number;
    /**
     * The center point of the rotation
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
}
export declare const BLUR_WEIGHTS: number[];
export declare const TAP_COUNT = 32;
/**
 * Sample coordinate for one angular-blur tap: rotate the (aspect-corrected) offset from `center` by
 * `tapIndex × angleStep`, where `angleStep = intensity·0.005 / 31`. `center` is the ALREADY-transformed
 * prop value (transformPosition stores `(x, 1 - y)`), so `1.0 - center.y` recovers the authored y.
 *
 * Each tap's rotation is computed DIRECTLY (`cos/sin(tapIndex·angleStep)`) rather than via a stateful
 * recurrence — no accumulated f32 drift and no loop-carried state.
 */
export declare const angularBlurTapCoord: import('typegpu').TgpuFn<(center: d.Vec2f, intensity: d.F32, uv: d.Vec2f, aspect: d.F32, tapIndex: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map