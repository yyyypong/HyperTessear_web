import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

/** Spawn: scatter motes across the WHOLE canvas (no empty-screen start), at rest. */
export declare const floatingParticlesInitKernel: import('typegpu').TgpuFn<(i: d.U32) => d.Void>;
/** Clear the additive render accumulator. */
export declare const floatingParticlesClearAccumKernel: import('typegpu').TgpuFn<(x: d.U32, y: d.U32) => d.Void>;
/**
 * Update integrator. The base drift is recomputed each frame from the per-mote hash (uniform
 * translation — density-preserving), the cursor gust is a kit magnet impulse on the stored
 * velocity decaying back to the drift. Positions wrap toroidally on all four sides — correct here
 * because the motion is pure drift, so coverage stays even (unlike a fluid box, nothing clumps).
 */
export declare const floatingParticlesUpdateKernel: import('typegpu').TgpuFn<(i: d.U32) => d.Void>;
/**
 * Splat factory (point shape baked from the shared kit/agents menu): rasterize one mote into the
 * accumulator by evaluating the shape's coverage per texel in WORLD units, at the drift position
 * plus the orbital-wander offset. `softness` feathers the baked hard shapes toward the glow
 * profile at runtime (the glow shape is always fully soft); twinkle and the per-mote presence
 * factor modulate the deposited energy. Additive atomics are order-independent — no sort needed.
 */
export declare function makeFloatingParticlesSplatKernel(shape: string): import('typegpu').TgpuFn<(i: d.U32) => d.Void>;
export declare const floatingParticlesSplatKernel: import('typegpu').TgpuFn<(i: d.U32) => d.Void>;
/** Resolve: accumulated energy → a single tint's alpha, written to the output texture. Transparent
 *  between motes, so the field composites cleanly over anything. One thread per texel. */
export declare const floatingParticlesResolveKernel: import('typegpu').TgpuFn<(x: d.U32, y: d.U32) => d.Void>;
/** Composite the particles OVER a child colour (straight-alpha over) — kept from v1 so the shader
 *  still works as a wrapping layer without an RTT. */
export declare const particleOverChild: import('typegpu').TgpuFn<(p: d.Vec4f, c: d.Vec4f) => d.Vec4f>;
export interface ComponentProps {
    /**
     * Number of particles
     *
     * Accepts a number within the specified range.
     * @default 1200
     */
    count: number;
    /**
     * Speed of the shared drift
     *
     * Accepts a number between 0 and 1.
     * @default 0.25
     */
    speed: number;
    /**
     * Drift heading in degrees (0 = left, 90 = up, 180 = right, 270 = down — the classic default floats upward)
     *
     * Accepts a number between 0 and 360.
     * @default 90
     */
    angle: number;
    /**
     * Per-particle speed variance around the shared drift (0 = everything moves in lockstep)
     *
     * Accepts a number between 0 and 1.
     * @default 0.3
     */
    speedVariance: number;
    /**
     * Per-particle heading variance in degrees (0 = one shared direction, 180 = every which way)
     *
     * Accepts a number between 0 and 180.
     * @default 30
     */
    angleVariance: number;
    /**
     * Orbital wander — each particle circles lazily around its drift path instead of gliding straight
     *
     * @default 0.25
     */
    randomness: number;
    /**
     * Intensity of the twinkle (0 = steady, 1 = full shimmer)
     *
     * @default 0.5
     */
    twinkle: number;
    /**
     * Size of the particles (each also varies slightly for depth)
     *
     * @default 1.2
     */
    particleSize: number;
    /**
     * Edge softness of each particle — 0 is a crisp shape, 1 a soft glow puff
     *
     * @default 0.1
     */
    softness: number;
    /**
     * What each particle is drawn as — a crisp dot or square (softness still feathers them), or a soft glow puff
     *
     * Accepts one of the predefined option values.
     * @default "dot"
     */
    shape: string;
    /**
     * Color of the particles
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    particleColor: Parameters<typeof transformColor>[0];
    /**
     * How strongly the cursor stirs the field — 0 (default) leaves the drift undisturbed
     *
     * Accepts a number between 0 and 1.
     * @default 0
     */
    cursorStrength: number;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map