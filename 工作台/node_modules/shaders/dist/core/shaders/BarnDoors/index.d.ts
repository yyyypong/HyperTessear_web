import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * How far the doors have opened (0 = fully visible, 1 = fully wiped away)
     *
     * @default 0.5
     */
    progress: number;
    /**
     * Direction the doors open in degrees (0 = apart horizontally)
     *
     * @default 0
     */
    angle: number;
    /**
     * Softness of the wipe edges
     *
     * @default 0.1
     */
    feather: number;
    /**
     * Close in from both edges instead of opening from the center
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    invert: boolean;
}
export declare const barnDoorsMask: import('typegpu').TgpuFn<(color: d.Vec4f, uv: d.Vec2f, aspect: d.F32, angleDeg: d.F32, progress: d.F32, feather: d.F32, invert: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map