import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Base liquid color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#0066ff"
     */
    baseColor: Parameters<typeof transformColor>[0];
    /**
     * Color for upward movement
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#00ff00"
     */
    upColor: Parameters<typeof transformColor>[0];
    /**
     * Color for downward movement
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ff0000"
     */
    downColor: Parameters<typeof transformColor>[0];
    /**
     * Color for leftward movement
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#0000ff"
     */
    leftColor: Parameters<typeof transformColor>[0];
    /**
     * Color for rightward movement
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffff00"
     */
    rightColor: Parameters<typeof transformColor>[0];
    /**
     * Strength of the liquid effect
     *
     * Accepts a number between 0.5 and 1.5.
     * @default 1
     */
    intensity: number;
    /**
     * Radius of the liquid effect
     *
     * Accepts a number between 0 and 5.
     * @default 3
     */
    radius: number;
    /**
     * How much momentum colors retain in their flow direction
     *
     * Accepts a number between 10 and 60.
     * @default 30
     */
    momentum: number;
}
/**
 * ChromaFlow fragment colour: one 5-tap smoothing cross over the packed flow field (r,g = flow
 * vector, b = liquid density) → directional colour from the flow direction, faded to base by density.
 * Samples arrive PRE-SAMPLED as builder-level Exprs; the 5 colours are vec4 uniforms.
 */
/** A field tap UV offset by (dx, dy) — computed in a body (Expr-level UV arithmetic is not fluent). */
export declare const chromaFlowTapUV: import('typegpu').TgpuFn<(uv: d.Vec2f, dx: d.F32, dy: d.F32) => d.Vec2f>;
export declare const chromaFlowColor: import('typegpu').TgpuFn<(s0: d.Vec4f, s1: d.Vec4f, s2: d.Vec4f, s3: d.Vec4f, s4: d.Vec4f, base: d.Vec4f, up: d.Vec4f, down: d.Vec4f, left: d.Vec4f, right: d.Vec4f) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map