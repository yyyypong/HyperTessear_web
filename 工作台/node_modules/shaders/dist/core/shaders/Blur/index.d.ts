import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Intensity of the blur effect
     *
     * @default 50
     */
    intensity: number;
}
/**
 * Blur intensity (UI 0–200) → Gaussian pixel radius. The kit's separable Gaussian then derives
 * its per-pass sigma from this radius (sigmaH = radius × 0.5). The factor is `intensity × 0.36`.
 */
export declare const INTENSITY_TO_RADIUS = 0.36;
export declare const intensityToRadius: (intensity: number) => number;
export type BlurMapChannel = 'luminance' | 'luminanceInverted' | 'alpha' | 'alphaInverted';
/**
 * GPU-free construction of the fill-blur-map node graph (bind-group layout + kernel fn). Mirrors
 * kit/blur's `buildFixedBlurGraph` style so it `tgpu.resolve`s without a device.
 */
export declare function buildFillBlurMapGraph(computeWidth: number, computeHeight: number, channel: BlurMapChannel): {
    layout: import('typegpu').TgpuBindGroupLayout<{
        source: {
            texture: d.WgslTexture2d<d.F32>;
        };
        blurMap: {
            storageTexture: d.WgslStorageTexture2d<"rgba32float", string>;
        };
        params: {
            uniform: d.WgslStruct<{
                inputWidth: d.F32;
                inputHeight: d.F32;
                inputMin: d.F32;
                inputMax: d.F32;
                outputMin: d.F32;
                outputMax: d.F32;
                curve: d.F32;
            }>;
        };
    }>;
    kernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
    Params: d.WgslStruct<{
        inputWidth: d.F32;
        inputHeight: d.F32;
        inputMin: d.F32;
        inputMax: d.F32;
        outputMin: d.F32;
        outputMax: d.F32;
        curve: d.F32;
    }>;
};
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map