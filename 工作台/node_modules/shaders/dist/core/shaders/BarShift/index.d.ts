import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Number of bars across the longest viewport dimension
     *
     * @default 6
     */
    count: number;
    /**
     * Angle of bar orientation in degrees (0 = vertical bars, 90 = horizontal bars)
     *
     * @default 0
     */
    angle: Parameters<typeof transformAngle>[0];
    /**
     * Maximum displacement per bar
     *
     * @default 0.15
     */
    intensity: number;
    /**
     * Randomization seed for per-bar offset variation
     *
     * Accepts a number between 0 and 100.
     * @default 0
     */
    seed: number;
    /**
     * Animation speed — each bar drifts at its own rate and direction
     *
     * Accepts a number between -2 and 2.
     * @default 0
     */
    speed: number;
    /**
     * How to handle edges when distortion pushes content out of bounds
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "mirror"
     */
    edges: string;
}
export declare const barHash: import('typegpu').TgpuFn<(x: d.F32) => d.F32>;
export declare const barSampleUV: import('typegpu').TgpuFn<(uv: d.Vec2f, aspect: d.F32, angle: d.F32, offset: d.F32) => d.Vec2f>;
export declare const barShiftUV: import('typegpu').TgpuFn<(count: d.F32, angle: d.F32, intensity: d.F32, seed: d.F32, animTime: d.F32, uv: d.Vec2f, aspect: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map