import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformPosition } from '../../utilities/transformations';
import { BoundingBoxOrigin, DimensionalValue } from '../../types';

export interface ComponentProps {
    /**
     * Reference edge the center position is measured from (center default). Lets you pin the shape relative to a corner or the canvas centre.
     *
     * @default "center"
     */
    origin: BoundingBoxOrigin;
    /**
     * The color of the circle
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    color: Parameters<typeof transformColor>[0];
    /**
     * The radius of the circle. A value of one (1) is sets the circle to fit the canvas.
     *
     * @default 1
     */
    radius: number | DimensionalValue;
    /**
     * Edge softness. Lower values like zero (0) are sharp, higher values like one (1) are softer.
     *
     * @default 0
     */
    softness: number | DimensionalValue;
    /**
     * The center point of the circle
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * The thickness of the stroke outline. Zero (0) means no stroke.
     *
     * @default 0
     */
    strokeThickness: number;
    /**
     * The color of the stroke outline
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    strokeColor: Parameters<typeof transformColor>[0];
    /**
     * Position of the stroke relative to the circle edge
     *
     * Accepts one of: `"outside"`, `"center"`, `"inside"`.
     * @default "center"
     */
    strokePosition: string;
    /**
     * Color space for blending fill and stroke colors in soft edges
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const circleShape: import('typegpu').TgpuFn<(center: d.Vec2f, radius: d.F32, softness: d.F32, strokeThickness: d.F32, strokePosition: d.F32, uv: d.Vec2f, aspect: d.F32) => d.WgslStruct<{
    overallMask: d.F32;
    strokeBlend: d.F32;
}>>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map