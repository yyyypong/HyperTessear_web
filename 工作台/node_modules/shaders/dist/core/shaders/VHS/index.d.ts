import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Overall amount of tape damage — waves, creases, and head-switching noise. Bursts on and off organically over time.
     *
     * @default 1
     */
    wobble: number;
    /**
     * Per-scanline fine chroma/luma jitter. Adds the classic horizontal-streak detail.
     *
     * @default 0.6
     */
    scanlineNoise: number;
    /**
     * Horizontal chroma smear (color bleed) amount. Positive trails colour to the right (classic VHS), negative trails it to the left.
     *
     * @default 0.2
     */
    smear: number;
    /**
     * Animation speed of the tape effects.
     *
     * Accepts a number between 0.1 and 3.
     * @default 1
     */
    speed: number;
}
export declare const vhsSampleUVs: import('typegpu').TgpuFn<(uv: d.Vec2f, time: d.F32, speed: d.F32, wobble: d.F32, scanlineNoise: d.F32) => d.Vec4f>;
export declare const vhsAcBeat: import('typegpu').TgpuFn<(uv: d.Vec2f, time: d.F32, speed: d.F32, wobble: d.F32) => d.F32>;
export declare const vhsChromaTapUV: import('typegpu').TgpuFn<(chromaUV: d.Vec2f, smearScale: d.F32, i: d.F32) => d.Vec2f>;
export declare const vhsCompose: import('typegpu').TgpuFn<(luma: d.Vec4f, c1: d.Vec4f, c2: d.Vec4f, c3: d.Vec4f, c4: d.Vec4f, c5: d.Vec4f, acBeat: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map