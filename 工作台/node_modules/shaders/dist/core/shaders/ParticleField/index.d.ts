import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

/** Clear the four additive accumulators (2D dispatch over the live outW×outH region). */
export declare const particleFieldClearKernel: import('typegpu').TgpuFn<(x: d.U32, y: d.U32) => d.Void>;
/**
 * Sim factory — bakes the depth-channel extractor. Per particle (2D grid dispatch so the grid
 * coordinates arrive as clean u32 — a 1D index reconstructed via `idx / gw` is unsafe because TGSL
 * transpiles `/` to FLOAT division): bilinear-sample the child at the home UV, unpremultiply, derive
 * the target Z from the chosen channel, then integrate the FORCE-BASED physics — spring toward home
 * (xy) / target Z, cursor magnet impulse, drag, speed clamp. First frame SNAPS to the target (no
 * start lurch).
 */
export declare function makeParticleFieldSimKernel(channelMode: number): import('typegpu').TgpuFn<(gx: d.U32, gy: d.U32) => d.Void>;
/**
 * Splat factory (shape baked from the shared kit/agents menu): rotate the particle's 3D world
 * position by the camera matrix, project through a perspective from screen center (near ones
 * enlarge, brighten and spread outward), then accumulate the shape's coverage as DEPTH-WEIGHTED
 * color into the four fixed-point buffers. The weight (persp²·shade) makes near particles dominate
 * the per-texel average → stylized pseudo-occlusion, no z-sort (additive atomics are
 * order-independent).
 */
export declare function makeParticleFieldSplatKernel(shape: string): import('typegpu').TgpuFn<(gx: d.U32, gy: d.U32) => d.Void>;
export declare const particleFieldSplatKernel: import('typegpu').TgpuFn<(gx: d.U32, gy: d.U32) => d.Void>;
/** Resolve: weighted-average color (RGB/W — the FP scale cancels) + a coverage alpha from W. */
export declare const particleFieldResolveKernel: import('typegpu').TgpuFn<(x: d.U32, y: d.U32) => d.Void>;
export interface ComponentProps {
    /**
     * Number of particles — how finely the child is sampled into the field
     *
     * Accepts a number within the specified range.
     * @default 15000
     */
    count: number;
    /**
     * Which channel of the child drives each particle
     *
     * Accepts one of the predefined option values.
     * @default "luminance"
     */
    depthSource: string;
    /**
     * Depth of the relief — how far bright and dark areas rise toward and sink away from the camera
     *
     * @default 0.7
     */
    depth: number;
    /**
     * What each particle is drawn as — a crisp dot or square, or a soft glow puff
     *
     * Accepts one of the predefined option values.
     * @default "dot"
     */
    particleShape: string;
    /**
     * Gentle idle drift that keeps the field breathing even on a still image
     *
     * @default 0.35
     */
    wobble: number;
    /**
     * Size of each particle relative to the grid spacing
     *
     * @default 0.75
     */
    particleSize: number;
    /**
     * How much far particles dim into the distance — the main cue that sells the 3D relief
     *
     * Accepts a number between 0 and 1.
     * @default 0.6
     */
    depthShading: number;
    /**
     * Camera distance — dolly the whole field closer or further away
     *
     * @default 1
     */
    zoom: number;
    /**
     * Camera orbit around the horizontal axis — tilt the relief toward or away from you
     *
     * @default 0
     */
    rotationX: number;
    /**
     * Camera orbit around the vertical axis — view the particle extrusion from the side
     *
     * @default 0
     */
    rotationY: number;
    /**
     * Camera roll around the view axis
     *
     * @default 0
     */
    rotationZ: number;
    /**
     * Shift the whole field horizontally across the frame (in screen widths)
     *
     * @default 0
     */
    offsetX: number;
    /**
     * Shift the whole field vertically across the frame (in screen heights) — tilt it flat, then sink it toward the bottom like a ground plane
     *
     * @default 0
     */
    offsetY: number;
    /**
     * How the cursor disturbs the field — push throws particles away and toward you, pull gathers them in
     *
     * Accepts one of the predefined option values.
     * @default "push"
     */
    cursorMode: string;
    /**
     * Strength of the cursor force — how hard particles get thrown before springing back
     *
     * Accepts a number between 0 and 2.
     * @default 0.8
     */
    cursorStrength: number;
    /**
     * Reach of the cursor field, in screen units
     *
     * Accepts a number between 0.05 and 1.
     * @default 0.2
     */
    cursorRadius: number;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map