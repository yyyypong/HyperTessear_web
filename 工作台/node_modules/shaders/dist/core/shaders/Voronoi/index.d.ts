import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';
import { ColorStop } from '../../utilities/colorStops';

export type { ColorStop };
export interface ComponentProps {
    /**
     * Color near each cell
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#3186cf"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Color at cell boundaries, far from any center point
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#fc02dd"
     */
    colorB: Parameters<typeof transformColor>[0];
    stops: ColorStop[] | null;
    /**
     * Color of the cell boundary lines
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    colorBorder: Parameters<typeof transformColor>[0];
    /**
     * Number of cells across the canvas
     *
     * @default 6
     */
    scale: number;
    /**
     * Animation speed — how fast the cell points drift
     *
     * Accepts a number between 0 and 5.
     * @default 0.5
     */
    speed: number;
    /**
     * Random seed — shifts the cell pattern without changing the overall structure
     *
     * @default 0
     */
    seed: number;
    /**
     * Controls how much of the cell interior is filled by the edge color. Low = center color dominates with a sharp boundary. High = edge color spreads further into the cell.
     *
     * @default 0.5
     */
    edgeIntensity: number;
    /**
     * Width of the cell boundary lines.
     *
     * @default 0.05
     */
    edgeSoftness: number;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const voronoiField: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, scale: d.F32, animT: d.F32, seed: d.F32, edgeIntensity: d.F32, edgeSoftness: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map