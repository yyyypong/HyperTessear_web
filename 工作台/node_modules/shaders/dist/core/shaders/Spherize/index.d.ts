import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformPosition, transformColor } from '../../utilities/transformations';

/**
 * Sphere geometry: the bulged sample UV + the sphere boundary alpha + the fresnel rim-light scalar,
 * packed as `vec4(transformedUV.xy, sphereAlpha, rimLight)`. The center/lightPos double-flip recovers
 * the authored y; z = sqrt(1 − r²) sphere surface; depth bulge; directional fresnel. Pure — no
 * texture. `center`/`lightPos` are the transformed (x, 1−y) prop values.
 */
export declare const spherizeGeom: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, center: d.Vec2f, radius: d.F32, depth: d.F32, lightPos: d.Vec2f, lightIntensity: d.F32, lightSoftness: d.F32) => d.Vec4f>;
/** Add the rim light to the (straight) child sample + apply the sphere boundary alpha. Pure. */
export declare const spherizeCompose: import('typegpu').TgpuFn<(straight: d.Vec4f, lightColor: d.Vec3f, rimLight: d.F32, sphereAlpha: d.F32) => d.Vec4f>;
export interface ComponentProps {
    /**
     * Radius of the sphere (1 = half viewport height)
     *
     * @default 1
     */
    radius: number;
    /**
     * How much the sphere bulges toward viewer (0 = flat, higher = more bulge)
     *
     * @default 1
     */
    depth: number;
    /**
     * The center point of the sphere
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Position of the specular light source
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.3,"y":0.3}
     */
    lightPosition: Parameters<typeof transformPosition>[0];
    /**
     * Intensity of the rim light (0 = off)
     *
     * @default 0.5
     */
    lightIntensity: number;
    /**
     * Softness of the rim light falloff (0 = hard edge, 1 = soft glow)
     *
     * @default 0.5
     */
    lightSoftness: number;
    /**
     * Color of the specular highlight
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    lightColor: Parameters<typeof transformColor>[0];
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map