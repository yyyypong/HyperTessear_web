import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Edge color at the curtain base
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#a533f8"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Core color in the bright center
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#22ee88"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Tip color at the ray ends
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#1694e8"
     */
    colorC: Parameters<typeof transformColor>[0];
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
    /**
     * Shifts color distribution across the curtain height
     *
     * @default 50
     */
    balance: number;
    /**
     * Overall aurora brightness
     *
     * @default 80
     */
    intensity: number;
    /**
     * Number of aurora curtain layers
     *
     * Accepts a number between 1 and 4.
     * @default 4
     */
    curtainCount: number;
    /**
     * Animation speed
     *
     * Accepts a number between -10 and 10.
     * @default 5
     */
    speed: number;
    /**
     * How much the curtains undulate
     *
     * @default 50
     */
    waviness: number;
    /**
     * Density of vertical ray structures
     *
     * @default 20
     */
    rayDensity: number;
    /**
     * How tall the aurora extends
     *
     * @default 120
     */
    height: number;
    /**
     * Center position of the aurora
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Random seed for variation
     *
     * @default 0
     */
    seed: number;
}
export declare const auroraCurtain: import('typegpu').TgpuFn<(uv: d.Vec2f, layerIdx: d.F32, layerTime: d.F32, centerX: d.F32, centerY: d.F32, aspect: d.F32, waviness: d.F32, rayAmount: d.F32, auroraHeight: d.F32, seed: d.F32, t: d.F32) => d.Vec2f>;
export declare const auroraField: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, animTime: d.F32, seed: d.F32, waviness: d.F32, rayDensity: d.F32, height: d.F32, center: d.Vec2f, balance: d.F32, intensity: d.F32, curtainCount: d.F32) => d.Vec3f>;
export declare const auroraCompose: import('typegpu').TgpuFn<(color: d.Vec4f, brightness: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map