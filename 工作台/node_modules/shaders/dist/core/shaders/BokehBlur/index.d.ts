import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Defocus amount — how far the lens blur spreads
     *
     * @default 50
     */
    radius: number;
    /**
     * How strongly bright highlights bloom into discs
     *
     * @default 4
     */
    highlightGain: number;
    /**
     * Brightness above which a highlight forms a disc
     *
     * @default 0.6
     */
    highlightThreshold: number;
    /**
     * Aperture shape the highlights bloom into — bladed iris or a novelty cut-out (heart, star, …)
     *
     * Accepts one of: `"blades"`, `"circle"`, `"star"`, `"heart"`, `"flower"`, `"cross"`, `"ring"`.
     * @default "blades"
     */
    bladeShape: string;
    /**
     * Blade or point count for the Blades, Star and Flower shapes — 0–2 blades is a circular iris
     *
     * Accepts a number between 0 and 9.
     * @default 6
     */
    bladeCount: number;
    /**
     * Rotation of the aperture shape (in degrees)
     *
     * @default 0
     */
    bladeRotation: Parameters<typeof transformAngle>[0];
    /**
     * Lens colour fringing on the disc edges
     *
     * @default 0.2
     */
    chromaticFringe: number;
}
/** Radius UI (0–100) → gather radius in input/canvas pixels. Max ~80px disc — creamy but bounded so
 *  the fixed tap comb stays dense enough to keep discs clean. */
export declare const RADIUS_SCALE = 0.8;
export declare const radiusToPixels: (radius: number) => number;
/** Fixed gather tap count. Bokeh isn't separable (a shaped disc can't split into two 1D passes),
 *  so taps cost linearly — but the gather runs at the kit blurs' capped compute resolution and the
 *  taps carry NO per-tap trig (positions are CPU-precomputed), so 100 taps lands in the same cost
 *  ballpark as the kit's fixed 2×49-tap separable Gaussian. One tier, no quality knob. */
export declare const TAP_COUNT = 100;
export interface BokehTap {
    x: number;
    y: number;
    rim: number;
}
export declare const BOKEH_SHAPES: readonly ["blades", "circle", "star", "heart", "flower", "cross", "ring"];
export type BokehShape = (typeof BOKEH_SHAPES)[number];
/** Polygon aperture radius along direction `theta` for a regular `sides`-gon inscribed in the unit
 *  circle (vertices at r=1); `sides < 3` → circle. CPU mirror of the old GPU helper. */
export declare const bokehPolygonRadius: (theta: number, sides: number) => number;
/**
 * Precompute `tapCount` gather offsets inside the chosen aperture shape (unit-radius space,
 * unrotated) plus each tap's rim-brightening weight. Circle/blades use the exact Vogel-spiral +
 * polygon-radius math the kernel used to run per tap (pixel-identical look); the other shapes
 * rejection-sample a Halton sequence against the CPU SDF mirrors. Offsets are NEGATED so the
 * VISIBLE disc (a bright point spreads to pixels at `point − offset`) matches the shape's screen
 * orientation — hearts upright, star tips up. Deterministic: same inputs → same table.
 */
export declare function generateBokehTaps(shape: string, bladeCount: number, tapCount: number): BokehTap[];
/** Map-driven channel (the driving scalar the radius-map source contributes per pixel). Same four
 *  channels the fragment map path supports; a build-time constant so the branch is comptime-folded
 *  (Blur's `BlurMapChannel` precedent). */
export type BokehMapChannel = 'luminance' | 'luminanceInverted' | 'alpha' | 'alphaInverted';
/**
 * GPU-free construction of the single-pass bokeh gather node graph (bind-group layout + kernel fn).
 * Bokeh is NOT separable, so unlike the kit's 2-pass Gaussian this scatters-as-gather over the
 * precomputed aperture tap table in ONE pass: for each output pixel it walks `tapCount` taps (unit
 * aperture offsets, rotated by the uniform cos/sin and scaled by the gather radius), boosts bright
 * taps so highlights bloom into aperture-shaped discs (the pre-multiply trick, folded into a per-tap
 * weight), brightens the rim via each tap's precomputed weight, and optionally fringes the R/B
 * channels for a lens look. Mirrors kit/blur's `buildFixedBlurGraph` style (GPU-free, 2D dispatch —
 * no manual bounds guard, SAMPLED `textureLoad` with a mip level for the input, STORAGE
 * `textureStore` for the output).
 */
export declare function buildBokehGraph(computeWidth: number, computeHeight: number, tapCount: number): {
    layout: import('typegpu').TgpuBindGroupLayout<{
        input: {
            texture: d.WgslTexture2d<d.F32>;
        };
        output: {
            storageTexture: d.WgslStorageTexture2d<"rgba16float", string>;
        };
        params: {
            uniform: d.WgslStruct<{
                radius: d.F32;
                highlightGain: d.F32;
                highlightThreshold: d.F32;
                rotCos: d.F32;
                rotSin: d.F32;
                chromaticFringe: d.F32;
                inputWidth: d.F32;
                inputHeight: d.F32;
            }>;
        };
        taps: {
            uniform: d.WgslArray<d.Vec4f>;
        };
    }>;
    kernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
    Params: d.WgslStruct<{
        radius: d.F32;
        highlightGain: d.F32;
        highlightThreshold: d.F32;
        rotCos: d.F32;
        rotSin: d.F32;
        chromaticFringe: d.F32;
        inputWidth: d.F32;
        inputHeight: d.F32;
    }>;
    TapArray: d.WgslArray<d.Vec4f>;
};
/**
 * GPU-free construction of the MAP-DRIVEN gather graph. Identical gather to `buildBokehGraph`, but the
 * per-pixel radius is sampled from the radius-map SOURCE (canvas-resolution RTT), remapped through the
 * same window `resolveMapForProp` uses in the fragment path, then scaled × RADIUS_SCALE. The `source`
 * texture is bound late (its RTT is allocated after composition). Mirrors Glow's map-prepass remap math.
 */
export declare function buildBokehMapGraph(computeWidth: number, computeHeight: number, tapCount: number, channel: BokehMapChannel): {
    layout: import('typegpu').TgpuBindGroupLayout<{
        input: {
            texture: d.WgslTexture2d<d.F32>;
        };
        source: {
            texture: d.WgslTexture2d<d.F32>;
        };
        output: {
            storageTexture: d.WgslStorageTexture2d<"rgba16float", string>;
        };
        params: {
            uniform: d.WgslStruct<{
                highlightGain: d.F32;
                highlightThreshold: d.F32;
                rotCos: d.F32;
                rotSin: d.F32;
                chromaticFringe: d.F32;
                inputWidth: d.F32;
                inputHeight: d.F32;
                inputMin: d.F32;
                inputMax: d.F32;
                outputMin: d.F32;
                outputMax: d.F32;
                curve: d.F32;
            }>;
        };
        taps: {
            uniform: d.WgslArray<d.Vec4f>;
        };
    }>;
    kernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
    Params: d.WgslStruct<{
        highlightGain: d.F32;
        highlightThreshold: d.F32;
        rotCos: d.F32;
        rotSin: d.F32;
        chromaticFringe: d.F32;
        inputWidth: d.F32;
        inputHeight: d.F32;
        inputMin: d.F32;
        inputMax: d.F32;
        outputMin: d.F32;
        outputMax: d.F32;
        curve: d.F32;
    }>;
    TapArray: d.WgslArray<d.Vec4f>;
};
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map