import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Deep background color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#0a0015"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Primary accent color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#6b17e6"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Secondary accent color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ff4d6a"
     */
    colorC: Parameters<typeof transformColor>[0];
    /**
     * Tertiary accent color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ff6b35"
     */
    colorD: Parameters<typeof transformColor>[0];
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "oklch"
     */
    colorSpace: string;
    /**
     * Animation speed
     *
     * Accepts a number between 0 and 10.
     * @default 1
     */
    speed: number;
    /**
     * Organic distortion intensity
     *
     * @default 0.5
     */
    distortion: number;
    /**
     * Random seed for variation
     *
     * @default 0
     */
    seed: number;
}
export declare const flowingFactors: import('typegpu').TgpuFn<(baseUV: d.Vec2f, viewport: d.Vec2f, distortion: d.F32, seed: d.F32, animTime: d.F32) => d.Vec3f>;
export declare const flowingCompose: import('typegpu').TgpuFn<(base: d.Vec4f, litScale: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map