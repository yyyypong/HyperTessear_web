import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * How far the diamonds have grown (0 = fully visible, 1 = fully wiped away)
     *
     * @default 0.5
     */
    progress: number;
    /**
     * Size of each diamond cell as a fraction of the frame width
     *
     * @default 0.15
     */
    size: number;
    /**
     * Softness of each diamond edge
     *
     * @default 0.1
     */
    feather: number;
    /**
     * Wipe from the cell corners inward instead
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    invert: boolean;
}
export declare const diamondWipeMask: import('typegpu').TgpuFn<(color: d.Vec4f, uv: d.Vec2f, aspect: d.F32, size: d.F32, feather: d.F32, progress: d.F32, invert: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map