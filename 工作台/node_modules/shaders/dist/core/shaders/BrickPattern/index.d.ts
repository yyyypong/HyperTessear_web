import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Brick color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    colorBrick: Parameters<typeof transformColor>[0];
    /**
     * Mortar / gap color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    colorMortar: Parameters<typeof transformColor>[0];
    /**
     * Number of bricks per row
     *
     * @default 8
     */
    cellsX: number;
    /**
     * Number of brick rows
     *
     * @default 10
     */
    cellsY: number;
    /**
     * Width of mortar gaps — equal pixel thickness in both directions
     *
     * @default 0.05
     */
    mortar: number;
    /**
     * Softness of the brick edges (0 = crisp, 1 = very soft)
     *
     * @default 0
     */
    softness: number;
    /**
     * Per-brick random lightening/darkening of the brick color
     *
     * @default 0
     */
    variation: number;
    /**
     * Rotation of the pattern in degrees
     *
     * @default 0
     */
    rotation: Parameters<typeof transformAngle>[0];
    /**
     * Animation speed
     *
     * Accepts a number between -2 and 2.
     * @default 0
     */
    speed: number;
    /**
     * Static horizontal offset — shifts the brick pattern without animating
     *
     * @default 0
     */
    offset: number;
    /**
     * How much each row
     *
     * @default 0
     */
    speedVariance: number;
    /**
     * Random seed for per-row speed variation
     *
     * Accepts a number between 0 and 100.
     * @default 0
     */
    seed: number;
    /**
     * Color space for interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const brickField: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, cellsX: d.F32, cellsY: d.F32, mortar: d.F32, softness: d.F32, variation: d.F32, rotation: d.F32, offset: d.F32, speedVariance: d.F32, seed: d.F32, animTime: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map