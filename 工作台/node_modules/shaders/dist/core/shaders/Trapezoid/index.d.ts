import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformPosition } from '../../utilities/transformations';
import { BoundingBoxOrigin, DimensionalValue } from '../../types';

export interface ComponentProps {
    /**
     * Reference edge the center position is measured from (center default). Lets you pin the shape relative to a corner or the canvas centre.
     *
     * @default "center"
     */
    origin: BoundingBoxOrigin;
    /**
     * Fill color of the trapezoid
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    color: Parameters<typeof transformColor>[0];
    /**
     * Center position of the trapezoid
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Half-width of the bottom edge
     *
     * Accepts a number between 0.01 and 1.
     * @default 0.35
     */
    bottomWidth: number | DimensionalValue;
    /**
     * Half-width of the top edge
     *
     * Accepts a number between 0.01 and 1.
     * @default 0.2
     */
    topWidth: number;
    /**
     * Half-height of the trapezoid
     *
     * Accepts a number between 0.01 and 1.
     * @default 0.25
     */
    height: number | DimensionalValue;
    /**
     * Rotation in degrees
     *
     * Accepts a number between 0 and 360.
     * @default 0
     */
    rotation: number;
    /**
     * Edge softness for antialiasing
     *
     * Accepts a number between 0 and 0.1.
     * @default 0
     */
    softness: number;
    /**
     * Stroke thickness. Zero means no stroke.
     *
     * Accepts a number between 0 and 0.2.
     * @default 0
     */
    strokeThickness: number;
    /**
     * Color of the stroke outline
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    strokeColor: Parameters<typeof transformColor>[0];
    /**
     * Position of the stroke relative to the shape edge
     *
     * Accepts one of: `"outside"`, `"center"`, `"inside"`.
     * @default "center"
     */
    strokePosition: string;
    /**
     * Color space for blending fill and stroke colors
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const trapezoidShape: import('typegpu').TgpuFn<(center: d.Vec2f, topWidth: d.F32, bottomWidth: d.F32, height: d.F32, rotation: d.F32, softness: d.F32, strokeThickness: d.F32, strokePosition: d.F32, uv: d.Vec2f, aspect: d.F32) => d.WgslStruct<{
    overallMask: d.F32;
    strokeBlend: d.F32;
}>>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map