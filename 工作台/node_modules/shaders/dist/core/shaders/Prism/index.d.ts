import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformPosition, transformColor } from '../../utilities/transformations';

/**
 * Prism — a beam of light that fans out and splits into a slowly hue-cycling rainbow past a
 * controllable point. Pure generator (transparent background, no children). This is the SHIPPING
 * Prism (a simple inline beam), NOT the orphaned DSOTM spectral-trace `effects/prism.ts`. `speed`
 * drives the hue rotation via the per-node animated clock (negative speed rotates the hue backward —
 * intended).
 */
/** Cyclic hue → RGB (classic ramp) so the rainbow rotates cleanly over time. Pure. */
export declare const prismHue2Rgb: import('typegpu').TgpuFn<(h: d.F32) => d.Vec3f>;
/**
 * The prism beam field: aspect-corrected beam frame anchored at the source, a smooth-relu split
 * elbow, longitudinal fade in/out, a lateral fan profile, and the white→rainbow blend rotating by
 * `animTime`. `position`/`splitPosition` are the transformed (x, 1−y) prop values. Pure.
 */
export declare const prismBeam: import('typegpu').TgpuFn<(uv: d.Vec2f, vp: d.Vec2f, animTime: d.F32, position: d.Vec2f, splitPosition: d.Vec2f, beamColor: d.Vec3f, beamWidth: d.F32, intensity: d.F32, startFalloff: d.F32, endFalloff: d.F32, spread: d.F32, softness: d.F32, saturation: d.F32) => d.Vec4f>;
export interface ComponentProps {
    /**
     * Where the beam originates
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.18,"y":0.18}
     */
    position: Parameters<typeof transformPosition>[0];
    /**
     * Where the beam starts to fan out and split (projected onto the beam axis)
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.42}
     */
    splitPosition: Parameters<typeof transformPosition>[0];
    /**
     * Thickness of the white beam
     *
     * @default 0.04
     */
    beamWidth: number;
    /**
     * How widely the rainbow diffuses as it travels past the split point
     *
     * @default 0.7
     */
    spread: number;
    /**
     * How quickly the white beam transitions into the rainbow
     *
     * @default 0.12
     */
    softness: number;
    /**
     * How softly the beam fades in at its source
     *
     * @default 0.15
     */
    startFalloff: number;
    /**
     * How quickly the rainbow fades out with distance
     *
     * @default 0.6
     */
    endFalloff: number;
    /**
     * Brightness (above 1 blows the core to white)
     *
     * @default 1.6
     */
    intensity: number;
    /**
     * Colour of the beam before it splits
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    beamColor: Parameters<typeof transformColor>[0];
    /**
     * Rainbow saturation — 0 = white, 1 = full colour
     *
     * @default 0.95
     */
    saturation: number;
    /**
     * Speed the hue slowly rotates through the rainbow (0 = static)
     *
     * Accepts a number between -1 and 1.
     * @default 0.1
     */
    speed: number;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map