import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Base background color of the marble
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Secondary marble tone
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#3a2d54"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Deepest marble color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#0f0f0f"
     */
    colorC: Parameters<typeof transformColor>[0];
    /**
     * Scale and density of the marble vein pattern
     *
     * @default 2
     */
    scale: number;
    /**
     * Amount of noise-driven distortion applied to the veins
     *
     * @default 10
     */
    turbulence: number;
    /**
     * Animation speed
     *
     * Accepts a number between 0 and 0.25.
     * @default 0.05
     */
    speed: number;
    /**
     * Random seed for pattern variation
     *
     * @default 0
     */
    seed: number;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const marbleFactors: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, scale: d.F32, turbulence: d.F32, seed: d.F32, animTime: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map