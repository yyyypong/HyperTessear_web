import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Intensity of the film grain noise
     *
     * @default 0.5
     */
    strength: number;
    /**
     * Concentrates grain in darker areas. Higher values focus grain more heavily on shadows; 0 applies grain uniformly.
     *
     * Accepts a number between 0 and 10.
     * @default 2
     */
    bias: number;
    /**
     * When enabled, the grain pattern changes each frame for a dynamic film effect
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    animated: boolean;
}
export declare const filmGrainColor: import('typegpu').TgpuFn<(color: d.Vec4f, uv: d.Vec2f, viewport: d.Vec2f, strength: d.F32, bias: d.F32, animTime: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map