import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';
import { ColorStop } from '../../utilities/colorStops';

export type { ColorStop };
export interface ComponentProps {
    /**
     * Color of fresh smoke
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#e29c8b"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Color smoke transitions to as it ages
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#d517f9"
     */
    colorB: Parameters<typeof transformColor>[0];
    stops: ColorStop[] | null;
    /**
     * How much smoke is emitted as you move the cursor
     *
     * Accepts a number between 0.1 and 2.
     * @default 1
     */
    intensity: number;
    /**
     * Size of smoke puff emitted at cursor
     *
     * Accepts a number between 0.01 and 0.3.
     * @default 0.07
     */
    emitRadius: number;
    /**
     * How much cursor velocity is transferred into the smoke (higher = more directed flow)
     *
     * Accepts a number between 0 and 50.
     * @default 20
     */
    momentum: number;
    /**
     * How fast smoke fades over time
     *
     * Accepts a number between 0.05 and 3.
     * @default 0.5
     */
    dissipation: number;
    /**
     * Fine-scale swirling detail driven by vorticity confinement
     *
     * Accepts a number between 0 and 60.
     * @default 10
     */
    detail: number;
    /**
     * Vertical drift — negative floats up, positive sinks down
     *
     * Accepts a number between -5 and 5.
     * @default 2
     */
    gravity: number;
    /**
     * How quickly smoke shifts from fresh to aged color
     *
     * Accepts a number between 0 and 3.
     * @default 0.5
     */
    colorDecay: number;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "OKLAB"
     */
    colorSpace: string;
}
export declare const splatKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const curlKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const vorticityKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const divergenceKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const jacobiKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const gradSubtractKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const advectVelKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const copyVelKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const advectDensKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const copyDensKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const outputKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map