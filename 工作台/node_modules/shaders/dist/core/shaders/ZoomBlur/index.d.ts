import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Intensity of the zoom blur effect
     *
     * @default 30
     */
    intensity: number;
    /**
     * Center point of the zoom blur
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
}
export declare const BLUR_WEIGHTS: number[];
export declare const TAP_COUNT = 32;
/**
 * Sample coordinate for one zoom-blur tap: scale the offset from `center` by
 * `scale = 1 + radius·(tapIndex/31)` (radius = intensity·0.01), pulling near-center samples out and
 * far samples in for the radial streak. `center` is the ALREADY-transformed prop value
 * (transformPosition stores `(x, 1 - y)`), so `1.0 - center.y` recovers the authored y.
 *
 * The x offset is aspect-corrected (×aspect) then un-corrected (÷aspect) at the end; the two cancel
 * algebraically but are kept in that exact op order so the f32 rounding matches. Pure.
 */
export declare const zoomBlurTapCoord: import('typegpu').TgpuFn<(center: d.Vec2f, intensity: d.F32, uv: d.Vec2f, aspect: d.F32, tapIndex: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map