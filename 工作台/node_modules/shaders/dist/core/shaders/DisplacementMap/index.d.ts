import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * The layer whose pixels drive the displacement — its red/green (or luminance) push the content around
     *
     * @default ""
     */
    source: string;
    /**
     * Overall displacement strength
     *
     * @default 0.3
     */
    amount: number;
    /**
     * How the source drives the push — red→X / green→Y, or luminance along a fixed angle
     *
     * Accepts one of: `"twoAxis"`, `"directional"`.
     * @default "twoAxis"
     */
    channelMode: string;
    /**
     * Push direction in degrees (used by the Luminance channel mode)
     *
     * @default 0
     */
    angle: number;
    /**
     * How to handle edges when distortion pushes content out of bounds
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "mirror"
     */
    edges: string;
}
/** twoAxis — source RED drives horizontal, GREEN drives vertical (0.5 = neutral). */
export declare const dmDisplaceRG: import('typegpu').TgpuFn<(uv: d.Vec2f, src: d.Vec4f, aspect: d.F32, amount: d.F32) => d.Vec2f>;
/** directional — source LUMINANCE pushes along a fixed angle (0.5 = neutral). */
export declare const dmDisplaceLuminance: import('typegpu').TgpuFn<(uv: d.Vec2f, src: d.Vec4f, aspect: d.F32, amount: d.F32, angleDeg: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map