import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Background color between the arcs
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Arc line color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Number of tiles across the shortest canvas edge
     *
     * @default 10
     */
    cells: number;
    /**
     * Thickness of the arc lines
     *
     * @default 2
     */
    thickness: number;
    /**
     * Rotation of the tiling in degrees
     *
     * @default 0
     */
    rotation: Parameters<typeof transformAngle>[0];
    /**
     * Softness of the arc edges (0 = crisp, 1 = very soft)
     *
     * @default 0
     */
    softness: number;
    /**
     * Random seed — changes which tiles flip, producing a different maze pattern
     *
     * @default 0
     */
    seed: number;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
}
export declare const truchetField: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, cells: d.F32, thickness: d.F32, rotation: d.F32, softness: d.F32, seed: d.F32) => d.F32>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map