import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Characters ordered from dense to sparse. First character is used for bright areas, last for dark areas.
     *
     * Accepts a string value.
     * @default "@%#*+=-:."
     */
    characters: string;
    /**
     * Size of each ASCII character cell (normalized to 1080p reference, scales proportionally at other resolutions)
     *
     * Accepts a number between 8 and 100.
     * @default 30
     */
    cellSize: number;
    /**
     * Font family for characters
     *
     * Accepts one of: `"Azeret"`, `"Courier"`, `"Cutive"`, `"Fira"`, `"Geist"`, `"IBM"`, `"JetBrains"`, `"Major"`, `"Martian"`, `"Nova"`, `"Press"`, `"Roboto"`, `"Share"`, `"Silkscreen"`, `"Source"`, `"Space"`, `"Syne"`, `"VT323"`, `"Xanh"`.
     * @default "JetBrains Mono"
     */
    fontFamily: string;
    /**
     * Character size within each cell (1.0 = optimal size, 0.0 = smallest)
     *
     * @default 1
     */
    spacing: number;
    /**
     * Brightness curve adjustment. <1 brightens darks (more light characters), >1 darkens midtones (more dark characters). Use to better fit characters to image brightness range.
     *
     * @default 1
     */
    gamma: number;
    /**
     * Pixels with alpha below this threshold become fully transparent.
     *
     * Accepts a number between 0 and 1.
     * @default 0
     */
    alphaThreshold: number;
    /**
     * When enabled, output alpha matches input alpha. When disabled, pixels above the alpha threshold become fully opaque.
     *
     * Accepts a boolean value (`true` or `false`).
     * @default true
     */
    preserveAlpha: boolean;
}
export declare const asciiGrid: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, cellSize: d.F32, spacing: d.F32) => d.WgslStruct<{
    cellCenter: d.Vec2f;
    cellUV: d.Vec2f;
    isOutside: d.F32;
}>>;
/** Map cell brightness → atlas glyph UV (gamma curve + inverted brightness → char index → atlas cell). */
export declare const asciiAtlasUV: import('typegpu').TgpuFn<(cellRgb: d.Vec3f, gamma: d.F32, charCount: d.F32, atlasSize: d.F32, atlasScale: d.F32, cellUV: d.Vec2f) => d.Vec2f>;
/** Final compose: tint the glyph by the cell colour; transparent on background / out-of-bounds / below-threshold. */
export declare const asciiCompose: import('typegpu').TgpuFn<(asciiRgb: d.Vec3f, cellColor: d.Vec4f, isOutside: d.F32, alphaThreshold: d.F32, preserveAlpha: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map