import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformPosition } from '../../utilities/transformations';
import { BoundingBoxOrigin } from '../../types';

export declare const holoRainbow: import('typegpu').TgpuFn<(t: d.F32) => d.Vec3f>;
export declare const holographicComposite: import('typegpu').TgpuFn<(sC: d.Vec4f, sR: d.Vec4f, sU: d.Vec4f, sdfUV: d.Vec2f, viewportSize: d.Vec2f, p0: d.Vec4f, p1: d.Vec4f, p2: d.Vec4f) => d.Vec4f>;
export interface ComponentProps {
    /**
     * Reference edge the center position is measured from (center default). Lets you pin the shape relative to a corner or the canvas centre.
     *
     * @default "center"
     */
    origin: BoundingBoxOrigin;
    /**
     * Center position of the foil shape
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Scale of the foil shape (1 = default size)
     *
     * Accepts a number between 0.1 and 3.
     * @default 1
     */
    scale: number;
    /**
     * Rotation of the foil shape in degrees
     *
     * Accepts a number between 0 and 360.
     * @default 0
     */
    rotation: number;
    /**
     * Rotates the rainbow spectrum across the foil
     *
     * Accepts a number between 0 and 360.
     * @default 0
     */
    hueShift: number;
    /**
     * Size of the iridescent color patches — higher = smaller, busier patches
     *
     * Accepts a number between 0.3 and 6.
     * @default 1.5
     */
    foilScale: number;
    /**
     * Rainbow saturation — 0 = plain silver foil, 1 = fully spectral
     *
     * Accepts a number between 0 and 1.
     * @default 0.75
     */
    saturation: number;
    /**
     * Matte laminate grain — softens the gloss with fine paper-like noise
     *
     * Accepts a number between 0 and 1.
     * @default 0.25
     */
    roughness: number;
    /**
     * Animation speed — the spectrum drifts across the foil as if the sticker is tilting in your hand. 0 pauses.
     *
     * Accepts a number between 0 and 2.
     * @default 1
     */
    speed: number;
    /**
     * Laminate wrinkles — perturbs the surface so the sheen and rainbow swirl locally instead of lying flat
     *
     * Accepts a number between 0 and 1.
     * @default 0.5
     */
    crinkle: number;
    /**
     * Size of the laminate wrinkles — higher = smaller, denser wrinkles
     *
     * Accepts a number between 0.3 and 6.
     * @default 1
     */
    crinkleScale: number;
    /**
     * Glitter flakes embedded in the foil — tiny facets that refract a shifted hue and twinkle as the spectrum drifts
     *
     * Accepts a number between 0 and 1.
     * @default 0.4
     */
    sparkle: number;
    /**
     * Softness of the shape boundary edge
     *
     * Accepts a number between 0 and 1.
     * @default 0.05
     */
    edgeSoftness: number;
    /**
     * Serialized shape configuration (JSON)
     *
     * @default "DEFAULT_SHAPE_CONFIG"
     */
    shape: string;
    /**
     * URL to a pre-generated SDF .bin file
     *
     * @default ""
     */
    shapeSdfUrl: string;
    /**
     * Active SDF shape type
     *
     * @default ""
     */
    shapeType: string;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map