import { GpuShaderDefinition } from '../../gpu/porters';
import { d, sdf3d } from '../../gpu/kit';
import { transformPosition, transformColor } from '../../utilities/transformations';
import { BoundingBoxOrigin } from '../../types';

type BakedSdf = sdf3d.BakedSdf;
/** Spawn: a loose ball cloud around the origin — the first frames converge it into the shape.
 *  Irrational per-axis strides decorrelate consecutive indices (linear strides through the
 *  fract-based hash leave visible filament alignments in the spawn cloud). */
export declare const particleInitKernel: import('typegpu').TgpuFn<(i: d.U32) => d.Void>;
/** Clear the 3D density grid. */
export declare const particleClearDensityKernel: import('typegpu').TgpuFn<(i: d.U32) => d.Void>;
/** Trilinear density splat (8 corner cells, 6-bit fixed point) — same per-frame grid offset as
 *  the sampler, so splat and read agree within a frame. */
export declare const particleDensityKernel: import('typegpu').TgpuFn<(i: d.U32) => d.Void>;
/** Clear the two additive render accumulators. */
export declare const particleClearAccumKernel: import('typegpu').TgpuFn<(x: d.U32, y: d.U32) => d.Void>;
/**
 * Splat-kernel factory (shape baked from the shared kit/agents menu): project the particle to
 * screen (2D placement rotation + a soft perspective from its z), then accumulate the particle's
 * profile into the energy buffer and a speed-weighted copy into the second buffer (the resolve
 * derives per-texel average speed for the rest→excited color ramp). The `softness` uniform mixes
 * the baked hard profile (kit AA edge) toward the classic Gaussian puff — the glow shape forces
 * full soft. Additive fixed-point atomics are ORDER-INDEPENDENT — no z-sort needed.
 */
export declare function makeParticleSplatKernel(shape: string): import('typegpu').TgpuFn<(i: d.U32) => d.Void>;
export declare const particleSplatKernel: import('typegpu').TgpuFn<(i: d.U32) => d.Void>;
/** Resolve factory: accumulators → color-ramped rgba16f texture (colorSpace variant baked). */
export declare function makeParticleResolveKernel(colorSpaceMode: number): import('typegpu').TgpuFn<(x: d.U32, y: d.U32) => d.Void>;
/**
 * Update-kernel factory. `evalSdf` is the baked shape family — 2D analytic extruded in z, an SVG
 * SDF texture extruded in z, or the full analytic 3D setup (rotation + animated sub-props applied
 * inside) — so ONE integrator serves every shape kind. Forces: density-gradient pressure (even
 * fill), the containment spring through the shape surface, the cursor magnet (xy cylinder, signed),
 * gravity (screen-down, pre-rotated on the CPU), and a hash turbulence kick. Semi-implicit Euler
 * with drag and a speed clamp.
 */
export declare function makeParticleUpdateKernel(evalSdf: BakedSdf): import('typegpu').TgpuFn<(i: d.U32) => d.Void>;
export interface ComponentProps {
    /**
     * Reference edge the center position is measured from (center default). Lets you pin the shape relative to a corner or the canvas centre.
     *
     * @default "center"
     */
    origin: BoundingBoxOrigin;
    /**
     * Center position of the shape
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Scale of the shape (1 = default size)
     *
     * Accepts a number between 0.1 and 3.
     * @default 1
     */
    scale: number;
    /**
     * Rotation of the shape in degrees
     *
     * Accepts a number between 0 and 360.
     * @default 0
     */
    rotation: number;
    /**
     * Color of particles at rest
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#8ec5ff"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Color particles shift toward when agitated — scattering from the cursor or catching up to a moving shape
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ff7ad9"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Number of simulated particles
     *
     * Accepts a number within the specified range.
     * @default 4000
     */
    count: number;
    /**
     * Base particle size (each particle also varies slightly, and grows as it drifts toward the viewer)
     *
     * Accepts a number between 0.6 and 6.
     * @default 1.5
     */
    size: number;
    /**
     * What each particle is drawn as — a crisp dot or square (softness still feathers them), or a soft glow puff
     *
     * Accepts one of the predefined option values.
     * @default "dot"
     */
    particleShape: string;
    /**
     * The pressure pushing particles apart — how strongly they insist on even spacing inside the shape
     *
     * Accepts a number between 0.1 and 3.
     * @default 1
     */
    spread: number;
    /**
     * Idle thermal motion of the swarm — 0 freezes it crystal-still once settled
     *
     * Accepts a number between 0 and 1.
     * @default 0.12
     */
    agitation: number;
    /**
     * How quickly disturbed particles calm back down and settle into place
     *
     * Accepts a number between 0 and 1.
     * @default 0.4
     */
    damping: number;
    /**
     * Downward pull on the swarm — negative floats the particles upward
     *
     * Accepts a number between -2 and 2.
     * @default 0
     */
    gravity: number;
    /**
     * Strength of the cursor field. Positive scatters particles away; negative pulls the swarm toward the cursor
     *
     * Accepts a number between -5 and 5.
     * @default 2
     */
    mouseInfluence: number;
    /**
     * Reach of the cursor field, in shape-local units
     *
     * Accepts a number between 0.05 and 0.8.
     * @default 0.2
     */
    mouseRadius: number;
    /**
     * Brightness of the additive particle glow
     *
     * Accepts a number between 0.2 and 3.
     * @default 1
     */
    exposure: number;
    /**
     * Edge softness of each particle — 0 is a crisp disc, 1 a soft glow puff
     *
     * Accepts a number between 0 and 1.
     * @default 0.1
     */
    softness: number;
    /**
     * Thickness of the particle volume behind flat shapes (3D shapes use their own true depth)
     *
     * Accepts a number between 0.02 and 0.6.
     * @default 0.18
     */
    depth: number;
    /**
     * Color space for the rest→excited color ramp
     *
     * Accepts one of the predefined option values.
     * @default "oklab"
     */
    colorSpace: string;
    /**
     * Serialized shape configuration (JSON)
     *
     * @default "DEFAULT_SHAPE_CONFIG"
     */
    shape: string;
    /**
     * URL to a pre-generated SDF .bin file
     *
     * @default ""
     */
    shapeSdfUrl: string;
    /**
     * Active SDF shape type
     *
     * @default ""
     */
    shapeType: string;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map