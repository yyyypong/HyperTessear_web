import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * The center point for rectangular coordinate conversion
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: {
        x: number;
        y: number;
    } | string;
    /**
     * Scale factor for the rectangular output
     *
     * @default 1
     */
    scale: number;
    /**
     * Blends between original UVs (0) and rectangular coordinates (1)
     *
     * @default 1
     */
    intensity: number;
    /**
     * How to handle edges when distortion pushes content out of bounds
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "transparent"
     */
    edges: string;
}
export declare const rectCoordsUV: import('typegpu').TgpuFn<(center: d.Vec2f, scale: d.F32, uv: d.Vec2f, aspect: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map