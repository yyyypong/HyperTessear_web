import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * How the webcam feed should be sized within the viewport
     *
     * Accepts one of: `"cover"`, `"contain"`, `"fill"`, `"scale-down"`, `"none"`.
     * @default "cover"
     */
    objectFit: string;
    /**
     * Mirror the webcam feed horizontally (selfie mode)
     *
     * Accepts a boolean value (`true` or `false`).
     * @default true
     */
    mirror: boolean;
}
/**
 * Selfie-mode horizontal flip: mirror the sampling U when `mirror > 0`. Runtime (not compile-time) —
 * `mirror` is an ordinary boolean uniform, so toggling it must not recompose. `1 - u` stays within
 * [0,1] so the letterbox alpha-cut on the mirrored UV is unchanged. Exported for unit tests.
 */
export declare const applyMirror: import('typegpu').TgpuFn<(uv: d.Vec2f, mirror: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map