import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { BoundingBoxOrigin, DimensionalValue } from '../../types';
import { transformColor, transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * The text to display (single line)
     *
     * Accepts a string value.
     * @default "Hello World"
     */
    text: string;
    /**
     * Google Fonts family name
     *
     * @default "Inter"
     */
    fontFamily: string;
    /**
     * Font weight (only weights published for the family render true; others are synthesized)
     *
     * @default 400
     */
    fontWeight: number;
    /**
     * Italic style
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    italic: boolean;
    /**
     * Font size as a fraction of canvas height
     *
     * Accepts a number between 0.01 and 0.5.
     * @default 0.07
     */
    fontSize: number | DimensionalValue;
    /**
     * Letter spacing in em units
     *
     * Accepts a number between -0.1 and 0.5.
     * @default 0
     */
    letterSpacing: number;
    /**
     * Line height as a multiple of font size (multi-line text)
     *
     * Accepts a number between 0.8 and 2.5.
     * @default 1.2
     */
    lineHeight: number;
    /**
     * Horizontal alignment of lines within the text block
     *
     * Accepts one of: `"left"`, `"center"`, `"right"`.
     * @default "center"
     */
    textAlign: string;
    /**
     * Wrap width — text wraps to fit; 0 = auto (hug contents, single line per explicit break). Edited via the Typography panel
     *
     * Accepts a number between 0 and 1.
     * @default 0
     */
    width: number | DimensionalValue;
    /**
     * Case transformation applied to the text
     *
     * Accepts one of: `"none"`, `"uppercase"`, `"lowercase"`, `"capitalize"`.
     * @default "none"
     */
    textTransform: string;
    /**
     * Text color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    color: Parameters<typeof transformColor>[0];
    /**
     * Reference edge the center position is measured from (center default). Lets you pin the text relative to a corner or the canvas centre.
     *
     * @default "center"
     */
    origin: BoundingBoxOrigin;
    /**
     * Center position of the text
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
    /**
     * Rotation in degrees
     *
     * Accepts a number between 0 and 360.
     * @default 0
     */
    rotation: number;
}
/**
 * Placement body (Ellipse math): aspect-corrected screen UV → rotated box-local UV + inside mask.
 * `center` is the TRANSFORMED prop (transformPosition stores (x, 1-y)), so `1 - center.y` recovers
 * the authored y — the same double-flip as Twirl/Ellipse. Returns vec3(localU, localV, insideMask);
 * the builder samples the glyph texture at `.xy` and tints via the color uniform. Exported for unit
 * tests. NO V-flip here (see the sample note in `fragment`).
 */
export declare const textField: import('typegpu').TgpuFn<(uv: d.Vec2f, aspect: d.F32, center: d.Vec2f, rotationDeg: d.F32, halfW: d.F32, halfH: d.F32) => d.Vec3f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map