import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Number of pixels along the longest edge (higher = smaller pixels)
     *
     * @default 50
     */
    scale: number;
    /**
     * Space between pixels as a fraction of cell size (0 = no gap, 1 = fully invisible)
     *
     * @default 0
     */
    gap: number;
    /**
     * Roundness of each pixel
     *
     * @default 0
     */
    roundness: number;
}
export declare const pixelateSample: import('typegpu').TgpuFn<(uv: d.Vec2f, aspect: d.F32, scale: d.F32, gap: d.F32, roundness: d.F32) => d.Vec3f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map