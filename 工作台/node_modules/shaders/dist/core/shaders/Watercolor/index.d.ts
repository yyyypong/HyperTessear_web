import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Brush size — radius of the painterly flattening
     *
     * Accepts a number between 1 and 8.
     * @default 3
     */
    radius: number;
    /**
     * Amount the pigment bleeds and wobbles past hard edges
     *
     * @default 1
     */
    bleed: number;
    /**
     * Blend between the original image (0) and the full watercolor effect (1)
     *
     * @default 1
     */
    strength: number;
    /**
     * Strength of the paper grain the pigment settles into
     *
     * @default 0.35
     */
    paper: number;
    /**
     * Tint of the paper grain
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#fbf7ec"
     */
    paperColor: Parameters<typeof transformColor>[0];
}
export declare const watercolorUvBase: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, bleed: d.F32, time: d.F32) => d.Vec2f>;
export declare const watercolorTapUV: import('typegpu').TgpuFn<(uvBase: d.Vec2f, viewport: d.Vec2f, ox: d.F32, oy: d.F32) => d.Vec2f>;
export declare const watercolorGrain: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f) => d.F32>;
export declare const watercolorCompose: import('typegpu').TgpuFn<(sum0: d.Vec3f, sum1: d.Vec3f, sum2: d.Vec3f, sum3: d.Vec3f, sq0: d.Vec3f, sq1: d.Vec3f, sq2: d.Vec3f, sq3: d.Vec3f, invN: d.F32, originalBleed: d.Vec4f, paperColor: d.Vec4f, paper: d.F32, strength: d.F32, grain: d.F32) => d.Vec3f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map