import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * The intensity of the posterization effect (lower is more posterized)
     *
     * @default 5
     */
    intensity: number;
}
export declare const posterizeColor: import('typegpu').TgpuFn<(color: d.Vec4f, steps: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map