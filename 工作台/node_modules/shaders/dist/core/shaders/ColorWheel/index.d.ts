import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Rainbow cycles through the full spectrum; Custom loops through your three chosen colors
     *
     * Accepts one of: `"rainbow"`, `"custom"`.
     * @default "rainbow"
     */
    mode: string;
    /**
     * First color in the cycle
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ff0000"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Second color in the cycle
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#00ff88"
     */
    colorB: Parameters<typeof transformColor>[0];
    /**
     * Third color in the cycle
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#0066ff"
     */
    colorC: Parameters<typeof transformColor>[0];
    /**
     * Number of color cycles across the viewport
     *
     * @default 1
     */
    scale: number;
    /**
     * Direction the gradient flows
     *
     * @default 0
     */
    angle: Parameters<typeof transformAngle>[0];
    /**
     * Speed at which the gradient cycles
     *
     * Accepts a number between -1 and 1.
     * @default 0.05
     */
    speed: number;
    /**
     * Color space for blending between custom colors
     *
     * Accepts one of the predefined option values.
     * @default "oklch"
     */
    colorSpace: string;
}
export declare const colorWheelT: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, angle: d.F32, scale: d.F32, animTime: d.F32) => d.F32>;
export declare const colorWheelRainbow: import('typegpu').TgpuFn<(t: d.F32) => d.Vec4f>;
export declare const colorWheelPhase: import('typegpu').TgpuFn<(t: d.F32) => d.Vec2f>;
export declare const colorWheelSelect: import('typegpu').TgpuFn<(c01: d.Vec4f, c12: d.Vec4f, c20: d.Vec4f, floorT3: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map