import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Mirror the content horizontally (left ↔ right)
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    flipX: boolean;
    /**
     * Mirror the content vertically (top ↔ bottom)
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    flipY: boolean;
}
export declare const flipUV: import('typegpu').TgpuFn<(uv: d.Vec2f, flipX: d.F32, flipY: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map