import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Thickness of crack lines
     *
     * @default 1
     */
    crackWidth: number;
    /**
     * How much shards shift
     *
     * Accepts a number between 0 and 20.
     * @default 4
     */
    intensity: number;
    /**
     * Cursor influence radius
     *
     * Accepts a number between 0.1 and 1.
     * @default 0.4
     */
    radius: number;
    /**
     * How fast shards return to rest
     *
     * Accepts a number between 0.1 and 10.
     * @default 1
     */
    decay: number;
    /**
     * Random seed for pattern
     *
     * Accepts a number between 0 and 50.
     * @default 2
     */
    seed: number;
    /**
     * RGB separation for prismatic glass effect
     *
     * @default 1
     */
    chromaticSplit: number;
    /**
     * How much cracks bend/distort the underlying image
     *
     * @default 5
     */
    refractionStrength: number;
    /**
     * Subtle lighting on tilted shards for 3D depth
     *
     * Accepts a number between 0 and 0.5.
     * @default 0.1
     */
    shardLighting: number;
    /**
     * How to handle edges when displacement pushes content out of bounds
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "mirror"
     */
    edges: string;
}
export declare const voronoiKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
/** UV of cell `idx`'s `field` (0=posX,1=posY,2=dispX,3=dispY) in the 64-wide data texture. */
export declare const cellDataUV: import('typegpu').TgpuFn<(idx: d.F32, field: d.F32) => d.Vec2f>;
export declare const ShatterGeom: d.WgslStruct<{
    crackIntensity: d.F32;
    edgeNormal: d.Vec2f;
    displacedUV: d.Vec2f;
    disp: d.Vec2f;
}>;
export declare const shatterGeom: import('typegpu').TgpuFn<(uv: d.Vec2f, nPosX: d.F32, nPosY: d.F32, sPosX: d.F32, sPosY: d.F32, dispX: d.F32, dispY: d.F32, crackWidth: d.F32) => d.WgslStruct<{
    crackIntensity: d.F32;
    edgeNormal: d.Vec2f;
    displacedUV: d.Vec2f;
    disp: d.Vec2f;
}>>;
/** Per-channel refracted sample UV (channel −1/0/+1 → chromatic split). */
export declare const shatterRefractUV: import('typegpu').TgpuFn<(displacedUV: d.Vec2f, edgeNormal: d.Vec2f, crackIntensity: d.F32, refractionStrength: d.F32, chromaticSplit: d.F32, channel: d.F32) => d.Vec2f>;
/** Final composite: crack refraction blend + tilted-shard lighting (pre-unpremultiply). */
export declare const shatterCompose: import('typegpu').TgpuFn<(normalColor: d.Vec4f, rFinal: d.Vec4f, gFinal: d.Vec4f, bFinal: d.Vec4f, crackIntensity: d.F32, disp: d.Vec2f, shardLighting: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map