import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';

export interface ComponentProps {
    /**
     * Strength of the distortion effect
     *
     * Accepts a number between 0 and 5.
     * @default 1
     */
    intensity: number;
    /**
     * Rate of distortion decay (higher = faster)
     *
     * Accepts a number between 0 and 10.
     * @default 3
     */
    decay: number;
    /**
     * Radius of the distortion effect
     *
     * Accepts a number between 0 and 3.
     * @default 1
     */
    radius: number;
    /**
     * Resolution of the distortion grid (higher = more detailed)
     *
     * @default 20
     */
    gridSize: number;
    /**
     * How to handle edges when distortion pushes content out of bounds
     *
     * Accepts one of: `"stretch"`, `"transparent"`, `"mirror"`, `"wrap"`.
     * @default "stretch"
     */
    edges: string;
}
/** Update kernel factory (grid size baked): per-cell dissipation + Gaussian cursor-velocity splat +
 *  clamp. No neighbour reads → single buffer, no ping-pong. Every cell updated (no border guard). */
export declare function makeUpdateKernel(gridSize: number): import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
/** Output kernel factory (grid size baked): copy the per-cell displacement into the RG texture. */
export declare function makeOutputKernel(gridSize: number): import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
/** Snap a UV to the centre of its distortion-grid cell (aspect-corrected cell counts). */
export declare const gridCellSnap: import('typegpu').TgpuFn<(uv: d.Vec2f, gridSize: d.F32, aspect: d.F32) => d.Vec2f>;
/** Offset a UV by the clamped displacement (±0.1 clamp). */
export declare const gridDistortOffsetUV: import('typegpu').TgpuFn<(uv: d.Vec2f, disp: d.Vec2f) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map