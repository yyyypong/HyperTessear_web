import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Background board color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#000000"
     */
    boardColor: Parameters<typeof transformColor>[0];
    /**
     * Chalk stroke color
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#eceadb"
     */
    chalkColor: Parameters<typeof transformColor>[0];
    /**
     * How readily edges are detected and drawn as chalk strokes
     *
     * @default 0.5
     */
    edgeSensitivity: number;
    /**
     * Thickness of the chalk outline strokes
     *
     * @default 1.5
     */
    edgeThickness: number;
    /**
     * Amount of cross-hatch shading filling darker regions
     *
     * @default 0.05
     */
    shading: number;
    /**
     * Spacing of the cross-hatch lines in pixels
     *
     * @default 16
     */
    hatchScale: number;
    /**
     * Chalk dustiness / grain that breaks up the strokes
     *
     * @default 0.45
     */
    grain: number;
}
export declare const chalkOffsetUV: import('typegpu').TgpuFn<(uv: d.Vec2f, viewport: d.Vec2f, edgeThickness: d.F32, ox: d.F32, oy: d.F32) => d.Vec2f>;
export declare const chalkboardCompose: import('typegpu').TgpuFn<(sobelA: d.Vec4f, sobelB: d.Vec4f, uv: d.Vec2f, viewport: d.Vec2f, misc: d.Vec4f, params: d.Vec4f, boardColor: d.Vec4f, chalkColor: d.Vec4f) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map