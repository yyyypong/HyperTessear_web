import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Intensity of the linear blur effect
     *
     * @default 30
     */
    intensity: number;
    /**
     * Direction of the linear blur (in degrees)
     *
     * @default 0
     */
    angle: Parameters<typeof transformAngle>[0];
}
export declare const BLUR_WEIGHTS: number[];
export declare const TAP_COUNT = 32;
/**
 * Sample coordinate for one linear-blur tap. `tapT ∈ [-0.5, 0.5]` is the centered tap position
 * ((i/31)-0.5). The blur vector is the (aspect-corrected) direction × intensity, normalised to the
 * viewport and doubled. `angleDeg` is degrees (transformAngle is identity-degrees), so
 * the body converts. All scalar infix (avoids vector `.div`).
 */
export declare const linearBlurTapCoord: import('typegpu').TgpuFn<(uv: d.Vec2f, angleDeg: d.F32, intensity: d.F32, viewport: d.Vec2f, tapT: d.F32) => d.Vec2f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map