import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor, transformPosition } from '../../utilities/transformations';
import { ColorStop } from '../../utilities/colorStops';
import { BoundingBoxOrigin } from '../../types';

export type { ColorStop };
export interface ComponentProps {
    /**
     * Reference edge the center position is measured from (center default). Lets you pin the shape relative to a corner or the canvas centre.
     *
     * @default "center"
     */
    origin: BoundingBoxOrigin;
    /**
     * Primary color of the blob
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ff6b35"
     */
    colorA: Parameters<typeof transformColor>[0];
    /**
     * Secondary color of the blob
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#e91e63"
     */
    colorB: Parameters<typeof transformColor>[0];
    stops: ColorStop[] | null;
    /**
     * Size of the blob
     *
     * Accepts a number between 0 and 2.
     * @default 0.5
     */
    size: number;
    /**
     * How organic and blobby the shape is (0 = circle, 1 = very blobby)
     *
     * Accepts a number between 0 and 1.
     * @default 0.5
     */
    deformation: number;
    /**
     * Softness of the blob edges (combines edge width and transition curve)
     *
     * @default 0.5
     */
    softness: number;
    /**
     * Intensity of specular highlight effect
     *
     * @default 0.5
     */
    highlightIntensity: number;
    /**
     * Light direction X component
     *
     * Accepts a number between -1 and 1.
     * @default 0.3
     */
    highlightX: number;
    /**
     * Light direction Y component
     *
     * Accepts a number between -1 and 1.
     * @default -0.3
     */
    highlightY: number;
    /**
     * Light direction Z component
     *
     * Accepts a number between -1 and 1.
     * @default 0.4
     */
    highlightZ: number;
    /**
     * Color of the specular highlight
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffe11a"
     */
    highlightColor: Parameters<typeof transformColor>[0];
    /**
     * Animation speed
     *
     * @default 0.5
     */
    speed: number;
    /**
     * Adjusts the starting state, useful for variation
     *
     * Accepts a number between 0 and 100.
     * @default 1
     */
    seed: number;
    /**
     * Color space for color interpolation
     *
     * Accepts one of the predefined option values.
     * @default "linear"
     */
    colorSpace: string;
    /**
     * The center point of the blob
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0.5,"y":0.5}
     */
    center: Parameters<typeof transformPosition>[0];
}
export declare const blobOrganicRadius: import('typegpu').TgpuFn<(uv: d.Vec2f, size: d.F32, deformation: d.F32, animTime: d.F32) => d.F32>;
export declare const blobField: import('typegpu').TgpuFn<(baseUV: d.Vec2f, viewport: d.Vec2f, center: d.Vec2f, size: d.F32, deformation: d.F32, softness: d.F32, highlightIntensity: d.F32, normLx: d.F32, normLy: d.F32, normLz: d.F32, animTime: d.F32) => d.Vec3f>;
export declare const blobCompose: import('typegpu').TgpuFn<(colorMix: d.Vec4f, highlightColor: d.Vec4f, finalHighlight: d.F32, avgAlpha: d.F32, blobMask: d.F32) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map