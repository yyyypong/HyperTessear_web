import { d } from './index';

/** cover — scale to cover the viewport (never smaller than the viewport). */
export declare const scaleCover: import('typegpu').TgpuFn<(dims: d.Vec2f, vp: d.Vec2f) => d.Vec2f>;
/** contain — scale to fit within the viewport (never larger than the viewport). */
export declare const scaleContain: import('typegpu').TgpuFn<(dims: d.Vec2f, vp: d.Vec2f) => d.Vec2f>;
/** fill — stretch to fill the viewport (uvScale = 1,1). */
export declare const scaleFill: import('typegpu').TgpuFn<() => d.Vec2f>;
/** scale-down — contain, but never scale up past the media's natural pixel size. */
export declare const scaleScaleDown: import('typegpu').TgpuFn<(dims: d.Vec2f, vp: d.Vec2f) => d.Vec2f>;
/** none — display at natural pixel size (1:1). WebcamTexture's 5th mode (Image/Video retire it). */
export declare const scaleNone: import('typegpu').TgpuFn<(dims: d.Vec2f, vp: d.Vec2f) => d.Vec2f>;
/** Centre the UV, divide by the fit scale, re-centre. No Y-flip (see the module note). */
export declare const sampleUV: import('typegpu').TgpuFn<(uv: d.Vec2f, uvScale: d.Vec2f) => d.Vec2f>;
/** Decode the sampled sRGB colour to linear, and cut alpha to 0 outside [0,1]² (letterbox). */
export declare const alphaCutLinear: import('typegpu').TgpuFn<(color: d.Vec4f, uv: d.Vec2f) => d.Vec4f>;
//# sourceMappingURL=media.d.ts.map