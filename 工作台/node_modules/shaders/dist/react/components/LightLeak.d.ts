import { default as React } from 'react';
import { BlendMode, PropDriver, TransformConfig, BoundingBoxConfig, LayoutConfig } from '../../core';
import { ComponentProps } from '../../core/shaders/LightLeak';

export type { ComponentProps };
/**
 * Base props that all shader components have
 */
interface BaseShaderProps {
    children?: React.ReactNode;
    blendMode?: BlendMode;
    opacity?: number;
    visible?: boolean;
    id?: string;
    maskSource?: string;
    maskType?: string;
    renderOrder?: number;
    transform?: Partial<TransformConfig>;
    boundingBox?: Partial<BoundingBoxConfig>;
    flow?: LayoutConfig;
    absolute?: boolean;
    ref?: React.Ref<any>;
}
/**
 * Component-specific props that merge base props with shader-specific props
 * Note: ComponentProps are made optional since they have defaults from the shader definition
 */
type ShaderComponentProps = BaseShaderProps & Partial<Omit<ComponentProps, 'position' | 'spread' | 'intensity' | 'streaks' | 'flicker' | 'seed'>> & {
    position?: ComponentProps['position'] | PropDriver;
    spread?: ComponentProps['spread'] | PropDriver;
    intensity?: ComponentProps['intensity'] | PropDriver;
    streaks?: ComponentProps['streaks'] | PropDriver;
    flicker?: ComponentProps['flicker'] | PropDriver;
    seed?: ComponentProps['seed'] | PropDriver;
};
/**
 * The main React wrapper component for Shader shader nodes
 */
export declare const ShaderComponent: React.FC<ShaderComponentProps>;
export default ShaderComponent;
//# sourceMappingURL=LightLeak.d.ts.map