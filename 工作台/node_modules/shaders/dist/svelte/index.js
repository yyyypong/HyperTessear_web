import "svelte/internal/disclose-version";
import * as $ from "svelte/internal/client";
import { getContext, onDestroy, onMount, setContext } from "svelte";
import { createGpuUniformsMap, debugWarn, getWebGPUSupport, isShadersDebug, isWebGPUSupported, resolveBoundingBox, rootPassthrough, setShadersDebug, shaderRendererGPU } from "../core/index.js";
import { setColorSpaceMode } from "../core/utilities/transformations/index.js";
import { componentDefinition } from "../core/shaders/AngularBlur/index.js";
import { componentDefinition as componentDefinition$1 } from "../core/shaders/Arc/index.js";
import { componentDefinition as componentDefinition$2 } from "../core/shaders/Ascii/index.js";
import { componentDefinition as componentDefinition$3 } from "../core/shaders/Aurora/index.js";
import { componentDefinition as componentDefinition$4 } from "../core/shaders/BarShift/index.js";
import { componentDefinition as componentDefinition$5 } from "../core/shaders/BarnDoors/index.js";
import { componentDefinition as componentDefinition$6 } from "../core/shaders/Beam/index.js";
import { componentDefinition as componentDefinition$7 } from "../core/shaders/Bend/index.js";
import { componentDefinition as componentDefinition$8 } from "../core/shaders/Blob/index.js";
import { componentDefinition as componentDefinition$9 } from "../core/shaders/BlockDissolve/index.js";
import { componentDefinition as componentDefinition$10 } from "../core/shaders/BlockNoise/index.js";
import { componentDefinition as componentDefinition$11 } from "../core/shaders/BlueNoise/index.js";
import { componentDefinition as componentDefinition$12 } from "../core/shaders/Blur/index.js";
import { componentDefinition as componentDefinition$13 } from "../core/shaders/Boids/index.js";
import { componentDefinition as componentDefinition$14 } from "../core/shaders/BokehBlur/index.js";
import { componentDefinition as componentDefinition$15 } from "../core/shaders/BrickPattern/index.js";
import { componentDefinition as componentDefinition$16 } from "../core/shaders/BrightnessContrast/index.js";
import { componentDefinition as componentDefinition$17 } from "../core/shaders/BrushedMetal/index.js";
import { componentDefinition as componentDefinition$18 } from "../core/shaders/Bulge/index.js";
import { componentDefinition as componentDefinition$19 } from "../core/shaders/CRTScreen/index.js";
import { componentDefinition as componentDefinition$20 } from "../core/shaders/CarbonFiber/index.js";
import { componentDefinition as componentDefinition$21 } from "../core/shaders/Chalkboard/index.js";
import { componentDefinition as componentDefinition$22 } from "../core/shaders/ChannelBlur/index.js";
import { componentDefinition as componentDefinition$23 } from "../core/shaders/CheckerWipe/index.js";
import { componentDefinition as componentDefinition$24 } from "../core/shaders/Checkerboard/index.js";
import { componentDefinition as componentDefinition$25 } from "../core/shaders/Chevron/index.js";
import { componentDefinition as componentDefinition$26 } from "../core/shaders/ChromaFlow/index.js";
import { componentDefinition as componentDefinition$27 } from "../core/shaders/ChromaticAberration/index.js";
import { componentDefinition as componentDefinition$28 } from "../core/shaders/Circle/index.js";
import { componentDefinition as componentDefinition$29 } from "../core/shaders/ColorWheel/index.js";
import { componentDefinition as componentDefinition$30 } from "../core/shaders/CompressionArtifacts/index.js";
import { componentDefinition as componentDefinition$31 } from "../core/shaders/ConcentricSpin/index.js";
import { componentDefinition as componentDefinition$32 } from "../core/shaders/ConicGradient/index.js";
import { componentDefinition as componentDefinition$33 } from "../core/shaders/ContourLines/index.js";
import { componentDefinition as componentDefinition$34 } from "../core/shaders/CornerPin/index.js";
import { componentDefinition as componentDefinition$35 } from "../core/shaders/Crescent/index.js";
import { componentDefinition as componentDefinition$36 } from "../core/shaders/Cross/index.js";
import { componentDefinition as componentDefinition$37 } from "../core/shaders/Crystal/index.js";
import { componentDefinition as componentDefinition$38 } from "../core/shaders/CurlNoise/index.js";
import { componentDefinition as componentDefinition$39 } from "../core/shaders/CursorRipples/index.js";
import { componentDefinition as componentDefinition$40 } from "../core/shaders/CursorTrail/index.js";
import { componentDefinition as componentDefinition$41 } from "../core/shaders/DataMosh/index.js";
import { componentDefinition as componentDefinition$42 } from "../core/shaders/DiamondGradient/index.js";
import { componentDefinition as componentDefinition$43 } from "../core/shaders/DiamondWipe/index.js";
import { componentDefinition as componentDefinition$44 } from "../core/shaders/DiffuseBlur/index.js";
import { componentDefinition as componentDefinition$45 } from "../core/shaders/DisplacementMap/index.js";
import { componentDefinition as componentDefinition$46 } from "../core/shaders/Dither/index.js";
import { componentDefinition as componentDefinition$47 } from "../core/shaders/DotGrid/index.js";
import { componentDefinition as componentDefinition$48 } from "../core/shaders/DropShadow/index.js";
import { componentDefinition as componentDefinition$49 } from "../core/shaders/Duotone/index.js";
import { componentDefinition as componentDefinition$50 } from "../core/shaders/Ellipse/index.js";
import { componentDefinition as componentDefinition$51 } from "../core/shaders/Emboss/index.js";
import { componentDefinition as componentDefinition$52 } from "../core/shaders/Engraving/index.js";
import { componentDefinition as componentDefinition$53 } from "../core/shaders/ErosionNoise/index.js";
import { componentDefinition as componentDefinition$54 } from "../core/shaders/Exposure/index.js";
import { componentDefinition as componentDefinition$55 } from "../core/shaders/FallingLines/index.js";
import { componentDefinition as componentDefinition$56 } from "../core/shaders/FilmGrain/index.js";
import { componentDefinition as componentDefinition$57 } from "../core/shaders/FilmStock/index.js";
import { componentDefinition as componentDefinition$58 } from "../core/shaders/Flip/index.js";
import { componentDefinition as componentDefinition$59 } from "../core/shaders/FloatingParticles/index.js";
import { componentDefinition as componentDefinition$60 } from "../core/shaders/FlowField/index.js";
import { componentDefinition as componentDefinition$61 } from "../core/shaders/Flower/index.js";
import { componentDefinition as componentDefinition$62 } from "../core/shaders/FlowingGradient/index.js";
import { componentDefinition as componentDefinition$63 } from "../core/shaders/FlutedGlass/index.js";
import { componentDefinition as componentDefinition$64 } from "../core/shaders/Fog/index.js";
import { componentDefinition as componentDefinition$65 } from "../core/shaders/Form3D/index.js";
import { componentDefinition as componentDefinition$66 } from "../core/shaders/FractalNoise/index.js";
import { componentDefinition as componentDefinition$67 } from "../core/shaders/Frost/index.js";
import { componentDefinition as componentDefinition$68 } from "../core/shaders/GaborNoise/index.js";
import { componentDefinition as componentDefinition$69 } from "../core/shaders/Glass/index.js";
import { componentDefinition as componentDefinition$70 } from "../core/shaders/GlassTiles/index.js";
import { componentDefinition as componentDefinition$71 } from "../core/shaders/Glitch/index.js";
import { componentDefinition as componentDefinition$72 } from "../core/shaders/Glow/index.js";
import { componentDefinition as componentDefinition$73 } from "../core/shaders/Godrays/index.js";
import { componentDefinition as componentDefinition$74 } from "../core/shaders/Goo/index.js";
import { componentDefinition as componentDefinition$75 } from "../core/shaders/GradientMap/index.js";
import { componentDefinition as componentDefinition$76 } from "../core/shaders/Grayscale/index.js";
import { componentDefinition as componentDefinition$77 } from "../core/shaders/Grid/index.js";
import { componentDefinition as componentDefinition$78 } from "../core/shaders/GridDistortion/index.js";
import { componentDefinition as componentDefinition$79 } from "../core/shaders/Group/index.js";
import { componentDefinition as componentDefinition$80 } from "../core/shaders/HTMLInCanvas/index.js";
import { componentDefinition as componentDefinition$81 } from "../core/shaders/Halftone/index.js";
import { componentDefinition as componentDefinition$82 } from "../core/shaders/Heart/index.js";
import { componentDefinition as componentDefinition$83 } from "../core/shaders/Heatmap/index.js";
import { componentDefinition as componentDefinition$84 } from "../core/shaders/HexGrid/index.js";
import { componentDefinition as componentDefinition$85 } from "../core/shaders/Hologram/index.js";
import { componentDefinition as componentDefinition$86 } from "../core/shaders/Holographic/index.js";
import { componentDefinition as componentDefinition$87 } from "../core/shaders/HueShift/index.js";
import { componentDefinition as componentDefinition$88 } from "../core/shaders/ImageTexture/index.js";
import { componentDefinition as componentDefinition$89 } from "../core/shaders/InkFlow/index.js";
import { componentDefinition as componentDefinition$90 } from "../core/shaders/Invert/index.js";
import { componentDefinition as componentDefinition$91 } from "../core/shaders/IrisWipe/index.js";
import { componentDefinition as componentDefinition$92 } from "../core/shaders/IsometricCubes/index.js";
import { componentDefinition as componentDefinition$93 } from "../core/shaders/Kaleidoscope/index.js";
import { componentDefinition as componentDefinition$94 } from "../core/shaders/KeyFrames/index.js";
import { componentDefinition as componentDefinition$95 } from "../core/shaders/LensFlare/index.js";
import { componentDefinition as componentDefinition$96 } from "../core/shaders/LightEdge/index.js";
import { componentDefinition as componentDefinition$97 } from "../core/shaders/LightLeak/index.js";
import { componentDefinition as componentDefinition$98 } from "../core/shaders/LinearBlur/index.js";
import { componentDefinition as componentDefinition$99 } from "../core/shaders/LinearGradient/index.js";
import { componentDefinition as componentDefinition$100 } from "../core/shaders/LinearWipe/index.js";
import { componentDefinition as componentDefinition$101 } from "../core/shaders/LiquidMetal/index.js";
import { componentDefinition as componentDefinition$102 } from "../core/shaders/Liquify/index.js";
import { componentDefinition as componentDefinition$103 } from "../core/shaders/MagneticFilings/index.js";
import { componentDefinition as componentDefinition$104 } from "../core/shaders/Marble/index.js";
import { componentDefinition as componentDefinition$105 } from "../core/shaders/Mirror/index.js";
import { componentDefinition as componentDefinition$106 } from "../core/shaders/MultiPointGradient/index.js";
import { componentDefinition as componentDefinition$107 } from "../core/shaders/Neon/index.js";
import { componentDefinition as componentDefinition$108 } from "../core/shaders/NoiseDissolve/index.js";
import { componentDefinition as componentDefinition$109 } from "../core/shaders/ObjectTracker/index.js";
import { componentDefinition as componentDefinition$110 } from "../core/shaders/PagePeel/index.js";
import { componentDefinition as componentDefinition$111 } from "../core/shaders/Paper/index.js";
import { componentDefinition as componentDefinition$112 } from "../core/shaders/Parallelogram/index.js";
import { componentDefinition as componentDefinition$113 } from "../core/shaders/ParticleField/index.js";
import { componentDefinition as componentDefinition$114 } from "../core/shaders/ParticleFlow/index.js";
import { componentDefinition as componentDefinition$115 } from "../core/shaders/Particles/index.js";
import { componentDefinition as componentDefinition$116 } from "../core/shaders/PerlinNoise/index.js";
import { componentDefinition as componentDefinition$117 } from "../core/shaders/Perspective/index.js";
import { componentDefinition as componentDefinition$118 } from "../core/shaders/PixelSort/index.js";
import { componentDefinition as componentDefinition$119 } from "../core/shaders/PixelThrow/index.js";
import { componentDefinition as componentDefinition$120 } from "../core/shaders/Pixelate/index.js";
import { componentDefinition as componentDefinition$121 } from "../core/shaders/Plasma/index.js";
import { componentDefinition as componentDefinition$122 } from "../core/shaders/Plastic/index.js";
import { componentDefinition as componentDefinition$123 } from "../core/shaders/PolarCoordinates/index.js";
import { componentDefinition as componentDefinition$124 } from "../core/shaders/Polygon/index.js";
import { componentDefinition as componentDefinition$125 } from "../core/shaders/Posterize/index.js";
import { componentDefinition as componentDefinition$126 } from "../core/shaders/Prism/index.js";
import { componentDefinition as componentDefinition$127 } from "../core/shaders/ProgressiveBlur/index.js";
import { componentDefinition as componentDefinition$128 } from "../core/shaders/RadialGradient/index.js";
import { componentDefinition as componentDefinition$129 } from "../core/shaders/RadialWipe/index.js";
import { componentDefinition as componentDefinition$130 } from "../core/shaders/RandomBars/index.js";
import { componentDefinition as componentDefinition$131 } from "../core/shaders/ReactionDiffusion/index.js";
import { componentDefinition as componentDefinition$132 } from "../core/shaders/RectangularCoordinates/index.js";
import { componentDefinition as componentDefinition$133 } from "../core/shaders/ReflectivePlane/index.js";
import { componentDefinition as componentDefinition$134 } from "../core/shaders/Repeater/index.js";
import { componentDefinition as componentDefinition$135 } from "../core/shaders/Ring/index.js";
import { componentDefinition as componentDefinition$136 } from "../core/shaders/RippleWipe/index.js";
import { componentDefinition as componentDefinition$137 } from "../core/shaders/Ripples/index.js";
import { componentDefinition as componentDefinition$138 } from "../core/shaders/RoundedRect/index.js";
import { componentDefinition as componentDefinition$139 } from "../core/shaders/Saturation/index.js";
import { componentDefinition as componentDefinition$140 } from "../core/shaders/Scratches/index.js";
import { componentDefinition as componentDefinition$141 } from "../core/shaders/Sharpness/index.js";
import { componentDefinition as componentDefinition$142 } from "../core/shaders/Shatter/index.js";
import { componentDefinition as componentDefinition$143 } from "../core/shaders/SimplexNoise/index.js";
import { componentDefinition as componentDefinition$144 } from "../core/shaders/SineWave/index.js";
import { componentDefinition as componentDefinition$145 } from "../core/shaders/SliceWipe/index.js";
import { componentDefinition as componentDefinition$146 } from "../core/shaders/Smoke/index.js";
import { componentDefinition as componentDefinition$147 } from "../core/shaders/SmokeFill/index.js";
import { componentDefinition as componentDefinition$148 } from "../core/shaders/SmokeFlow/index.js";
import { componentDefinition as componentDefinition$149 } from "../core/shaders/Solarize/index.js";
import { componentDefinition as componentDefinition$150 } from "../core/shaders/SolidColor/index.js";
import { componentDefinition as componentDefinition$151 } from "../core/shaders/Spherize/index.js";
import { componentDefinition as componentDefinition$152 } from "../core/shaders/Spiral/index.js";
import { componentDefinition as componentDefinition$153 } from "../core/shaders/Star/index.js";
import { componentDefinition as componentDefinition$154 } from "../core/shaders/Stone/index.js";
import { componentDefinition as componentDefinition$155 } from "../core/shaders/Strands/index.js";
import { componentDefinition as componentDefinition$156 } from "../core/shaders/Stretch/index.js";
import { componentDefinition as componentDefinition$157 } from "../core/shaders/Stripes/index.js";
import { componentDefinition as componentDefinition$158 } from "../core/shaders/StudioBackground/index.js";
import { componentDefinition as componentDefinition$159 } from "../core/shaders/SunBurst/index.js";
import { componentDefinition as componentDefinition$160 } from "../core/shaders/Surface3D/index.js";
import { componentDefinition as componentDefinition$161 } from "../core/shaders/Swirl/index.js";
import { componentDefinition as componentDefinition$162 } from "../core/shaders/Teardrop/index.js";
import { componentDefinition as componentDefinition$163 } from "../core/shaders/Text/index.js";
import { componentDefinition as componentDefinition$164 } from "../core/shaders/ThinFilm/index.js";
import { componentDefinition as componentDefinition$165 } from "../core/shaders/TiltShift/index.js";
import { componentDefinition as componentDefinition$166 } from "../core/shaders/TimeTrail/index.js";
import { componentDefinition as componentDefinition$167 } from "../core/shaders/Tint/index.js";
import { componentDefinition as componentDefinition$168 } from "../core/shaders/Trapezoid/index.js";
import { componentDefinition as componentDefinition$169 } from "../core/shaders/TriangularGrid/index.js";
import { componentDefinition as componentDefinition$170 } from "../core/shaders/Tritone/index.js";
import { componentDefinition as componentDefinition$171 } from "../core/shaders/Truchet/index.js";
import { componentDefinition as componentDefinition$172 } from "../core/shaders/Twirl/index.js";
import { componentDefinition as componentDefinition$173 } from "../core/shaders/VHS/index.js";
import { componentDefinition as componentDefinition$174 } from "../core/shaders/VenetianBlinds/index.js";
import { componentDefinition as componentDefinition$175 } from "../core/shaders/Vesica/index.js";
import { componentDefinition as componentDefinition$176 } from "../core/shaders/Vibrance/index.js";
import { componentDefinition as componentDefinition$177 } from "../core/shaders/VideoTexture/index.js";
import { componentDefinition as componentDefinition$178 } from "../core/shaders/Vignette/index.js";
import { componentDefinition as componentDefinition$179 } from "../core/shaders/Voronoi/index.js";
import { componentDefinition as componentDefinition$180 } from "../core/shaders/Water/index.js";
import { componentDefinition as componentDefinition$181 } from "../core/shaders/Watercolor/index.js";
import { componentDefinition as componentDefinition$182 } from "../core/shaders/WaveDistortion/index.js";
import { componentDefinition as componentDefinition$183 } from "../core/shaders/WaveletNoise/index.js";
import { componentDefinition as componentDefinition$184 } from "../core/shaders/Weave/index.js";
import { componentDefinition as componentDefinition$185 } from "../core/shaders/WebcamTexture/index.js";
import { componentDefinition as componentDefinition$186 } from "../core/shaders/Wool/index.js";
import { componentDefinition as componentDefinition$187 } from "../core/shaders/WorleyNoise/index.js";
import { componentDefinition as componentDefinition$188 } from "../core/shaders/ZoomBlur/index.js";
import { isExternalUser, startTelemetry } from "../core/telemetry/index.js";
var root_2$188 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$189 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function AngularBlur($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition && typeof componentDefinition.fragment === "function") {
			parentRegister(instanceId, componentDefinition.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition,
			fragment: componentDefinition?.fragment,
			type: typeof componentDefinition?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$189();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$188();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$187 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$188 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Arc($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$1.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$1.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$1.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$1.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$1.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$1, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$1.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$1 && typeof componentDefinition$1.fragment === "function") {
			parentRegister(instanceId, componentDefinition$1.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$1, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$1,
			fragment: componentDefinition$1?.fragment,
			type: typeof componentDefinition$1?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$188();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$187();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$186 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$187 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Ascii($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$2.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$2.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$2.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$2.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$2.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$2, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$2.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$2 && typeof componentDefinition$2.fragment === "function") {
			parentRegister(instanceId, componentDefinition$2.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$2, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$2,
			fragment: componentDefinition$2?.fragment,
			type: typeof componentDefinition$2?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$187();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$186();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$185 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$186 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Aurora($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$3.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$3.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$3.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$3.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$3.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$3, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$3.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$3 && typeof componentDefinition$3.fragment === "function") {
			parentRegister(instanceId, componentDefinition$3.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$3, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$3,
			fragment: componentDefinition$3?.fragment,
			type: typeof componentDefinition$3?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$186();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$185();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$184 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$185 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function BarShift($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$4.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$4.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$4.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$4.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$4.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$4, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$4.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$4 && typeof componentDefinition$4.fragment === "function") {
			parentRegister(instanceId, componentDefinition$4.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$4, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$4,
			fragment: componentDefinition$4?.fragment,
			type: typeof componentDefinition$4?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$185();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$184();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$183 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$184 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function BarnDoors($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$5.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$5.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$5.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$5.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$5.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$5, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$5.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$5 && typeof componentDefinition$5.fragment === "function") {
			parentRegister(instanceId, componentDefinition$5.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$5, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$5,
			fragment: componentDefinition$5?.fragment,
			type: typeof componentDefinition$5?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$184();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$183();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$182 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$183 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Beam($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$6.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$6.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$6.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$6.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$6.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$6, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$6.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$6 && typeof componentDefinition$6.fragment === "function") {
			parentRegister(instanceId, componentDefinition$6.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$6, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$6,
			fragment: componentDefinition$6?.fragment,
			type: typeof componentDefinition$6?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$183();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$182();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$181 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$182 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Bend($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$7.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$7.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$7.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$7.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$7.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$7, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$7.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$7 && typeof componentDefinition$7.fragment === "function") {
			parentRegister(instanceId, componentDefinition$7.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$7, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$7,
			fragment: componentDefinition$7?.fragment,
			type: typeof componentDefinition$7?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$182();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$181();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$180 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$181 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Blob($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$8.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$8.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$8.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$8.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$8.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$8, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$8.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$8 && typeof componentDefinition$8.fragment === "function") {
			parentRegister(instanceId, componentDefinition$8.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$8, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$8,
			fragment: componentDefinition$8?.fragment,
			type: typeof componentDefinition$8?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$181();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$180();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$179 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$180 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function BlockDissolve($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$9.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$9.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$9.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$9.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$9.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$9, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$9.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$9 && typeof componentDefinition$9.fragment === "function") {
			parentRegister(instanceId, componentDefinition$9.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$9, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$9,
			fragment: componentDefinition$9?.fragment,
			type: typeof componentDefinition$9?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$180();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$179();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$178 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$179 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function BlockNoise($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$10.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$10.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$10.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$10.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$10.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$10, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$10.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$10 && typeof componentDefinition$10.fragment === "function") {
			parentRegister(instanceId, componentDefinition$10.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$10, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$10,
			fragment: componentDefinition$10?.fragment,
			type: typeof componentDefinition$10?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$179();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$178();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$177 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$178 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function BlueNoise($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$11.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$11.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$11.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$11.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$11.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$11, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$11.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$11 && typeof componentDefinition$11.fragment === "function") {
			parentRegister(instanceId, componentDefinition$11.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$11, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$11,
			fragment: componentDefinition$11?.fragment,
			type: typeof componentDefinition$11?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$178();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$177();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$176 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$177 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Blur($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$12.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$12.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$12.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$12.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$12.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$12, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$12.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$12 && typeof componentDefinition$12.fragment === "function") {
			parentRegister(instanceId, componentDefinition$12.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$12, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$12,
			fragment: componentDefinition$12?.fragment,
			type: typeof componentDefinition$12?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$177();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$176();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$175 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$176 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Boids($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$13.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$13.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$13.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$13.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$13.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$13, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$13.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$13 && typeof componentDefinition$13.fragment === "function") {
			parentRegister(instanceId, componentDefinition$13.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$13, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$13,
			fragment: componentDefinition$13?.fragment,
			type: typeof componentDefinition$13?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$176();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$175();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$174 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$175 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function BokehBlur($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$14.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$14.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$14.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$14.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$14.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$14, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$14.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$14 && typeof componentDefinition$14.fragment === "function") {
			parentRegister(instanceId, componentDefinition$14.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$14, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$14,
			fragment: componentDefinition$14?.fragment,
			type: typeof componentDefinition$14?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$175();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$174();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$173 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$174 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function BrickPattern($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$15.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$15.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$15.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$15.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$15.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$15, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$15.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$15 && typeof componentDefinition$15.fragment === "function") {
			parentRegister(instanceId, componentDefinition$15.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$15, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$15,
			fragment: componentDefinition$15?.fragment,
			type: typeof componentDefinition$15?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$174();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$173();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$172 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$173 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function BrightnessContrast($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$16.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$16.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$16.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$16.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$16.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$16, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$16.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$16 && typeof componentDefinition$16.fragment === "function") {
			parentRegister(instanceId, componentDefinition$16.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$16, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$16,
			fragment: componentDefinition$16?.fragment,
			type: typeof componentDefinition$16?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$173();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$172();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$171 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$172 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function BrushedMetal($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$17.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$17.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$17.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$17.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$17.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$17, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$17.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$17 && typeof componentDefinition$17.fragment === "function") {
			parentRegister(instanceId, componentDefinition$17.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$17, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$17,
			fragment: componentDefinition$17?.fragment,
			type: typeof componentDefinition$17?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$172();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$171();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$170 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$171 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Bulge($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$18.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$18.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$18.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$18.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$18.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$18, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$18.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$18 && typeof componentDefinition$18.fragment === "function") {
			parentRegister(instanceId, componentDefinition$18.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$18, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$18,
			fragment: componentDefinition$18?.fragment,
			type: typeof componentDefinition$18?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$171();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$170();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$169 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$170 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function CRTScreen($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$19.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$19.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$19.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$19.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$19.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$19, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$19.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$19 && typeof componentDefinition$19.fragment === "function") {
			parentRegister(instanceId, componentDefinition$19.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$19, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$19,
			fragment: componentDefinition$19?.fragment,
			type: typeof componentDefinition$19?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$170();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$169();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$168 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$169 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function CarbonFiber($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$20.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$20.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$20.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$20.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$20.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$20, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$20.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$20 && typeof componentDefinition$20.fragment === "function") {
			parentRegister(instanceId, componentDefinition$20.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$20, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$20,
			fragment: componentDefinition$20?.fragment,
			type: typeof componentDefinition$20?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$169();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$168();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$167 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$168 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Chalkboard($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$21.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$21.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$21.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$21.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$21.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$21, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$21.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$21 && typeof componentDefinition$21.fragment === "function") {
			parentRegister(instanceId, componentDefinition$21.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$21, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$21,
			fragment: componentDefinition$21?.fragment,
			type: typeof componentDefinition$21?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$168();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$167();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$166 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$167 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ChannelBlur($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$22.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$22.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$22.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$22.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$22.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$22, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$22.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$22 && typeof componentDefinition$22.fragment === "function") {
			parentRegister(instanceId, componentDefinition$22.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$22, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$22,
			fragment: componentDefinition$22?.fragment,
			type: typeof componentDefinition$22?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$167();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$166();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$165 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$166 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function CheckerWipe($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$23.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$23.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$23.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$23.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$23.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$23, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$23.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$23 && typeof componentDefinition$23.fragment === "function") {
			parentRegister(instanceId, componentDefinition$23.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$23, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$23,
			fragment: componentDefinition$23?.fragment,
			type: typeof componentDefinition$23?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$166();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$165();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$164 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$165 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Checkerboard($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$24.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$24.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$24.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$24.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$24.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$24, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$24.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$24 && typeof componentDefinition$24.fragment === "function") {
			parentRegister(instanceId, componentDefinition$24.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$24, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$24,
			fragment: componentDefinition$24?.fragment,
			type: typeof componentDefinition$24?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$165();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$164();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$163 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$164 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Chevron($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$25.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$25.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$25.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$25.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$25.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$25, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$25.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$25 && typeof componentDefinition$25.fragment === "function") {
			parentRegister(instanceId, componentDefinition$25.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$25, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$25,
			fragment: componentDefinition$25?.fragment,
			type: typeof componentDefinition$25?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$164();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$163();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$162 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$163 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ChromaFlow($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$26.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$26.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$26.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$26.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$26.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$26, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$26.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$26 && typeof componentDefinition$26.fragment === "function") {
			parentRegister(instanceId, componentDefinition$26.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$26, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$26,
			fragment: componentDefinition$26?.fragment,
			type: typeof componentDefinition$26?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$163();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$162();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$161 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$162 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ChromaticAberration($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$27.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$27.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$27.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$27.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$27.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$27, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$27.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$27 && typeof componentDefinition$27.fragment === "function") {
			parentRegister(instanceId, componentDefinition$27.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$27, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$27,
			fragment: componentDefinition$27?.fragment,
			type: typeof componentDefinition$27?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$162();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$161();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$160 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$161 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Circle($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$28.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$28.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$28.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$28.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$28.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$28, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$28.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$28 && typeof componentDefinition$28.fragment === "function") {
			parentRegister(instanceId, componentDefinition$28.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$28, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$28,
			fragment: componentDefinition$28?.fragment,
			type: typeof componentDefinition$28?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$161();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$160();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$159 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$160 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ColorWheel($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$29.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$29.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$29.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$29.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$29.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$29, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$29.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$29 && typeof componentDefinition$29.fragment === "function") {
			parentRegister(instanceId, componentDefinition$29.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$29, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$29,
			fragment: componentDefinition$29?.fragment,
			type: typeof componentDefinition$29?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$160();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$159();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$158 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$159 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function CompressionArtifacts($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$30.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$30.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$30.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$30.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$30.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$30, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$30.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$30 && typeof componentDefinition$30.fragment === "function") {
			parentRegister(instanceId, componentDefinition$30.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$30, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$30,
			fragment: componentDefinition$30?.fragment,
			type: typeof componentDefinition$30?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$159();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$158();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$157 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$158 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ConcentricSpin($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$31.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$31.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$31.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$31.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$31.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$31, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$31.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$31 && typeof componentDefinition$31.fragment === "function") {
			parentRegister(instanceId, componentDefinition$31.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$31, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$31,
			fragment: componentDefinition$31?.fragment,
			type: typeof componentDefinition$31?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$158();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$157();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$156 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$157 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ConicGradient($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$32.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$32.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$32.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$32.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$32.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$32, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$32.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$32 && typeof componentDefinition$32.fragment === "function") {
			parentRegister(instanceId, componentDefinition$32.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$32, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$32,
			fragment: componentDefinition$32?.fragment,
			type: typeof componentDefinition$32?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$157();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$156();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$155 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$156 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ContourLines($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$33.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$33.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$33.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$33.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$33.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$33, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$33.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$33 && typeof componentDefinition$33.fragment === "function") {
			parentRegister(instanceId, componentDefinition$33.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$33, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$33,
			fragment: componentDefinition$33?.fragment,
			type: typeof componentDefinition$33?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$156();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$155();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$154 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$155 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function CornerPin($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$34.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$34.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$34.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$34.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$34.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$34, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$34.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$34 && typeof componentDefinition$34.fragment === "function") {
			parentRegister(instanceId, componentDefinition$34.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$34, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$34,
			fragment: componentDefinition$34?.fragment,
			type: typeof componentDefinition$34?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$155();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$154();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$153 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$154 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Crescent($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$35.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$35.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$35.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$35.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$35.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$35, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$35.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$35 && typeof componentDefinition$35.fragment === "function") {
			parentRegister(instanceId, componentDefinition$35.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$35, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$35,
			fragment: componentDefinition$35?.fragment,
			type: typeof componentDefinition$35?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$154();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$153();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$152 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$153 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Cross($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$36.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$36.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$36.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$36.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$36.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$36, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$36.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$36 && typeof componentDefinition$36.fragment === "function") {
			parentRegister(instanceId, componentDefinition$36.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$36, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$36,
			fragment: componentDefinition$36?.fragment,
			type: typeof componentDefinition$36?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$153();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$152();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$151 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$152 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Crystal($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$37.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$37.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$37.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$37.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$37.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$37, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$37.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$37 && typeof componentDefinition$37.fragment === "function") {
			parentRegister(instanceId, componentDefinition$37.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$37, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$37,
			fragment: componentDefinition$37?.fragment,
			type: typeof componentDefinition$37?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$152();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$151();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$150 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$151 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function CurlNoise($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$38.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$38.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$38.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$38.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$38.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$38, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$38.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$38 && typeof componentDefinition$38.fragment === "function") {
			parentRegister(instanceId, componentDefinition$38.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$38, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$38,
			fragment: componentDefinition$38?.fragment,
			type: typeof componentDefinition$38?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$151();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$150();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$149 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$150 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function CursorRipples($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$39.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$39.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$39.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$39.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$39.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$39, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$39.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$39 && typeof componentDefinition$39.fragment === "function") {
			parentRegister(instanceId, componentDefinition$39.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$39, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$39,
			fragment: componentDefinition$39?.fragment,
			type: typeof componentDefinition$39?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$150();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$149();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$148 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$149 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function CursorTrail($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$40.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$40.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$40.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$40.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$40.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$40, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$40.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$40 && typeof componentDefinition$40.fragment === "function") {
			parentRegister(instanceId, componentDefinition$40.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$40, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$40,
			fragment: componentDefinition$40?.fragment,
			type: typeof componentDefinition$40?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$149();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$148();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$147 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$148 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function DataMosh($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$41.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$41.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$41.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$41.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$41.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$41, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$41.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$41 && typeof componentDefinition$41.fragment === "function") {
			parentRegister(instanceId, componentDefinition$41.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$41, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$41,
			fragment: componentDefinition$41?.fragment,
			type: typeof componentDefinition$41?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$148();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$147();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$146 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$147 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function DiamondGradient($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$42.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$42.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$42.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$42.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$42.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$42, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$42.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$42 && typeof componentDefinition$42.fragment === "function") {
			parentRegister(instanceId, componentDefinition$42.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$42, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$42,
			fragment: componentDefinition$42?.fragment,
			type: typeof componentDefinition$42?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$147();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$146();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$145 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$146 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function DiamondWipe($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$43.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$43.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$43.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$43.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$43.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$43, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$43.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$43 && typeof componentDefinition$43.fragment === "function") {
			parentRegister(instanceId, componentDefinition$43.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$43, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$43,
			fragment: componentDefinition$43?.fragment,
			type: typeof componentDefinition$43?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$146();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$145();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$144 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$145 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function DiffuseBlur($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$44.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$44.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$44.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$44.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$44.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$44, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$44.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$44 && typeof componentDefinition$44.fragment === "function") {
			parentRegister(instanceId, componentDefinition$44.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$44, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$44,
			fragment: componentDefinition$44?.fragment,
			type: typeof componentDefinition$44?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$145();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$144();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$143 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$144 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function DisplacementMap($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$45.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$45.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$45.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$45.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$45.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$45, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$45.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$45 && typeof componentDefinition$45.fragment === "function") {
			parentRegister(instanceId, componentDefinition$45.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$45, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$45,
			fragment: componentDefinition$45?.fragment,
			type: typeof componentDefinition$45?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$144();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$143();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$142 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$143 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Dither($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$46.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$46.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$46.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$46.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$46.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$46, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$46.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$46 && typeof componentDefinition$46.fragment === "function") {
			parentRegister(instanceId, componentDefinition$46.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$46, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$46,
			fragment: componentDefinition$46?.fragment,
			type: typeof componentDefinition$46?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$143();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$142();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$141 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$142 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function DotGrid($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$47.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$47.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$47.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$47.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$47.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$47, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$47.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$47 && typeof componentDefinition$47.fragment === "function") {
			parentRegister(instanceId, componentDefinition$47.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$47, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$47,
			fragment: componentDefinition$47?.fragment,
			type: typeof componentDefinition$47?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$142();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$141();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$140 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$141 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function DropShadow($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$48.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$48.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$48.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$48.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$48.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$48, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$48.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$48 && typeof componentDefinition$48.fragment === "function") {
			parentRegister(instanceId, componentDefinition$48.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$48, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$48,
			fragment: componentDefinition$48?.fragment,
			type: typeof componentDefinition$48?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$141();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$140();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$139 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$140 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Duotone($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$49.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$49.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$49.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$49.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$49.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$49, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$49.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$49 && typeof componentDefinition$49.fragment === "function") {
			parentRegister(instanceId, componentDefinition$49.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$49, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$49,
			fragment: componentDefinition$49?.fragment,
			type: typeof componentDefinition$49?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$140();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$139();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$138 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$139 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Ellipse($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$50.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$50.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$50.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$50.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$50.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$50, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$50.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$50 && typeof componentDefinition$50.fragment === "function") {
			parentRegister(instanceId, componentDefinition$50.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$50, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$50,
			fragment: componentDefinition$50?.fragment,
			type: typeof componentDefinition$50?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$139();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$138();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$137 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$138 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Emboss($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$51.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$51.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$51.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$51.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$51.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$51, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$51.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$51 && typeof componentDefinition$51.fragment === "function") {
			parentRegister(instanceId, componentDefinition$51.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$51, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$51,
			fragment: componentDefinition$51?.fragment,
			type: typeof componentDefinition$51?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$138();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$137();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$136 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$137 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Engraving($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$52.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$52.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$52.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$52.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$52.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$52, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$52.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$52 && typeof componentDefinition$52.fragment === "function") {
			parentRegister(instanceId, componentDefinition$52.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$52, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$52,
			fragment: componentDefinition$52?.fragment,
			type: typeof componentDefinition$52?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$137();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$136();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$135 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$136 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ErosionNoise($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$53.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$53.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$53.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$53.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$53.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$53, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$53.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$53 && typeof componentDefinition$53.fragment === "function") {
			parentRegister(instanceId, componentDefinition$53.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$53, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$53,
			fragment: componentDefinition$53?.fragment,
			type: typeof componentDefinition$53?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$136();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$135();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$134 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$135 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Exposure($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$54.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$54.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$54.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$54.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$54.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$54, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$54.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$54 && typeof componentDefinition$54.fragment === "function") {
			parentRegister(instanceId, componentDefinition$54.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$54, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$54,
			fragment: componentDefinition$54?.fragment,
			type: typeof componentDefinition$54?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$135();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$134();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$133 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$134 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function FallingLines($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$55.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$55.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$55.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$55.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$55.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$55, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$55.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$55 && typeof componentDefinition$55.fragment === "function") {
			parentRegister(instanceId, componentDefinition$55.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$55, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$55,
			fragment: componentDefinition$55?.fragment,
			type: typeof componentDefinition$55?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$134();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$133();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$132 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$133 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function FilmGrain($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$56.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$56.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$56.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$56.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$56.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$56, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$56.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$56 && typeof componentDefinition$56.fragment === "function") {
			parentRegister(instanceId, componentDefinition$56.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$56, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$56,
			fragment: componentDefinition$56?.fragment,
			type: typeof componentDefinition$56?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$133();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$132();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$131 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$132 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function FilmStock($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$57.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$57.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$57.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$57.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$57.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$57, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$57.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$57 && typeof componentDefinition$57.fragment === "function") {
			parentRegister(instanceId, componentDefinition$57.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$57, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$57,
			fragment: componentDefinition$57?.fragment,
			type: typeof componentDefinition$57?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$132();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$131();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$130 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$131 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Flip($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$58.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$58.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$58.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$58.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$58.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$58, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$58.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$58 && typeof componentDefinition$58.fragment === "function") {
			parentRegister(instanceId, componentDefinition$58.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$58, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$58,
			fragment: componentDefinition$58?.fragment,
			type: typeof componentDefinition$58?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$131();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$130();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$129 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$130 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function FloatingParticles($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$59.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$59.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$59.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$59.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$59.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$59, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$59.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$59 && typeof componentDefinition$59.fragment === "function") {
			parentRegister(instanceId, componentDefinition$59.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$59, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$59,
			fragment: componentDefinition$59?.fragment,
			type: typeof componentDefinition$59?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$130();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$129();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$128 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$129 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function FlowField($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$60.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$60.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$60.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$60.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$60.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$60, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$60.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$60 && typeof componentDefinition$60.fragment === "function") {
			parentRegister(instanceId, componentDefinition$60.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$60, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$60,
			fragment: componentDefinition$60?.fragment,
			type: typeof componentDefinition$60?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$129();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$128();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$127 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$128 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Flower($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$61.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$61.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$61.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$61.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$61.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$61, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$61.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$61 && typeof componentDefinition$61.fragment === "function") {
			parentRegister(instanceId, componentDefinition$61.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$61, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$61,
			fragment: componentDefinition$61?.fragment,
			type: typeof componentDefinition$61?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$128();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$127();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$126 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$127 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function FlowingGradient($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$62.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$62.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$62.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$62.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$62.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$62, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$62.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$62 && typeof componentDefinition$62.fragment === "function") {
			parentRegister(instanceId, componentDefinition$62.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$62, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$62,
			fragment: componentDefinition$62?.fragment,
			type: typeof componentDefinition$62?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$127();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$126();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$125 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$126 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function FlutedGlass($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$63.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$63.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$63.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$63.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$63.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$63, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$63.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$63 && typeof componentDefinition$63.fragment === "function") {
			parentRegister(instanceId, componentDefinition$63.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$63, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$63,
			fragment: componentDefinition$63?.fragment,
			type: typeof componentDefinition$63?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$126();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$125();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$124 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$125 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Fog($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$64.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$64.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$64.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$64.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$64.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$64, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$64.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$64 && typeof componentDefinition$64.fragment === "function") {
			parentRegister(instanceId, componentDefinition$64.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$64, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$64,
			fragment: componentDefinition$64?.fragment,
			type: typeof componentDefinition$64?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$125();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$124();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$123 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$124 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Form3D($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$65.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$65.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$65.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$65.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$65.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$65, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$65.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$65 && typeof componentDefinition$65.fragment === "function") {
			parentRegister(instanceId, componentDefinition$65.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$65, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$65,
			fragment: componentDefinition$65?.fragment,
			type: typeof componentDefinition$65?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$124();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$123();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$122 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$123 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function FractalNoise($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$66.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$66.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$66.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$66.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$66.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$66, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$66.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$66 && typeof componentDefinition$66.fragment === "function") {
			parentRegister(instanceId, componentDefinition$66.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$66, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$66,
			fragment: componentDefinition$66?.fragment,
			type: typeof componentDefinition$66?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$123();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$122();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$121 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$122 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Frost($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$67.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$67.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$67.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$67.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$67.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$67, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$67.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$67 && typeof componentDefinition$67.fragment === "function") {
			parentRegister(instanceId, componentDefinition$67.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$67, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$67,
			fragment: componentDefinition$67?.fragment,
			type: typeof componentDefinition$67?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$122();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$121();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$120 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$121 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function GaborNoise($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$68.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$68.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$68.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$68.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$68.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$68, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$68.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$68 && typeof componentDefinition$68.fragment === "function") {
			parentRegister(instanceId, componentDefinition$68.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$68, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$68,
			fragment: componentDefinition$68?.fragment,
			type: typeof componentDefinition$68?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$121();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$120();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$119 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$120 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Glass($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$69.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$69.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$69.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$69.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$69.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$69, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$69.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$69 && typeof componentDefinition$69.fragment === "function") {
			parentRegister(instanceId, componentDefinition$69.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$69, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$69,
			fragment: componentDefinition$69?.fragment,
			type: typeof componentDefinition$69?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$120();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$119();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$118 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$119 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function GlassTiles($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$70.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$70.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$70.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$70.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$70.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$70, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$70.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$70 && typeof componentDefinition$70.fragment === "function") {
			parentRegister(instanceId, componentDefinition$70.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$70, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$70,
			fragment: componentDefinition$70?.fragment,
			type: typeof componentDefinition$70?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$119();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$118();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$117 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$118 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Glitch($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$71.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$71.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$71.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$71.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$71.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$71, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$71.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$71 && typeof componentDefinition$71.fragment === "function") {
			parentRegister(instanceId, componentDefinition$71.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$71, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$71,
			fragment: componentDefinition$71?.fragment,
			type: typeof componentDefinition$71?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$118();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$117();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$116 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$117 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Glow($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$72.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$72.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$72.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$72.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$72.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$72, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$72.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$72 && typeof componentDefinition$72.fragment === "function") {
			parentRegister(instanceId, componentDefinition$72.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$72, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$72,
			fragment: componentDefinition$72?.fragment,
			type: typeof componentDefinition$72?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$117();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$116();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$115 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$116 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Godrays($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$73.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$73.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$73.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$73.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$73.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$73, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$73.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$73 && typeof componentDefinition$73.fragment === "function") {
			parentRegister(instanceId, componentDefinition$73.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$73, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$73,
			fragment: componentDefinition$73?.fragment,
			type: typeof componentDefinition$73?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$116();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$115();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$114 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$115 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Goo($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$74.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$74.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$74.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$74.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$74.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$74, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$74.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$74 && typeof componentDefinition$74.fragment === "function") {
			parentRegister(instanceId, componentDefinition$74.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$74, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$74,
			fragment: componentDefinition$74?.fragment,
			type: typeof componentDefinition$74?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$115();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$114();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$113 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$114 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function GradientMap($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$75.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$75.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$75.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$75.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$75.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$75, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$75.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$75 && typeof componentDefinition$75.fragment === "function") {
			parentRegister(instanceId, componentDefinition$75.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$75, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$75,
			fragment: componentDefinition$75?.fragment,
			type: typeof componentDefinition$75?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$114();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$113();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$112 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$113 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Grayscale($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$76.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$76.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$76.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$76.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$76.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$76, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$76.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$76 && typeof componentDefinition$76.fragment === "function") {
			parentRegister(instanceId, componentDefinition$76.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$76, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$76,
			fragment: componentDefinition$76?.fragment,
			type: typeof componentDefinition$76?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$113();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$112();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$111 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$112 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Grid($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$77.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$77.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$77.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$77.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$77.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$77, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$77.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$77 && typeof componentDefinition$77.fragment === "function") {
			parentRegister(instanceId, componentDefinition$77.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$77, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$77,
			fragment: componentDefinition$77?.fragment,
			type: typeof componentDefinition$77?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$112();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$111();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$110 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$111 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function GridDistortion($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$78.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$78.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$78.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$78.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$78.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$78, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$78.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$78 && typeof componentDefinition$78.fragment === "function") {
			parentRegister(instanceId, componentDefinition$78.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$78, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$78,
			fragment: componentDefinition$78?.fragment,
			type: typeof componentDefinition$78?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$111();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$110();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$109 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$110 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Group($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$79.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$79.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$79.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$79.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$79.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$79, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$79.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$79 && typeof componentDefinition$79.fragment === "function") {
			parentRegister(instanceId, componentDefinition$79.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$79, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$79,
			fragment: componentDefinition$79?.fragment,
			type: typeof componentDefinition$79?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$110();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$109();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$108 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$109 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function HTMLInCanvas($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$80.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$80.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$80.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$80.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$80.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$80, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$80.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$80 && typeof componentDefinition$80.fragment === "function") {
			parentRegister(instanceId, componentDefinition$80.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$80, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$80,
			fragment: componentDefinition$80?.fragment,
			type: typeof componentDefinition$80?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$109();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$108();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$107 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$108 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Halftone($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$81.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$81.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$81.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$81.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$81.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$81, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$81.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$81 && typeof componentDefinition$81.fragment === "function") {
			parentRegister(instanceId, componentDefinition$81.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$81, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$81,
			fragment: componentDefinition$81?.fragment,
			type: typeof componentDefinition$81?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$108();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$107();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$106 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$107 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Heart($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$82.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$82.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$82.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$82.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$82.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$82, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$82.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$82 && typeof componentDefinition$82.fragment === "function") {
			parentRegister(instanceId, componentDefinition$82.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$82, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$82,
			fragment: componentDefinition$82?.fragment,
			type: typeof componentDefinition$82?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$107();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$106();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$105 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$106 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Heatmap($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$83.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$83.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$83.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$83.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$83.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$83, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$83.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$83 && typeof componentDefinition$83.fragment === "function") {
			parentRegister(instanceId, componentDefinition$83.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$83, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$83,
			fragment: componentDefinition$83?.fragment,
			type: typeof componentDefinition$83?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$106();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$105();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$104 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$105 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function HexGrid($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$84.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$84.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$84.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$84.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$84.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$84, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$84.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$84 && typeof componentDefinition$84.fragment === "function") {
			parentRegister(instanceId, componentDefinition$84.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$84, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$84,
			fragment: componentDefinition$84?.fragment,
			type: typeof componentDefinition$84?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$105();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$104();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$103 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$104 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Hologram($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$85.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$85.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$85.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$85.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$85.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$85, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$85.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$85 && typeof componentDefinition$85.fragment === "function") {
			parentRegister(instanceId, componentDefinition$85.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$85, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$85,
			fragment: componentDefinition$85?.fragment,
			type: typeof componentDefinition$85?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$104();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$103();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$102 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$103 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Holographic($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$86.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$86.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$86.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$86.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$86.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$86, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$86.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$86 && typeof componentDefinition$86.fragment === "function") {
			parentRegister(instanceId, componentDefinition$86.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$86, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$86,
			fragment: componentDefinition$86?.fragment,
			type: typeof componentDefinition$86?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$103();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$102();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$101 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$102 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function HueShift($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$87.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$87.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$87.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$87.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$87.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$87, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$87.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$87 && typeof componentDefinition$87.fragment === "function") {
			parentRegister(instanceId, componentDefinition$87.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$87, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$87,
			fragment: componentDefinition$87?.fragment,
			type: typeof componentDefinition$87?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$102();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$101();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$100 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$101 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ImageTexture($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$88.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$88.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$88.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$88.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$88.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$88, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$88.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$88 && typeof componentDefinition$88.fragment === "function") {
			parentRegister(instanceId, componentDefinition$88.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$88, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$88,
			fragment: componentDefinition$88?.fragment,
			type: typeof componentDefinition$88?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$101();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$100();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$99 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$100 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function InkFlow($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$89.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$89.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$89.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$89.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$89.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$89, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$89.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$89 && typeof componentDefinition$89.fragment === "function") {
			parentRegister(instanceId, componentDefinition$89.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$89, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$89,
			fragment: componentDefinition$89?.fragment,
			type: typeof componentDefinition$89?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$100();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$99();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$98 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$99 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Invert($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$90.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$90.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$90.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$90.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$90.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$90, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$90.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$90 && typeof componentDefinition$90.fragment === "function") {
			parentRegister(instanceId, componentDefinition$90.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$90, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$90,
			fragment: componentDefinition$90?.fragment,
			type: typeof componentDefinition$90?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$99();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$98();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$97 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$98 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function IrisWipe($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$91.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$91.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$91.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$91.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$91.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$91, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$91.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$91 && typeof componentDefinition$91.fragment === "function") {
			parentRegister(instanceId, componentDefinition$91.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$91, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$91,
			fragment: componentDefinition$91?.fragment,
			type: typeof componentDefinition$91?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$98();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$97();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$96 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$97 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function IsometricCubes($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$92.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$92.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$92.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$92.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$92.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$92, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$92.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$92 && typeof componentDefinition$92.fragment === "function") {
			parentRegister(instanceId, componentDefinition$92.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$92, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$92,
			fragment: componentDefinition$92?.fragment,
			type: typeof componentDefinition$92?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$97();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$96();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$95 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$96 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Kaleidoscope($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$93.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$93.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$93.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$93.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$93.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$93, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$93.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$93 && typeof componentDefinition$93.fragment === "function") {
			parentRegister(instanceId, componentDefinition$93.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$93, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$93,
			fragment: componentDefinition$93?.fragment,
			type: typeof componentDefinition$93?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$96();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$95();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$94 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$95 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function KeyFrames($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$94.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$94.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$94.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$94.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$94.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$94, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$94.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$94 && typeof componentDefinition$94.fragment === "function") {
			parentRegister(instanceId, componentDefinition$94.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$94, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$94,
			fragment: componentDefinition$94?.fragment,
			type: typeof componentDefinition$94?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$95();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$94();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$93 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$94 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function LensFlare($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$95.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$95.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$95.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$95.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$95.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$95, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$95.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$95 && typeof componentDefinition$95.fragment === "function") {
			parentRegister(instanceId, componentDefinition$95.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$95, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$95,
			fragment: componentDefinition$95?.fragment,
			type: typeof componentDefinition$95?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$94();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$93();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$92 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$93 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function LightEdge($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$96.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$96.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$96.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$96.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$96.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$96, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$96.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$96 && typeof componentDefinition$96.fragment === "function") {
			parentRegister(instanceId, componentDefinition$96.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$96, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$96,
			fragment: componentDefinition$96?.fragment,
			type: typeof componentDefinition$96?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$93();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$92();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$91 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$92 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function LightLeak($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$97.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$97.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$97.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$97.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$97.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$97, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$97.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$97 && typeof componentDefinition$97.fragment === "function") {
			parentRegister(instanceId, componentDefinition$97.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$97, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$97,
			fragment: componentDefinition$97?.fragment,
			type: typeof componentDefinition$97?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$92();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$91();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$90 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$91 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function LinearBlur($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$98.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$98.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$98.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$98.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$98.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$98, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$98.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$98 && typeof componentDefinition$98.fragment === "function") {
			parentRegister(instanceId, componentDefinition$98.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$98, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$98,
			fragment: componentDefinition$98?.fragment,
			type: typeof componentDefinition$98?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$91();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$90();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$89 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$90 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function LinearGradient($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$99.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$99.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$99.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$99.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$99.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$99, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$99.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$99 && typeof componentDefinition$99.fragment === "function") {
			parentRegister(instanceId, componentDefinition$99.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$99, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$99,
			fragment: componentDefinition$99?.fragment,
			type: typeof componentDefinition$99?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$90();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$89();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$88 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$89 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function LinearWipe($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$100.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$100.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$100.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$100.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$100.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$100, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$100.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$100 && typeof componentDefinition$100.fragment === "function") {
			parentRegister(instanceId, componentDefinition$100.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$100, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$100,
			fragment: componentDefinition$100?.fragment,
			type: typeof componentDefinition$100?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$89();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$88();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$87 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$88 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function LiquidMetal($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$101.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$101.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$101.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$101.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$101.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$101, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$101.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$101 && typeof componentDefinition$101.fragment === "function") {
			parentRegister(instanceId, componentDefinition$101.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$101, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$101,
			fragment: componentDefinition$101?.fragment,
			type: typeof componentDefinition$101?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$88();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$87();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$86 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$87 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Liquify($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$102.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$102.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$102.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$102.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$102.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$102, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$102.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$102 && typeof componentDefinition$102.fragment === "function") {
			parentRegister(instanceId, componentDefinition$102.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$102, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$102,
			fragment: componentDefinition$102?.fragment,
			type: typeof componentDefinition$102?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$87();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$86();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$85 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$86 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function MagneticFilings($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$103.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$103.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$103.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$103.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$103.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$103, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$103.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$103 && typeof componentDefinition$103.fragment === "function") {
			parentRegister(instanceId, componentDefinition$103.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$103, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$103,
			fragment: componentDefinition$103?.fragment,
			type: typeof componentDefinition$103?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$86();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$85();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$84 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$85 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Marble($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$104.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$104.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$104.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$104.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$104.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$104, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$104.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$104 && typeof componentDefinition$104.fragment === "function") {
			parentRegister(instanceId, componentDefinition$104.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$104, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$104,
			fragment: componentDefinition$104?.fragment,
			type: typeof componentDefinition$104?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$85();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$84();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$83 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$84 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Mirror($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$105.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$105.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$105.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$105.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$105.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$105, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$105.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$105 && typeof componentDefinition$105.fragment === "function") {
			parentRegister(instanceId, componentDefinition$105.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$105, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$105,
			fragment: componentDefinition$105?.fragment,
			type: typeof componentDefinition$105?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$84();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$83();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$82 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$83 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function MultiPointGradient($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$106.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$106.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$106.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$106.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$106.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$106, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$106.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$106 && typeof componentDefinition$106.fragment === "function") {
			parentRegister(instanceId, componentDefinition$106.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$106, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$106,
			fragment: componentDefinition$106?.fragment,
			type: typeof componentDefinition$106?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$83();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$82();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$81 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$82 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Neon($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$107.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$107.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$107.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$107.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$107.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$107, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$107.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$107 && typeof componentDefinition$107.fragment === "function") {
			parentRegister(instanceId, componentDefinition$107.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$107, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$107,
			fragment: componentDefinition$107?.fragment,
			type: typeof componentDefinition$107?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$82();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$81();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$80 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$81 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function NoiseDissolve($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$108.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$108.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$108.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$108.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$108.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$108, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$108.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$108 && typeof componentDefinition$108.fragment === "function") {
			parentRegister(instanceId, componentDefinition$108.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$108, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$108,
			fragment: componentDefinition$108?.fragment,
			type: typeof componentDefinition$108?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$81();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$80();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$79 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$80 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ObjectTracker($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$109.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$109.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$109.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$109.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$109.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$109, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$109.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$109 && typeof componentDefinition$109.fragment === "function") {
			parentRegister(instanceId, componentDefinition$109.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$109, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$109,
			fragment: componentDefinition$109?.fragment,
			type: typeof componentDefinition$109?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$80();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$79();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$78 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$79 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function PagePeel($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$110.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$110.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$110.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$110.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$110.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$110, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$110.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$110 && typeof componentDefinition$110.fragment === "function") {
			parentRegister(instanceId, componentDefinition$110.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$110, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$110,
			fragment: componentDefinition$110?.fragment,
			type: typeof componentDefinition$110?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$79();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$78();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$77 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$78 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Paper($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$111.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$111.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$111.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$111.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$111.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$111, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$111.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$111 && typeof componentDefinition$111.fragment === "function") {
			parentRegister(instanceId, componentDefinition$111.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$111, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$111,
			fragment: componentDefinition$111?.fragment,
			type: typeof componentDefinition$111?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$78();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$77();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$76 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$77 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Parallelogram($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$112.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$112.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$112.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$112.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$112.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$112, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$112.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$112 && typeof componentDefinition$112.fragment === "function") {
			parentRegister(instanceId, componentDefinition$112.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$112, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$112,
			fragment: componentDefinition$112?.fragment,
			type: typeof componentDefinition$112?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$77();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$76();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$75 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$76 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ParticleField($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$113.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$113.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$113.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$113.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$113.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$113, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$113.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$113 && typeof componentDefinition$113.fragment === "function") {
			parentRegister(instanceId, componentDefinition$113.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$113, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$113,
			fragment: componentDefinition$113?.fragment,
			type: typeof componentDefinition$113?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$76();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$75();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$74 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$75 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ParticleFlow($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$114.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$114.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$114.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$114.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$114.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$114, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$114.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$114 && typeof componentDefinition$114.fragment === "function") {
			parentRegister(instanceId, componentDefinition$114.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$114, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$114,
			fragment: componentDefinition$114?.fragment,
			type: typeof componentDefinition$114?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$75();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$74();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$73 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$74 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Particles($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$115.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$115.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$115.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$115.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$115.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$115, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$115.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$115 && typeof componentDefinition$115.fragment === "function") {
			parentRegister(instanceId, componentDefinition$115.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$115, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$115,
			fragment: componentDefinition$115?.fragment,
			type: typeof componentDefinition$115?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$74();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$73();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$72 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$73 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function PerlinNoise($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$116.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$116.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$116.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$116.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$116.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$116, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$116.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$116 && typeof componentDefinition$116.fragment === "function") {
			parentRegister(instanceId, componentDefinition$116.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$116, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$116,
			fragment: componentDefinition$116?.fragment,
			type: typeof componentDefinition$116?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$73();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$72();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$71 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$72 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Perspective($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$117.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$117.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$117.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$117.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$117.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$117, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$117.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$117 && typeof componentDefinition$117.fragment === "function") {
			parentRegister(instanceId, componentDefinition$117.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$117, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$117,
			fragment: componentDefinition$117?.fragment,
			type: typeof componentDefinition$117?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$72();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$71();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$70 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$71 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function PixelSort($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$118.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$118.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$118.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$118.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$118.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$118, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$118.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$118 && typeof componentDefinition$118.fragment === "function") {
			parentRegister(instanceId, componentDefinition$118.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$118, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$118,
			fragment: componentDefinition$118?.fragment,
			type: typeof componentDefinition$118?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$71();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$70();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$69 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$70 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function PixelThrow($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$119.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$119.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$119.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$119.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$119.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$119, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$119.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$119 && typeof componentDefinition$119.fragment === "function") {
			parentRegister(instanceId, componentDefinition$119.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$119, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$119,
			fragment: componentDefinition$119?.fragment,
			type: typeof componentDefinition$119?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$70();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$69();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$68 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$69 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Pixelate($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$120.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$120.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$120.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$120.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$120.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$120, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$120.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$120 && typeof componentDefinition$120.fragment === "function") {
			parentRegister(instanceId, componentDefinition$120.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$120, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$120,
			fragment: componentDefinition$120?.fragment,
			type: typeof componentDefinition$120?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$69();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$68();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$67 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$68 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Plasma($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$121.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$121.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$121.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$121.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$121.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$121, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$121.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$121 && typeof componentDefinition$121.fragment === "function") {
			parentRegister(instanceId, componentDefinition$121.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$121, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$121,
			fragment: componentDefinition$121?.fragment,
			type: typeof componentDefinition$121?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$68();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$67();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$66 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$67 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Plastic($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$122.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$122.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$122.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$122.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$122.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$122, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$122.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$122 && typeof componentDefinition$122.fragment === "function") {
			parentRegister(instanceId, componentDefinition$122.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$122, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$122,
			fragment: componentDefinition$122?.fragment,
			type: typeof componentDefinition$122?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$67();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$66();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$65 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$66 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function PolarCoordinates($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$123.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$123.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$123.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$123.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$123.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$123, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$123.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$123 && typeof componentDefinition$123.fragment === "function") {
			parentRegister(instanceId, componentDefinition$123.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$123, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$123,
			fragment: componentDefinition$123?.fragment,
			type: typeof componentDefinition$123?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$66();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$65();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$64 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$65 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Polygon($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$124.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$124.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$124.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$124.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$124.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$124, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$124.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$124 && typeof componentDefinition$124.fragment === "function") {
			parentRegister(instanceId, componentDefinition$124.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$124, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$124,
			fragment: componentDefinition$124?.fragment,
			type: typeof componentDefinition$124?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$65();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$64();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$63 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$64 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Posterize($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$125.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$125.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$125.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$125.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$125.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$125, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$125.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$125 && typeof componentDefinition$125.fragment === "function") {
			parentRegister(instanceId, componentDefinition$125.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$125, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$125,
			fragment: componentDefinition$125?.fragment,
			type: typeof componentDefinition$125?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$64();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$63();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$62 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$63 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Prism($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$126.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$126.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$126.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$126.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$126.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$126, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$126.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$126 && typeof componentDefinition$126.fragment === "function") {
			parentRegister(instanceId, componentDefinition$126.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$126, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$126,
			fragment: componentDefinition$126?.fragment,
			type: typeof componentDefinition$126?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$63();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$62();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$61 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$62 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ProgressiveBlur($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$127.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$127.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$127.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$127.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$127.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$127, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$127.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$127 && typeof componentDefinition$127.fragment === "function") {
			parentRegister(instanceId, componentDefinition$127.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$127, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$127,
			fragment: componentDefinition$127?.fragment,
			type: typeof componentDefinition$127?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$62();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$61();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$60 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$61 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function RadialGradient($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$128.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$128.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$128.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$128.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$128.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$128, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$128.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$128 && typeof componentDefinition$128.fragment === "function") {
			parentRegister(instanceId, componentDefinition$128.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$128, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$128,
			fragment: componentDefinition$128?.fragment,
			type: typeof componentDefinition$128?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$61();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$60();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$59 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$60 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function RadialWipe($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$129.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$129.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$129.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$129.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$129.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$129, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$129.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$129 && typeof componentDefinition$129.fragment === "function") {
			parentRegister(instanceId, componentDefinition$129.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$129, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$129,
			fragment: componentDefinition$129?.fragment,
			type: typeof componentDefinition$129?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$60();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$59();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$58 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$59 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function RandomBars($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$130.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$130.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$130.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$130.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$130.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$130, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$130.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$130 && typeof componentDefinition$130.fragment === "function") {
			parentRegister(instanceId, componentDefinition$130.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$130, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$130,
			fragment: componentDefinition$130?.fragment,
			type: typeof componentDefinition$130?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$59();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$58();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$57 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$58 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ReactionDiffusion($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$131.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$131.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$131.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$131.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$131.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$131, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$131.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$131 && typeof componentDefinition$131.fragment === "function") {
			parentRegister(instanceId, componentDefinition$131.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$131, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$131,
			fragment: componentDefinition$131?.fragment,
			type: typeof componentDefinition$131?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$58();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$57();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$56 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$57 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function RectangularCoordinates($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$132.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$132.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$132.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$132.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$132.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$132, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$132.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$132 && typeof componentDefinition$132.fragment === "function") {
			parentRegister(instanceId, componentDefinition$132.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$132, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$132,
			fragment: componentDefinition$132?.fragment,
			type: typeof componentDefinition$132?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$57();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$56();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$55 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$56 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ReflectivePlane($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$133.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$133.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$133.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$133.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$133.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$133, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$133.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$133 && typeof componentDefinition$133.fragment === "function") {
			parentRegister(instanceId, componentDefinition$133.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$133, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$133,
			fragment: componentDefinition$133?.fragment,
			type: typeof componentDefinition$133?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$56();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$55();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$54 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$55 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Repeater($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$134.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$134.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$134.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$134.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$134.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$134, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$134.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$134 && typeof componentDefinition$134.fragment === "function") {
			parentRegister(instanceId, componentDefinition$134.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$134, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$134,
			fragment: componentDefinition$134?.fragment,
			type: typeof componentDefinition$134?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$55();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$54();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$53 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$54 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Ring($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$135.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$135.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$135.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$135.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$135.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$135, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$135.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$135 && typeof componentDefinition$135.fragment === "function") {
			parentRegister(instanceId, componentDefinition$135.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$135, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$135,
			fragment: componentDefinition$135?.fragment,
			type: typeof componentDefinition$135?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$54();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$53();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$52 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$53 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function RippleWipe($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$136.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$136.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$136.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$136.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$136.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$136, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$136.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$136 && typeof componentDefinition$136.fragment === "function") {
			parentRegister(instanceId, componentDefinition$136.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$136, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$136,
			fragment: componentDefinition$136?.fragment,
			type: typeof componentDefinition$136?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$53();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$52();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$51 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$52 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Ripples($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$137.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$137.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$137.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$137.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$137.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$137, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$137.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$137 && typeof componentDefinition$137.fragment === "function") {
			parentRegister(instanceId, componentDefinition$137.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$137, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$137,
			fragment: componentDefinition$137?.fragment,
			type: typeof componentDefinition$137?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$52();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$51();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$50 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$51 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function RoundedRect($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$138.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$138.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$138.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$138.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$138.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$138, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$138.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$138 && typeof componentDefinition$138.fragment === "function") {
			parentRegister(instanceId, componentDefinition$138.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$138, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$138,
			fragment: componentDefinition$138?.fragment,
			type: typeof componentDefinition$138?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$51();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$50();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$49 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$50 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Saturation($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$139.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$139.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$139.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$139.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$139.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$139, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$139.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$139 && typeof componentDefinition$139.fragment === "function") {
			parentRegister(instanceId, componentDefinition$139.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$139, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$139,
			fragment: componentDefinition$139?.fragment,
			type: typeof componentDefinition$139?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$50();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$49();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$48 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$49 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Scratches($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$140.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$140.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$140.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$140.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$140.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$140, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$140.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$140 && typeof componentDefinition$140.fragment === "function") {
			parentRegister(instanceId, componentDefinition$140.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$140, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$140,
			fragment: componentDefinition$140?.fragment,
			type: typeof componentDefinition$140?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$49();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$48();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$47 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$48 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Sharpness($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$141.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$141.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$141.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$141.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$141.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$141, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$141.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$141 && typeof componentDefinition$141.fragment === "function") {
			parentRegister(instanceId, componentDefinition$141.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$141, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$141,
			fragment: componentDefinition$141?.fragment,
			type: typeof componentDefinition$141?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$48();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$47();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$46 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$47 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Shatter($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$142.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$142.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$142.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$142.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$142.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$142, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$142.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$142 && typeof componentDefinition$142.fragment === "function") {
			parentRegister(instanceId, componentDefinition$142.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$142, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$142,
			fragment: componentDefinition$142?.fragment,
			type: typeof componentDefinition$142?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$47();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$46();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$45 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$46 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function SimplexNoise($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$143.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$143.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$143.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$143.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$143.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$143, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$143.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$143 && typeof componentDefinition$143.fragment === "function") {
			parentRegister(instanceId, componentDefinition$143.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$143, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$143,
			fragment: componentDefinition$143?.fragment,
			type: typeof componentDefinition$143?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$46();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$45();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$44 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$45 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function SineWave($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$144.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$144.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$144.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$144.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$144.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$144, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$144.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$144 && typeof componentDefinition$144.fragment === "function") {
			parentRegister(instanceId, componentDefinition$144.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$144, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$144,
			fragment: componentDefinition$144?.fragment,
			type: typeof componentDefinition$144?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$45();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$44();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$43 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$44 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function SliceWipe($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$145.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$145.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$145.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$145.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$145.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$145, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$145.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$145 && typeof componentDefinition$145.fragment === "function") {
			parentRegister(instanceId, componentDefinition$145.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$145, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$145,
			fragment: componentDefinition$145?.fragment,
			type: typeof componentDefinition$145?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$44();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$43();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$42 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$43 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Smoke($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$146.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$146.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$146.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$146.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$146.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$146, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$146.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$146 && typeof componentDefinition$146.fragment === "function") {
			parentRegister(instanceId, componentDefinition$146.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$146, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$146,
			fragment: componentDefinition$146?.fragment,
			type: typeof componentDefinition$146?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$43();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$42();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$41 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$42 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function SmokeFill($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$147.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$147.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$147.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$147.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$147.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$147, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$147.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$147 && typeof componentDefinition$147.fragment === "function") {
			parentRegister(instanceId, componentDefinition$147.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$147, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$147,
			fragment: componentDefinition$147?.fragment,
			type: typeof componentDefinition$147?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$42();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$41();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$40 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$41 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function SmokeFlow($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$148.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$148.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$148.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$148.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$148.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$148, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$148.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$148 && typeof componentDefinition$148.fragment === "function") {
			parentRegister(instanceId, componentDefinition$148.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$148, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$148,
			fragment: componentDefinition$148?.fragment,
			type: typeof componentDefinition$148?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$41();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$40();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$39 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$40 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Solarize($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$149.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$149.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$149.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$149.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$149.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$149, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$149.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$149 && typeof componentDefinition$149.fragment === "function") {
			parentRegister(instanceId, componentDefinition$149.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$149, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$149,
			fragment: componentDefinition$149?.fragment,
			type: typeof componentDefinition$149?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$40();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$39();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$38 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$39 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function SolidColor($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$150.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$150.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$150.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$150.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$150.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$150, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$150.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$150 && typeof componentDefinition$150.fragment === "function") {
			parentRegister(instanceId, componentDefinition$150.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$150, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$150,
			fragment: componentDefinition$150?.fragment,
			type: typeof componentDefinition$150?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$39();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$38();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$37 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$38 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Spherize($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$151.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$151.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$151.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$151.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$151.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$151, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$151.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$151 && typeof componentDefinition$151.fragment === "function") {
			parentRegister(instanceId, componentDefinition$151.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$151, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$151,
			fragment: componentDefinition$151?.fragment,
			type: typeof componentDefinition$151?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$38();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$37();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$36 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$37 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Spiral($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$152.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$152.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$152.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$152.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$152.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$152, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$152.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$152 && typeof componentDefinition$152.fragment === "function") {
			parentRegister(instanceId, componentDefinition$152.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$152, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$152,
			fragment: componentDefinition$152?.fragment,
			type: typeof componentDefinition$152?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$37();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$36();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$35 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$36 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Star($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$153.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$153.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$153.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$153.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$153.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$153, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$153.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$153 && typeof componentDefinition$153.fragment === "function") {
			parentRegister(instanceId, componentDefinition$153.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$153, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$153,
			fragment: componentDefinition$153?.fragment,
			type: typeof componentDefinition$153?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$36();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$35();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$34 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$35 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Stone($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$154.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$154.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$154.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$154.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$154.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$154, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$154.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$154 && typeof componentDefinition$154.fragment === "function") {
			parentRegister(instanceId, componentDefinition$154.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$154, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$154,
			fragment: componentDefinition$154?.fragment,
			type: typeof componentDefinition$154?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$35();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$34();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$33 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$34 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Strands($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$155.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$155.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$155.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$155.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$155.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$155, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$155.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$155 && typeof componentDefinition$155.fragment === "function") {
			parentRegister(instanceId, componentDefinition$155.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$155, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$155,
			fragment: componentDefinition$155?.fragment,
			type: typeof componentDefinition$155?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$34();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$33();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$32 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$33 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Stretch($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$156.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$156.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$156.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$156.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$156.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$156, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$156.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$156 && typeof componentDefinition$156.fragment === "function") {
			parentRegister(instanceId, componentDefinition$156.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$156, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$156,
			fragment: componentDefinition$156?.fragment,
			type: typeof componentDefinition$156?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$33();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$32();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$31 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$32 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Stripes($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$157.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$157.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$157.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$157.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$157.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$157, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$157.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$157 && typeof componentDefinition$157.fragment === "function") {
			parentRegister(instanceId, componentDefinition$157.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$157, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$157,
			fragment: componentDefinition$157?.fragment,
			type: typeof componentDefinition$157?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$32();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$31();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$30 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$31 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function StudioBackground($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$158.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$158.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$158.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$158.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$158.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$158, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$158.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$158 && typeof componentDefinition$158.fragment === "function") {
			parentRegister(instanceId, componentDefinition$158.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$158, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$158,
			fragment: componentDefinition$158?.fragment,
			type: typeof componentDefinition$158?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$31();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$30();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$29 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$30 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function SunBurst($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$159.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$159.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$159.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$159.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$159.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$159, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$159.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$159 && typeof componentDefinition$159.fragment === "function") {
			parentRegister(instanceId, componentDefinition$159.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$159, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$159,
			fragment: componentDefinition$159?.fragment,
			type: typeof componentDefinition$159?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$30();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$29();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$28 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$29 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Surface3D($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$160.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$160.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$160.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$160.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$160.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$160, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$160.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$160 && typeof componentDefinition$160.fragment === "function") {
			parentRegister(instanceId, componentDefinition$160.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$160, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$160,
			fragment: componentDefinition$160?.fragment,
			type: typeof componentDefinition$160?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$29();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$28();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$27 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$28 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Swirl($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$161.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$161.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$161.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$161.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$161.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$161, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$161.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$161 && typeof componentDefinition$161.fragment === "function") {
			parentRegister(instanceId, componentDefinition$161.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$161, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$161,
			fragment: componentDefinition$161?.fragment,
			type: typeof componentDefinition$161?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$28();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$27();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$26 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$27 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Teardrop($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$162.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$162.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$162.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$162.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$162.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$162, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$162.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$162 && typeof componentDefinition$162.fragment === "function") {
			parentRegister(instanceId, componentDefinition$162.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$162, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$162,
			fragment: componentDefinition$162?.fragment,
			type: typeof componentDefinition$162?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$27();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$26();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$25 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$26 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Text($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$163.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$163.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$163.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$163.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$163.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$163, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$163.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$163 && typeof componentDefinition$163.fragment === "function") {
			parentRegister(instanceId, componentDefinition$163.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$163, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$163,
			fragment: componentDefinition$163?.fragment,
			type: typeof componentDefinition$163?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$26();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$25();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$24 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$25 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ThinFilm($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$164.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$164.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$164.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$164.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$164.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$164, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$164.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$164 && typeof componentDefinition$164.fragment === "function") {
			parentRegister(instanceId, componentDefinition$164.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$164, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$164,
			fragment: componentDefinition$164?.fragment,
			type: typeof componentDefinition$164?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$25();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$24();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$23 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$24 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function TiltShift($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$165.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$165.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$165.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$165.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$165.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$165, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$165.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$165 && typeof componentDefinition$165.fragment === "function") {
			parentRegister(instanceId, componentDefinition$165.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$165, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$165,
			fragment: componentDefinition$165?.fragment,
			type: typeof componentDefinition$165?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$24();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$23();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$22 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$23 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function TimeTrail($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$166.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$166.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$166.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$166.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$166.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$166, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$166.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$166 && typeof componentDefinition$166.fragment === "function") {
			parentRegister(instanceId, componentDefinition$166.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$166, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$166,
			fragment: componentDefinition$166?.fragment,
			type: typeof componentDefinition$166?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$23();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$22();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$21 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$22 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Tint($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$167.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$167.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$167.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$167.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$167.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$167, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$167.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$167 && typeof componentDefinition$167.fragment === "function") {
			parentRegister(instanceId, componentDefinition$167.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$167, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$167,
			fragment: componentDefinition$167?.fragment,
			type: typeof componentDefinition$167?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$22();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$21();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$20 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$21 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Trapezoid($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$168.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$168.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$168.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$168.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$168.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$168, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$168.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$168 && typeof componentDefinition$168.fragment === "function") {
			parentRegister(instanceId, componentDefinition$168.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$168, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$168,
			fragment: componentDefinition$168?.fragment,
			type: typeof componentDefinition$168?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$21();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$20();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$19 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$20 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function TriangularGrid($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$169.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$169.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$169.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$169.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$169.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$169, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$169.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$169 && typeof componentDefinition$169.fragment === "function") {
			parentRegister(instanceId, componentDefinition$169.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$169, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$169,
			fragment: componentDefinition$169?.fragment,
			type: typeof componentDefinition$169?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$20();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$19();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$18 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$19 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Tritone($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$170.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$170.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$170.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$170.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$170.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$170, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$170.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$170 && typeof componentDefinition$170.fragment === "function") {
			parentRegister(instanceId, componentDefinition$170.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$170, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$170,
			fragment: componentDefinition$170?.fragment,
			type: typeof componentDefinition$170?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$19();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$18();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$17 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$18 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Truchet($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$171.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$171.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$171.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$171.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$171.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$171, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$171.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$171 && typeof componentDefinition$171.fragment === "function") {
			parentRegister(instanceId, componentDefinition$171.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$171, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$171,
			fragment: componentDefinition$171?.fragment,
			type: typeof componentDefinition$171?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$18();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$17();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$16 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$17 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Twirl($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$172.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$172.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$172.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$172.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$172.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$172, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$172.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$172 && typeof componentDefinition$172.fragment === "function") {
			parentRegister(instanceId, componentDefinition$172.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$172, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$172,
			fragment: componentDefinition$172?.fragment,
			type: typeof componentDefinition$172?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$17();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$16();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$15 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$16 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function VHS($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$173.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$173.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$173.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$173.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$173.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$173, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$173.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$173 && typeof componentDefinition$173.fragment === "function") {
			parentRegister(instanceId, componentDefinition$173.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$173, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$173,
			fragment: componentDefinition$173?.fragment,
			type: typeof componentDefinition$173?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$16();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$15();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$14 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$15 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function VenetianBlinds($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$174.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$174.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$174.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$174.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$174.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$174, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$174.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$174 && typeof componentDefinition$174.fragment === "function") {
			parentRegister(instanceId, componentDefinition$174.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$174, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$174,
			fragment: componentDefinition$174?.fragment,
			type: typeof componentDefinition$174?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$15();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$14();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$13 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$14 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Vesica($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$175.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$175.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$175.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$175.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$175.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$175, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$175.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$175 && typeof componentDefinition$175.fragment === "function") {
			parentRegister(instanceId, componentDefinition$175.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$175, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$175,
			fragment: componentDefinition$175?.fragment,
			type: typeof componentDefinition$175?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$14();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$13();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$12 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$13 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Vibrance($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$176.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$176.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$176.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$176.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$176.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$176, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$176.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$176 && typeof componentDefinition$176.fragment === "function") {
			parentRegister(instanceId, componentDefinition$176.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$176, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$176,
			fragment: componentDefinition$176?.fragment,
			type: typeof componentDefinition$176?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$13();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$12();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$11 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$12 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function VideoTexture($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$177.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$177.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$177.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$177.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$177.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$177, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$177.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$177 && typeof componentDefinition$177.fragment === "function") {
			parentRegister(instanceId, componentDefinition$177.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$177, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$177,
			fragment: componentDefinition$177?.fragment,
			type: typeof componentDefinition$177?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$12();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$11();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$10 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$11 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Vignette($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$178.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$178.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$178.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$178.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$178.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$178, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$178.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$178 && typeof componentDefinition$178.fragment === "function") {
			parentRegister(instanceId, componentDefinition$178.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$178, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$178,
			fragment: componentDefinition$178?.fragment,
			type: typeof componentDefinition$178?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$11();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$10();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$9 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$10 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Voronoi($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$179.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$179.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$179.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$179.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$179.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$179, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$179.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$179 && typeof componentDefinition$179.fragment === "function") {
			parentRegister(instanceId, componentDefinition$179.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$179, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$179,
			fragment: componentDefinition$179?.fragment,
			type: typeof componentDefinition$179?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$10();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$9();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$8 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$9 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Water($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$180.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$180.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$180.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$180.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$180.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$180, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$180.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$180 && typeof componentDefinition$180.fragment === "function") {
			parentRegister(instanceId, componentDefinition$180.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$180, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$180,
			fragment: componentDefinition$180?.fragment,
			type: typeof componentDefinition$180?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$9();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$8();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$7 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$8 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Watercolor($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$181.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$181.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$181.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$181.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$181.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$181, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$181.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$181 && typeof componentDefinition$181.fragment === "function") {
			parentRegister(instanceId, componentDefinition$181.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$181, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$181,
			fragment: componentDefinition$181?.fragment,
			type: typeof componentDefinition$181?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$8();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$7();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$6 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$7 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function WaveDistortion($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$182.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$182.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$182.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$182.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$182.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$182, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$182.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$182 && typeof componentDefinition$182.fragment === "function") {
			parentRegister(instanceId, componentDefinition$182.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$182, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$182,
			fragment: componentDefinition$182?.fragment,
			type: typeof componentDefinition$182?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$7();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$6();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$5 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$6 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function WaveletNoise($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$183.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$183.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$183.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$183.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$183.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$183, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$183.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$183 && typeof componentDefinition$183.fragment === "function") {
			parentRegister(instanceId, componentDefinition$183.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$183, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$183,
			fragment: componentDefinition$183?.fragment,
			type: typeof componentDefinition$183?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$6();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$5();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$4 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$5 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Weave($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$184.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$184.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$184.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$184.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$184.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$184, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$184.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$184 && typeof componentDefinition$184.fragment === "function") {
			parentRegister(instanceId, componentDefinition$184.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$184, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$184,
			fragment: componentDefinition$184?.fragment,
			type: typeof componentDefinition$184?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$5();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$4();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$3 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$4 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function WebcamTexture($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$185.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$185.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$185.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$185.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$185.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$185, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$185.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$185 && typeof componentDefinition$185.fragment === "function") {
			parentRegister(instanceId, componentDefinition$185.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$185, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$185,
			fragment: componentDefinition$185?.fragment,
			type: typeof componentDefinition$185?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$4();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$3();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$2 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$3 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function Wool($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$186.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$186.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$186.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$186.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$186.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$186, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$186.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$186 && typeof componentDefinition$186.fragment === "function") {
			parentRegister(instanceId, componentDefinition$186.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$186, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$186,
			fragment: componentDefinition$186?.fragment,
			type: typeof componentDefinition$186?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$3();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$2();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2$1 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$2 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function WorleyNoise($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$187.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$187.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$187.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$187.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$187.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$187, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$187.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$187 && typeof componentDefinition$187.fragment === "function") {
			parentRegister(instanceId, componentDefinition$187.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$187, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$187,
			fragment: componentDefinition$187?.fragment,
			type: typeof componentDefinition$187?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$2();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2$1();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_2 = $.from_html(`<canvas layoutsubtree="" style="position:fixed;inset:0;width:100vw;height:100vh;z-index:-9999"><!></canvas>`);
var root$1 = $.from_html(`<span style="display:contents"><!></span> <!>`, 1);
function ZoomBlur($$anchor, $$props) {
	$.push($$props, true);
	let _experimentalWarnedOnce = false;
	if (componentDefinition$188.experimental && !_experimentalWarnedOnce) {
		_experimentalWarnedOnce = true;
		const _e = componentDefinition$188.experimental;
		console.info(`%c⚠ [Shaders] ${componentDefinition$188.name} is experimental: ${_e.message}`, "color: #f59e0b; font-weight: bold");
	}
	function isPropDriver(value) {
		return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
	}
	const DEFAULT_TRANSFORM = {
		offsetX: 0,
		offsetY: 0,
		rotation: 0,
		scale: 1,
		anchorX: .5,
		anchorY: .5,
		edges: "transparent"
	};
	const componentDefaults = {
		blendMode: "normal",
		visible: true,
		...Object.entries(componentDefinition$188.props).reduce((acc, [key, config]) => {
			acc[key] = config.default;
			return acc;
		}, {})
	};
	const props = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy"
	]);
	const blendMode = $.derived(() => $$props.blendMode ?? componentDefaults.blendMode);
	const opacity = $.derived(() => $$props.opacity);
	const visible = $.derived(() => $$props.visible ?? componentDefaults.visible);
	const id = $.derived(() => $$props.id);
	const maskSource = $.derived(() => $$props.maskSource);
	const maskType = $.derived(() => $$props.maskType);
	const renderOrder = $.derived(() => $$props.renderOrder);
	const { children } = props;
	const mapsFromProps = $.derived(() => {
		const maps = {};
		for (const key of Object.keys(componentDefinition$188.props)) {
			const val = props[key];
			if (isPropDriver(val)) maps[key] = val;
		}
		return Object.keys(maps).length > 0 ? maps : void 0;
	});
	const effectiveTransform = $.derived(() => ({
		...DEFAULT_TRANSFORM,
		...$$props.transform
	}));
	const effectiveBoundingBox = $.derived(() => resolveBoundingBox($$props.boundingBox));
	const parentId = getContext("shaderParentId");
	if (parentId === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const instanceId = ($.get(id) ? $.get(id).replace(/[^a-zA-Z0-9_]/g, "_") : null) || Math.random().toString(36).substring(2, 10);
	setContext("shaderParentId", instanceId);
	const shaderReadyProps = $.derived(() => {
		let baseProps = { ...componentDefaults };
		for (const key in props) if (key !== "blendMode" && key !== "opacity" && key !== "visible" && key !== "id" && key !== "maskSource" && key !== "maskType" && key !== "renderOrder" && key !== "transform" && key !== "boundingBox" && key !== "flow" && key !== "absolute" && key !== "children") {
			const propValue = props[key];
			if (isPropDriver(propValue)) continue;
			const defaultValue = componentDefaults[key];
			if (propValue !== void 0 && propValue !== defaultValue) baseProps[key] = propValue;
		}
		return baseProps;
	});
	const shaderColorSpace = getContext("shaderColorSpace");
	if (shaderColorSpace) setColorSpaceMode(shaderColorSpace());
	const uniforms = createGpuUniformsMap(componentDefinition$188, $.get(shaderReadyProps), instanceId);
	const parentRegister = getContext("shaderNodeRegister");
	if (parentRegister === void 0) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const parentUniformUpdate = getContext("shaderUniformUpdate");
	if (parentUniformUpdate === void 0) throw new Error("Shader components require shaderUniformUpdate from parent");
	const parentMetadataUpdate = getContext("shaderMetadataUpdate");
	if (parentMetadataUpdate === void 0) throw new Error("Shader components require shaderMetadataUpdate from parent");
	const isCapturesDOM = !!componentDefinition$188.capturesDOM;
	let captureCanvas;
	let captureW = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0));
	let captureH = $.state($.proxy(typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0));
	function teleportToBody(node) {
		document.body.appendChild(node);
		return { destroy() {
			node.remove();
		} };
	}
	if (isCapturesDOM) {
		const onWinResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			$.set(captureW, Math.round(window.innerWidth * d), true);
			$.set(captureH, Math.round(window.innerHeight * d), true);
		};
		onMount(() => {
			window.addEventListener("resize", onWinResize);
		});
		onDestroy(() => {
			window.removeEventListener("resize", onWinResize);
		});
	}
	let orderMarker;
	let detectedRenderOrder = void 0;
	let isRegistered = $.state(false);
	Object.entries(uniforms).forEach(([propName, entry]) => {
		$.user_effect(() => {
			if (!$.get(isRegistered)) return;
			if (entry && entry.value !== void 0) {
				const newValue = props[propName];
				if (newValue !== void 0 && !isPropDriver(newValue)) parentUniformUpdate(instanceId, propName, newValue);
			}
		});
	});
	$.user_effect(() => {
		if (!$.get(isRegistered)) return;
		parentMetadataUpdate(instanceId, {
			blendMode: $.get(blendMode),
			opacity: $.get(opacity),
			visible: $.get(visible) === false ? false : true,
			id: $.get(id),
			mask: $.get(maskSource) ? {
				source: $.get(maskSource),
				type: $.get(maskType) || "alpha"
			} : void 0,
			maps: $.get(mapsFromProps),
			renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
			transform: $.get(effectiveTransform),
			boundingBox: $.get(effectiveBoundingBox),
			flow: $$props.flow,
			absolute: $$props.absolute
		});
	});
	onMount(() => {
		if (componentDefinition$188 && typeof componentDefinition$188.fragment === "function") {
			parentRegister(instanceId, componentDefinition$188.fragment, parentId, {
				blendMode: $.get(blendMode),
				opacity: $.get(opacity),
				visible: $.get(visible) !== false ? true : false,
				id: $.get(id),
				mask: $.get(maskSource) ? {
					source: $.get(maskSource),
					type: $.get(maskType) || "alpha"
				} : void 0,
				maps: $.get(mapsFromProps),
				renderOrder: $.get(renderOrder) ?? detectedRenderOrder,
				transform: $.get(effectiveTransform),
				boundingBox: $.get(effectiveBoundingBox),
				flow: $$props.flow,
				absolute: $$props.absolute
			}, uniforms, componentDefinition$188, isCapturesDOM ? captureCanvas : void 0);
			$.set(isRegistered, true);
			if ($.get(renderOrder) === void 0 && orderMarker) {
				const parent = orderMarker.parentElement;
				if (parent) {
					const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
					const position = Array.from(siblings).indexOf(orderMarker);
					if (position >= 0) {
						detectedRenderOrder = position;
						parentMetadataUpdate(instanceId, { renderOrder: position });
					}
				}
			}
		} else console.error("componentDefinition.fragment is not a function:", {
			componentDefinition: componentDefinition$188,
			fragment: componentDefinition$188?.fragment,
			type: typeof componentDefinition$188?.fragment
		});
	});
	onDestroy(() => {
		$.set(isRegistered, false);
		parentRegister(instanceId, null, null, null, null);
	});
	var fragment = root$1();
	var span = $.first_child(fragment);
	var node_1 = $.child(span);
	var consequent = ($$anchor$1) => {
		children?.($$anchor$1);
	};
	$.if(node_1, ($$render) => {
		if (!isCapturesDOM) $$render(consequent);
	});
	$.reset(span);
	$.bind_this(span, ($$value) => orderMarker = $$value, () => orderMarker);
	var node_2 = $.sibling(span, 2);
	var consequent_1 = ($$anchor$1) => {
		var canvas = root_2();
		var node_3 = $.child(canvas);
		children?.(node_3);
		$.reset(canvas);
		$.action(canvas, ($$node) => teleportToBody?.($$node));
		$.bind_this(canvas, ($$value) => captureCanvas = $$value, () => captureCanvas);
		$.template_effect(() => {
			$.set_attribute(canvas, "width", $.get(captureW));
			$.set_attribute(canvas, "height", $.get(captureH));
		});
		$.append($$anchor$1, canvas);
	};
	$.if(node_2, ($$render) => {
		if (isCapturesDOM) $$render(consequent_1);
	});
	$.template_effect(() => $.set_attribute(span, "data-shader-id", instanceId));
	$.append($$anchor, fragment);
	$.pop();
}
var root_1$1 = $.from_html(`<canvas data-renderer="shaders" style="width: 100%; height: 100%; display: block;"></canvas> <!>`, 1);
var root = $.from_html(`<div><!></div>`);
function Shader($$anchor, $$props) {
	$.push($$props, true);
	const disableTelemetry = $.prop($$props, "disableTelemetry", 3, false), colorSpace = $.prop($$props, "colorSpace", 3, "p3-linear"), toneMapping = $.prop($$props, "toneMapping", 3, "linear"), isPreview = $.prop($$props, "isPreview", 3, false), rest = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy",
		"disableTelemetry",
		"colorSpace",
		"toneMapping",
		"isPreview",
		"onready",
		"onunavailable",
		"children"
	]);
	let containerRef;
	let canvasRef;
	let mounted = $.state(false);
	const rootId = "shader-root-" + Math.random().toString(36).substring(7);
	let rendererInstance = null;
	let gpuUnavailable = null;
	let telemetryCollector = null;
	let telemetryStartTimeout = null;
	let shouldSendTelemetry = null;
	setContext("shaderParentId", rootId);
	setContext("shaderColorSpace", () => colorSpace());
	setContext("shaderNodeRegister", (id, fragmentNodeFunc, parentId, metadata, uniforms = null, componentDef = null, domCanvas) => {
		if (!rendererInstance) return;
		if (fragmentNodeFunc === null) rendererInstance.removeNode(id);
		else rendererInstance.registerNode(id, fragmentNodeFunc, parentId, metadata, uniforms, componentDef, domCanvas);
	});
	setContext("shaderUniformUpdate", (nodeId, uniformName, value) => {
		if (!rendererInstance) return;
		rendererInstance.updateUniformValue(nodeId, uniformName, value);
	});
	setContext("shaderMetadataUpdate", (nodeId, metadata) => {
		if (!rendererInstance) return;
		rendererInstance.updateNodeMetadata(nodeId, metadata);
	});
	let wasVisible = false;
	let visibilityObserver = null;
	const MAX_TELEMETRY_POLLS = 40;
	const startTelemetryWhenReady = () => {
		let polls = 0;
		const checkRendering = () => {
			if (!rendererInstance) return;
			if (gpuUnavailable || ++polls > MAX_TELEMETRY_POLLS) {
				telemetryStartTimeout = null;
				return;
			}
			if (rendererInstance.getPerformanceStats().fps > 0) {
				telemetryCollector = startTelemetry(rendererInstance, "3.0.446", disableTelemetry(), isPreview());
				if (telemetryCollector) telemetryCollector.start();
				telemetryStartTimeout = null;
			} else telemetryStartTimeout = window.setTimeout(checkRendering, 500);
		};
		telemetryStartTimeout = window.setTimeout(checkRendering, 500);
	};
	const initializeRenderer = async () => {
		if (!canvasRef || !rendererInstance || gpuUnavailable) return;
		try {
			if (!rendererInstance.isInitialized()) await rendererInstance.initialize({
				canvas: canvasRef,
				colorSpace: colorSpace(),
				toneMapping: toneMapping()
			});
			if (rendererInstance.getFailureReason()) return;
			rendererInstance.registerNode(rootId, rootPassthrough.fragment, null, null, {}, rootPassthrough);
			if (shouldSendTelemetry === null) shouldSendTelemetry = isExternalUser();
			if (shouldSendTelemetry && !telemetryCollector) startTelemetryWhenReady();
		} catch (err) {
			debugWarn("[Shaders] renderer initialization failed:", err);
		}
	};
	const setupVisibilityObserver = () => {
		if (!containerRef || visibilityObserver) return;
		visibilityObserver = new IntersectionObserver((entries) => {
			const entry = entries[0];
			if (!entry) return;
			const rect = containerRef?.getBoundingClientRect();
			const isCurrentlyVisible = entry.isIntersecting && rect && rect.width > 0 && rect.height > 0;
			if (isCurrentlyVisible && !wasVisible) {
				if (rendererInstance?.isInitialized()) {
					rendererInstance.startAnimation();
					if (shouldSendTelemetry && !telemetryCollector && !telemetryStartTimeout) startTelemetryWhenReady();
				} else initializeRenderer();
				wasVisible = true;
			} else if (!isCurrentlyVisible && wasVisible) {
				rendererInstance?.stopAnimation();
				wasVisible = false;
			}
		}, { threshold: 0 });
		visibilityObserver.observe(containerRef);
	};
	onMount(async () => {
		rendererInstance = shaderRendererGPU();
		rendererInstance.setOnReady(() => $$props.onready?.());
		rendererInstance.setOnUnavailable((reason) => {
			gpuUnavailable = reason;
			try {
				$$props.onunavailable?.(reason);
			} catch (err) {
				debugWarn("[Shaders] onunavailable callback threw:", err);
			}
		});
		$.set(mounted, true);
		await new Promise((resolve) => requestAnimationFrame(resolve));
		if (!canvasRef || !containerRef) {
			debugWarn("[Shaders] canvas or container ref is null in Shader onMount");
			return;
		}
		const rect = containerRef.getBoundingClientRect();
		if (rect.width > 0 && rect.height > 0) {
			await initializeRenderer();
			wasVisible = true;
		} else wasVisible = false;
		setupVisibilityObserver();
	});
	onDestroy(() => {
		if (telemetryCollector) {
			telemetryCollector.stop();
			telemetryCollector = null;
		}
		if (telemetryStartTimeout !== null) {
			clearTimeout(telemetryStartTimeout);
			telemetryStartTimeout = null;
		}
		if (visibilityObserver) {
			visibilityObserver.disconnect();
			visibilityObserver = null;
		}
		if (rendererInstance?.isInitialized()) try {
			rendererInstance.cleanup();
		} catch (err) {
			debugWarn("[Shaders] error during cleanup:", err);
		}
	});
	let isFirstEffect = true;
	$.user_effect(() => {
		const currentColorSpace = colorSpace();
		if (isFirstEffect) {
			isFirstEffect = false;
			return;
		}
		if (rendererInstance?.isInitialized()) {
			setColorSpaceMode(currentColorSpace);
			rendererInstance.cleanup();
			initializeRenderer();
		}
	});
	let isFirstToneMappingEffect = true;
	$.user_effect(() => {
		toneMapping();
		if (isFirstToneMappingEffect) {
			isFirstToneMappingEffect = false;
			return;
		}
		if (rendererInstance?.isInitialized()) {
			rendererInstance.cleanup();
			initializeRenderer();
		}
	});
	var div = root();
	$.attribute_effect(div, () => ({
		class: "shader",
		...rest
	}));
	var node = $.child(div);
	var consequent = ($$anchor$1) => {
		var fragment = root_1$1();
		var canvas = $.first_child(fragment);
		$.bind_this(canvas, ($$value) => canvasRef = $$value, () => canvasRef);
		var node_1 = $.sibling(canvas, 2);
		$.snippet(node_1, () => $$props.children ?? $.noop);
		$.append($$anchor$1, fragment);
	};
	$.if(node, ($$render) => {
		if ($.get(mounted)) $$render(consequent);
	});
	$.reset(div);
	$.bind_this(div, ($$value) => containerRef = $$value, () => containerRef);
	$.append($$anchor, div);
	$.pop();
}
function PreviewRenderComponent($$anchor, $$props) {
	$.push($$props, true);
	const structureVersion = $.prop($$props, "structureVersion", 3, void 0);
	const Component = $.derived(() => $$props.componentMap[$$props.config.type]);
	const componentProps = $.derived(() => $$props.config.props ? { ...$$props.config.props } : {});
	var fragment = $.comment();
	var node = $.first_child(fragment);
	var consequent_1 = ($$anchor$1) => {
		var fragment_1 = $.comment();
		var node_1 = $.first_child(fragment_1);
		$.component(node_1, () => $.get(Component), ($$anchor$2, $$component) => {
			$$component($$anchor$2, $.spread_props({
				get id() {
					return $$props.config.id;
				},
				get renderOrder() {
					return $$props.config.renderOrder;
				}
			}, () => $.get(componentProps), {
				children: ($$anchor$3, $$slotProps) => {
					var fragment_2 = $.comment();
					var node_2 = $.first_child(fragment_2);
					var consequent = ($$anchor$4) => {
						var fragment_3 = $.comment();
						var node_3 = $.first_child(fragment_3);
						$.each(node_3, 19, () => $$props.config.children, (child, index) => child.id || `child_${index}_v${structureVersion() || 0}`, ($$anchor$5, child, index) => {
							var fragment_4 = $.comment();
							var node_4 = $.first_child(fragment_4);
							{
								let $0 = $.derived(() => ({
									...$.get(child),
									renderOrder: $.get(child).renderOrder ?? $.get(index)
								}));
								PreviewRenderComponent(node_4, {
									get config() {
										return $.get($0);
									},
									get componentMap() {
										return $$props.componentMap;
									},
									get structureVersion() {
										return structureVersion();
									}
								});
							}
							$.append($$anchor$5, fragment_4);
						});
						$.append($$anchor$4, fragment_3);
					};
					$.if(node_2, ($$render) => {
						if ($$props.config.children) $$render(consequent);
					});
					$.append($$anchor$3, fragment_2);
				},
				$$slots: { default: true }
			}));
		});
		$.append($$anchor$1, fragment_1);
	};
	$.if(node, ($$render) => {
		if ($.get(Component)) $$render(consequent_1);
	});
	$.append($$anchor, fragment);
	$.pop();
}
var root_4 = $.from_html(`<a target="_blank" rel="noopener noreferrer"> </a>`);
var root_1 = $.from_html(`<div><!> <!></div>`);
function Preview($$anchor, $$props) {
	$.push($$props, true);
	const componentMap = {
		AngularBlur,
		Arc,
		Ascii,
		Aurora,
		BarShift,
		BarnDoors,
		Beam,
		Bend,
		Blob,
		BlockDissolve,
		BlockNoise,
		BlueNoise,
		Blur,
		Boids,
		BokehBlur,
		BrickPattern,
		BrightnessContrast,
		BrushedMetal,
		Bulge,
		CRTScreen,
		CarbonFiber,
		Chalkboard,
		ChannelBlur,
		CheckerWipe,
		Checkerboard,
		Chevron,
		ChromaFlow,
		ChromaticAberration,
		Circle,
		ColorWheel,
		CompressionArtifacts,
		ConcentricSpin,
		ConicGradient,
		ContourLines,
		CornerPin,
		Crescent,
		Cross,
		Crystal,
		CurlNoise,
		CursorRipples,
		CursorTrail,
		DataMosh,
		DiamondGradient,
		DiamondWipe,
		DiffuseBlur,
		DisplacementMap,
		Dither,
		DotGrid,
		DropShadow,
		Duotone,
		Ellipse,
		Emboss,
		Engraving,
		ErosionNoise,
		Exposure,
		FallingLines,
		FilmGrain,
		FilmStock,
		Flip,
		FloatingParticles,
		FlowField,
		Flower,
		FlowingGradient,
		FlutedGlass,
		Fog,
		Form3D,
		FractalNoise,
		Frost,
		GaborNoise,
		Glass,
		GlassTiles,
		Glitch,
		Glow,
		Godrays,
		Goo,
		GradientMap,
		Grayscale,
		Grid,
		GridDistortion,
		Group,
		HTMLInCanvas,
		DOMTexture: HTMLInCanvas,
		Halftone,
		Heart,
		Heatmap,
		HexGrid,
		Hologram,
		Holographic,
		HueShift,
		ImageTexture,
		InkFlow,
		Invert,
		IrisWipe,
		IsometricCubes,
		Kaleidoscope,
		KeyFrames,
		LensFlare,
		LightEdge,
		LightLeak,
		LinearBlur,
		LinearGradient,
		LinearWipe,
		LiquidMetal,
		Liquify,
		MagneticFilings,
		Marble,
		Mirror,
		MultiPointGradient,
		Neon,
		NoiseDissolve,
		ObjectTracker,
		PagePeel,
		Paper,
		Parallelogram,
		ParticleField,
		ParticleFlow,
		Particles,
		PerlinNoise,
		Perspective,
		PixelSort,
		PixelThrow,
		Pixelate,
		Plasma,
		Plastic,
		PolarCoordinates,
		Polygon,
		Posterize,
		Prism,
		ProgressiveBlur,
		RadialGradient,
		RadialWipe,
		RandomBars,
		ReactionDiffusion,
		RectangularCoordinates,
		ReflectivePlane,
		Repeater,
		Ring,
		RippleWipe,
		Ripples,
		RoundedRect,
		Saturation,
		Scratches,
		Sharpness,
		Shatter,
		SimplexNoise,
		SineWave,
		SliceWipe,
		Smoke,
		SmokeFill,
		SmokeFlow,
		Solarize,
		SolidColor,
		Spherize,
		Spiral,
		Star,
		Stone,
		Strands,
		Stretch,
		Stripes,
		StudioBackground,
		SunBurst,
		Surface3D,
		Swirl,
		Teardrop,
		Text,
		ThinFilm,
		TiltShift,
		TimeTrail,
		Tint,
		Trapezoid,
		TriangularGrid,
		Tritone,
		Truchet,
		Twirl,
		VHS,
		VenetianBlinds,
		Vesica,
		Vibrance,
		VideoTexture,
		Vignette,
		Voronoi,
		Water,
		Watercolor,
		WaveDistortion,
		WaveletNoise,
		Weave,
		WebcamTexture,
		Wool,
		WorleyNoise,
		ZoomBlur
	};
	function xorCrypt(data, key) {
		const keyBytes = new TextEncoder().encode(key);
		const result = new Uint8Array(data.length);
		for (let i = 0; i < data.length; i++) result[i] = data[i] ^ keyBytes[i % keyBytes.length];
		return result;
	}
	function decodePreviewDefinition(encoded, key) {
		const binaryString = atob(encoded);
		const encrypted = new Uint8Array(binaryString.length);
		for (let i = 0; i < binaryString.length; i++) encrypted[i] = binaryString.charCodeAt(i);
		const decrypted = xorCrypt(encrypted, key);
		const json = new TextDecoder().decode(decrypted);
		try {
			return JSON.parse(json);
		} catch {
			throw new Error("Failed to decode shader definition: invalid data or incorrect key");
		}
	}
	function fetchAndDecode(url, key) {
		return fetch(url).then((res) => {
			if (!res.ok) throw new Error(`Failed to fetch: ${res.status}`);
			return res.json();
		}).then((data) => {
			const item = data.preset || data.shader;
			if (item?.definition && typeof item.definition === "string") return {
				definition: decodePreviewDefinition(item.definition, key),
				keyProps: Array.isArray(item.key_props) ? item.key_props : null
			};
			return {
				definition: null,
				keyProps: null
			};
		}).catch((err) => {
			console.error("[Shaders Preview] Failed to fetch preview:", err);
			return {
				definition: null,
				keyProps: null
			};
		});
	}
	const RESERVED_IDENTIFIERS = new Set([
		"break",
		"case",
		"catch",
		"class",
		"const",
		"continue",
		"default",
		"delete",
		"do",
		"else",
		"export",
		"extends",
		"finally",
		"for",
		"function",
		"if",
		"import",
		"in",
		"instanceof",
		"new",
		"return",
		"super",
		"switch",
		"this",
		"throw",
		"try",
		"typeof",
		"var",
		"void",
		"while",
		"with",
		"yield"
	]);
	function slugifyIdentifier(label, fallback) {
		const cleaned = label.replace(/[^A-Za-z0-9]+/g, " ").trim();
		if (!cleaned) return fallback;
		const camel = cleaned.split(/\s+/).map((part, i) => i === 0 ? part.charAt(0).toLowerCase() + part.slice(1) : part.charAt(0).toUpperCase() + part.slice(1)).join("");
		const safe = /^[A-Za-z_$]/.test(camel) ? camel : `_${camel}`;
		return RESERVED_IDENTIFIERS.has(safe) ? `_${safe}` : safe;
	}
	function buildKeyPropIdentifierMap(keyProps) {
		const used = new Set(["style"]);
		const map = /* @__PURE__ */ new Map();
		if (!keyProps?.length) return map;
		keyProps.forEach((kp, i) => {
			if (!kp.targets?.length) return;
			const base = slugifyIdentifier(kp.label, `keyProp${i}`);
			let ident = base;
			let suffix = 1;
			while (used.has(ident)) {
				suffix++;
				ident = `${base}${suffix}`;
			}
			used.add(ident);
			map.set(ident, kp);
		});
		return map;
	}
	function findInTree(id, list) {
		for (const c of list) {
			if (c.id === id) return c;
			if (c.children?.length) {
				const hit = findInTree(id, c.children);
				if (hit) return hit;
			}
		}
		return null;
	}
	function applyConfiguration(definition, configuration$1, keyProps) {
		if (!configuration$1 || !keyProps?.length) return definition;
		const entries = Object.entries(configuration$1);
		if (entries.length === 0) return definition;
		const identMap = buildKeyPropIdentifierMap(keyProps);
		const cloned = JSON.parse(JSON.stringify(definition.components));
		for (const [ident, value] of entries) {
			const kp = identMap.get(ident);
			if (!kp) continue;
			for (const t of kp.targets ?? []) {
				const comp = findInTree(t.component_id, cloned);
				if (comp?.props && t.prop in comp.props) comp.props[t.prop] = value;
			}
		}
		return {
			...definition,
			components: cloned
		};
	}
	const shader = $.prop($$props, "shader", 3, void 0), presetId = $.prop($$props, "presetId", 3, void 0), apiBaseUrl = $.prop($$props, "apiBaseUrl", 3, "https://shaders.com"), obfuscationKey = $.prop($$props, "obfuscationKey", 3, "shaders-preview-key"), watermarkText = $.prop($$props, "watermarkText", 3, "Unlock your Shaders Pro license"), watermarkLink = $.prop($$props, "watermarkLink", 3, "https://shaders.com/dashboard?pricing=true"), style = $.prop($$props, "style", 3, void 0), configuration = $.prop($$props, "configuration", 3, void 0), version = $.prop($$props, "version", 3, void 0), restProps = $.rest_props($$props, [
		"$$slots",
		"$$events",
		"$$legacy",
		"shader",
		"presetId",
		"apiBaseUrl",
		"obfuscationKey",
		"watermarkText",
		"watermarkLink",
		"style",
		"configuration",
		"version"
	]);
	let fetchedDefinition = $.state(null);
	let fetchedKeyProps = $.state(null);
	let watermarkHovered = $.state(false);
	let resolvedDefinition = $.derived(() => $.get(fetchedDefinition) ? applyConfiguration($.get(fetchedDefinition), configuration(), $.get(fetchedKeyProps)) : null);
	$.user_effect(() => {
		if (shader()) {
			$.set(fetchedDefinition, null);
			$.set(fetchedKeyProps, null);
			fetchAndDecode(`${apiBaseUrl()}/api/preview/shader/${shader()}`, obfuscationKey()).then((result) => {
				$.set(fetchedDefinition, result.definition, true);
				$.set(fetchedKeyProps, result.keyProps, true);
			});
		} else if (!presetId()) {
			$.set(fetchedDefinition, null);
			$.set(fetchedKeyProps, null);
		}
	});
	$.user_effect(() => {
		if (presetId()) {
			$.set(fetchedDefinition, null);
			$.set(fetchedKeyProps, null);
			const versionParam = version() ? `?version=${encodeURIComponent(version())}` : "";
			fetchAndDecode(`${apiBaseUrl()}/api/preview/preset/${presetId()}${versionParam}`, obfuscationKey()).then((result) => {
				$.set(fetchedDefinition, result.definition, true);
				$.set(fetchedKeyProps, result.keyProps, true);
			});
		} else if (!shader()) {
			$.set(fetchedDefinition, null);
			$.set(fetchedKeyProps, null);
		}
	});
	onMount(() => {
		console.warn("[Shaders] Preview component is intended for use with a Shaders license. Visit https://shaders.com for more information.");
	});
	var fragment = $.comment();
	var node = $.first_child(fragment);
	var consequent_1 = ($$anchor$1) => {
		var div = root_1();
		$.attribute_effect(div, () => ({
			class: "shaders-preview",
			style: `position: relative; width: 100%; height: 100%;${style() ? " " + style() : ""}`,
			...restProps
		}));
		var node_1 = $.child(div);
		{
			let $0 = $.derived(() => $.get(resolvedDefinition).colorSpace || "p3-linear");
			Shader(node_1, {
				get colorSpace() {
					return $.get($0);
				},
				isPreview: true,
				enablePerformanceTracking: true,
				style: "width: 100%; height: 100%;",
				children: ($$anchor$2, $$slotProps) => {
					var fragment_1 = $.comment();
					var node_2 = $.first_child(fragment_1);
					$.each(node_2, 19, () => $.get(resolvedDefinition).components, (config, index) => config.id || `root_${index}_v${$.get(resolvedDefinition).structureVersion || 0}`, ($$anchor$3, config, index) => {
						{
							let $0$1 = $.derived(() => ({
								...$.get(config),
								renderOrder: $.get(config).renderOrder ?? $.get(index)
							}));
							PreviewRenderComponent($$anchor$3, {
								get config() {
									return $.get($0$1);
								},
								get componentMap() {
									return componentMap;
								},
								get structureVersion() {
									return $.get(resolvedDefinition).structureVersion;
								}
							});
						}
					});
					$.append($$anchor$2, fragment_1);
				},
				$$slots: { default: true }
			});
		}
		var node_3 = $.sibling(node_1, 2);
		var consequent = ($$anchor$2) => {
			var a = root_4();
			var text = $.child(a, true);
			$.reset(a);
			$.template_effect(() => {
				$.set_attribute(a, "href", watermarkLink());
				$.set_style(a, `
          position: absolute;
          bottom: 8px;
          right: 12px;
          font-size: 11px;
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
          color: ${$.get(watermarkHovered) ? "rgba(255, 255, 255, 0.8)" : "rgba(255, 255, 255, 0.5)"};
          text-decoration: none;
          pointer-events: auto;
          z-index: 10;
          text-shadow: 0 1px 3px rgba(0, 0, 0, 0.4);
          transition: color 0.2s ease;
        `);
				$.set_text(text, watermarkText());
			});
			$.event("mouseenter", a, () => $.set(watermarkHovered, true));
			$.event("mouseleave", a, () => $.set(watermarkHovered, false));
			$.append($$anchor$2, a);
		};
		$.if(node_3, ($$render) => {
			if (watermarkText()) $$render(consequent);
		});
		$.reset(div);
		$.append($$anchor$1, div);
	};
	$.if(node, ($$render) => {
		if ($.get(resolvedDefinition)) $$render(consequent_1);
	});
	$.append($$anchor, fragment);
	$.pop();
}
export { AngularBlur, Arc, Ascii, Aurora, BarShift, BarnDoors, Beam, Bend, Blob, BlockDissolve, BlockNoise, BlueNoise, Blur, Boids, BokehBlur, BrickPattern, BrightnessContrast, BrushedMetal, Bulge, CRTScreen, CarbonFiber, Chalkboard, ChannelBlur, CheckerWipe, Checkerboard, Chevron, ChromaFlow, ChromaticAberration, Circle, ColorWheel, CompressionArtifacts, ConcentricSpin, ConicGradient, ContourLines, CornerPin, Crescent, Cross, Crystal, CurlNoise, CursorRipples, CursorTrail, HTMLInCanvas as DOMTexture, HTMLInCanvas, DataMosh, DiamondGradient, DiamondWipe, DiffuseBlur, DisplacementMap, Dither, DotGrid, DropShadow, Duotone, Ellipse, Emboss, Engraving, ErosionNoise, Exposure, FallingLines, FilmGrain, FilmStock, Flip, FloatingParticles, FlowField, Flower, FlowingGradient, FlutedGlass, Fog, Form3D, FractalNoise, Frost, GaborNoise, Glass, GlassTiles, Glitch, Glow, Godrays, Goo, GradientMap, Grayscale, Grid, GridDistortion, Group, Halftone, Heart, Heatmap, HexGrid, Hologram, Holographic, HueShift, ImageTexture, InkFlow, Invert, IrisWipe, IsometricCubes, Kaleidoscope, KeyFrames, LensFlare, LightEdge, LightLeak, LinearBlur, LinearGradient, LinearWipe, LiquidMetal, Liquify, MagneticFilings, Marble, Mirror, MultiPointGradient, Neon, NoiseDissolve, ObjectTracker, PagePeel, Paper, Parallelogram, ParticleField, ParticleFlow, Particles, PerlinNoise, Perspective, PixelSort, PixelThrow, Pixelate, Plasma, Plastic, PolarCoordinates, Polygon, Posterize, Preview, Prism, ProgressiveBlur, RadialGradient, RadialWipe, RandomBars, ReactionDiffusion, RectangularCoordinates, ReflectivePlane, Repeater, Ring, RippleWipe, Ripples, RoundedRect, Saturation, Scratches, Shader, Sharpness, Shatter, SimplexNoise, SineWave, SliceWipe, Smoke, SmokeFill, SmokeFlow, Solarize, SolidColor, Spherize, Spiral, Star, Stone, Strands, Stretch, Stripes, StudioBackground, SunBurst, Surface3D, Swirl, Teardrop, Text, ThinFilm, TiltShift, TimeTrail, Tint, Trapezoid, TriangularGrid, Tritone, Truchet, Twirl, VHS, VenetianBlinds, Vesica, Vibrance, VideoTexture, Vignette, Voronoi, Water, Watercolor, WaveDistortion, WaveletNoise, Weave, WebcamTexture, Wool, WorleyNoise, ZoomBlur, getWebGPUSupport, isShadersDebug, isWebGPUSupported, setShadersDebug };
