import { n as __exportAll, r as __toESM } from "./rolldown-runtime-B-1-B7_t.js";
import { t as require_react } from "./react.js";
import { C as ZERO, Cn as std_exports, P as call, R as composeEdgeRemapExpr, Sn as src_default, Ut as resolveBoundingBox, Yt as sampleRemappedExpr, cr as data_exports, h as transformPosition, hn as unpremultiplyAlpha, hr as vec2f, l as setColorSpaceMode, m as transformEdges, n as ShaderContext, qn as max, rr as sin, s as createGpuUniformsMap, sr as tan, yr as f32, zn as cos } from "./Shader-BPX4e-H3.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { t as require_react_dom } from "./react-dom.js";
//#region node_modules/shaders/dist/core/Perspective-CJSrtEy5.js
var DEG_TO_RAD = Math.PI / 180;
var perspectiveUV = src_default.fn([
	vec2f,
	f32,
	f32,
	f32,
	f32,
	vec2f,
	vec2f
], vec2f)(/* @__PURE__ */ (($) => (globalThis.__TYPEGPU_META__ ??= /* @__PURE__ */ new WeakMap()).set($.f = ((center, pan, tilt, fov, zoom, offset, uv) => {
	"use gpu";
	const centerPos = vec2f(center.x, __tsover_sub(1, center.y));
	const x = __tsover_div(__tsover_sub(uv.x, centerPos.x), zoom);
	const y = __tsover_div(__tsover_sub(uv.y, centerPos.y), zoom);
	const panRad = __tsover_mul(pan, DEG_TO_RAD);
	const tiltRad = __tsover_mul(tilt, DEG_TO_RAD);
	const fovRad = __tsover_mul(fov, DEG_TO_RAD);
	const cosPan = cos(panRad);
	const sinPan = sin(panRad);
	const cosTilt = cos(tiltRad);
	const sinTilt = sin(tiltRad);
	const perspectiveFactor = __tsover_mul(tan(__tsover_mul(fovRad, .5)), 2);
	const safePanDivisor = max(__tsover_add(cosPan, __tsover_mul(__tsover_mul(sinPan, x), perspectiveFactor)), .001);
	const afterPanX = __tsover_div(x, safePanDivisor);
	const afterPanY = __tsover_div(__tsover_mul(y, cosPan), safePanDivisor);
	const safeTiltDivisor = max(__tsover_add(cosTilt, __tsover_mul(__tsover_mul(sinTilt, afterPanY), perspectiveFactor)), .001);
	const finalX = __tsover_div(__tsover_mul(afterPanX, cosTilt), safeTiltDivisor);
	const finalY = __tsover_div(afterPanY, safeTiltDivisor);
	const offsetPos = vec2f(offset.x, __tsover_sub(1, offset.y));
	return vec2f(finalX, finalY).add(centerPos).sub(offsetPos).add(vec2f(.5, .5));
}), {
	v: 1,
	name: void 0,
	ast: {
		"params": [
			{
				"type": "i",
				"name": "center"
			},
			{
				"type": "i",
				"name": "pan"
			},
			{
				"type": "i",
				"name": "tilt"
			},
			{
				"type": "i",
				"name": "fov"
			},
			{
				"type": "i",
				"name": "zoom"
			},
			{
				"type": "i",
				"name": "offset"
			},
			{
				"type": "i",
				"name": "uv"
			}
		],
		"body": [0, [
			[
				13,
				"centerPos",
				[
					6,
					[
						7,
						"d",
						"vec2f"
					],
					[[
						7,
						"center",
						"x"
					], [
						1,
						[5, "1"],
						"-",
						[
							7,
							"center",
							"y"
						]
					]]
				]
			],
			[
				13,
				"x",
				[
					1,
					[
						1,
						[
							7,
							"uv",
							"x"
						],
						"-",
						[
							7,
							"centerPos",
							"x"
						]
					],
					"/",
					"zoom"
				]
			],
			[
				13,
				"y",
				[
					1,
					[
						1,
						[
							7,
							"uv",
							"y"
						],
						"-",
						[
							7,
							"centerPos",
							"y"
						]
					],
					"/",
					"zoom"
				]
			],
			[
				13,
				"panRad",
				[
					1,
					"pan",
					"*",
					"DEG_TO_RAD"
				]
			],
			[
				13,
				"tiltRad",
				[
					1,
					"tilt",
					"*",
					"DEG_TO_RAD"
				]
			],
			[
				13,
				"fovRad",
				[
					1,
					"fov",
					"*",
					"DEG_TO_RAD"
				]
			],
			[
				13,
				"cosPan",
				[
					6,
					[
						7,
						"std",
						"cos"
					],
					["panRad"]
				]
			],
			[
				13,
				"sinPan",
				[
					6,
					[
						7,
						"std",
						"sin"
					],
					["panRad"]
				]
			],
			[
				13,
				"cosTilt",
				[
					6,
					[
						7,
						"std",
						"cos"
					],
					["tiltRad"]
				]
			],
			[
				13,
				"sinTilt",
				[
					6,
					[
						7,
						"std",
						"sin"
					],
					["tiltRad"]
				]
			],
			[
				13,
				"perspectiveFactor",
				[
					1,
					[
						6,
						[
							7,
							"std",
							"tan"
						],
						[[
							1,
							"fovRad",
							"*",
							[5, "0.5"]
						]]
					],
					"*",
					[5, "2"]
				]
			],
			[
				13,
				"panDivisor",
				[
					1,
					"cosPan",
					"+",
					[
						1,
						[
							1,
							"sinPan",
							"*",
							"x"
						],
						"*",
						"perspectiveFactor"
					]
				]
			],
			[
				13,
				"safePanDivisor",
				[
					6,
					[
						7,
						"std",
						"max"
					],
					["panDivisor", [5, "0.001"]]
				]
			],
			[
				13,
				"afterPanX",
				[
					1,
					"x",
					"/",
					"safePanDivisor"
				]
			],
			[
				13,
				"afterPanY",
				[
					1,
					[
						1,
						"y",
						"*",
						"cosPan"
					],
					"/",
					"safePanDivisor"
				]
			],
			[
				13,
				"tiltDivisor",
				[
					1,
					"cosTilt",
					"+",
					[
						1,
						[
							1,
							"sinTilt",
							"*",
							"afterPanY"
						],
						"*",
						"perspectiveFactor"
					]
				]
			],
			[
				13,
				"safeTiltDivisor",
				[
					6,
					[
						7,
						"std",
						"max"
					],
					["tiltDivisor", [5, "0.001"]]
				]
			],
			[
				13,
				"finalX",
				[
					1,
					[
						1,
						"afterPanX",
						"*",
						"cosTilt"
					],
					"/",
					"safeTiltDivisor"
				]
			],
			[
				13,
				"finalY",
				[
					1,
					"afterPanY",
					"/",
					"safeTiltDivisor"
				]
			],
			[
				13,
				"offsetPos",
				[
					6,
					[
						7,
						"d",
						"vec2f"
					],
					[[
						7,
						"offset",
						"x"
					], [
						1,
						[5, "1"],
						"-",
						[
							7,
							"offset",
							"y"
						]
					]]
				]
			],
			[10, [
				6,
				[
					7,
					[
						6,
						[
							7,
							[
								6,
								[
									7,
									[
										6,
										[
											7,
											"d",
											"vec2f"
										],
										["finalX", "finalY"]
									],
									"add"
								],
								["centerPos"]
							],
							"sub"
						],
						["offsetPos"]
					],
					"add"
				],
				[[
					6,
					[
						7,
						"d",
						"vec2f"
					],
					[[5, "0.5"], [5, "0.5"]]
				]]
			]]
		]],
		"externalNames": [
			"d",
			"DEG_TO_RAD",
			"std"
		]
	},
	externals: () => ({
		d: data_exports,
		DEG_TO_RAD,
		std: std_exports
	})
}) && $.f)({}));
var componentDefinition = {
	name: "Perspective",
	category: "Distortions",
	description: "Rotate the plane in 3D space with pan and tilt",
	requiresRTT: true,
	requiresChild: true,
	boundingBoxDeclaration: { aspectRatio: null },
	props: {
		center: {
			default: {
				x: .5,
				y: .5
			},
			transform: transformPosition,
			description: "Center point of rotation",
			ui: {
				type: "position",
				label: "Center",
				group: "Position"
			}
		},
		pan: {
			default: 0,
			description: "Horizontal rotation (left/right)",
			ui: {
				type: ["range", "map"],
				min: -90,
				max: 90,
				step: .1,
				label: "Pan",
				group: "Effect"
			}
		},
		tilt: {
			default: 0,
			description: "Vertical rotation (up/down)",
			ui: {
				type: ["range", "map"],
				min: -90,
				max: 90,
				step: .1,
				label: "Tilt",
				group: "Effect"
			}
		},
		fov: {
			default: 60,
			description: "Field of view - controls perspective intensity",
			ui: {
				type: ["range", "map"],
				min: 30,
				max: 120,
				step: 1,
				label: "FOV",
				group: "Effect"
			}
		},
		zoom: {
			default: 1,
			description: "Zoom in to fill the frame after rotation",
			ui: {
				type: ["range", "map"],
				min: .5,
				max: 3,
				step: .1,
				label: "Zoom",
				group: "Effect"
			}
		},
		offset: {
			default: {
				x: .5,
				y: .5
			},
			transform: transformPosition,
			description: "Shift the result in X/Y",
			ui: {
				type: "position",
				label: "Offset",
				group: "Position"
			}
		},
		edges: {
			default: "transparent",
			description: "How to handle edges",
			transform: transformEdges,
			compileTime: true,
			ui: {
				type: "select",
				options: [
					{
						label: "Stretch",
						value: "stretch"
					},
					{
						label: "Transparent",
						value: "transparent"
					},
					{
						label: "Mirror",
						value: "mirror"
					},
					{
						label: "Wrap",
						value: "wrap"
					}
				],
				label: "Edges",
				group: "Effect"
			}
		}
	},
	fragment: ({ childNode, ctx, uniforms, propValues, convertToTexture }) => {
		if (!childNode) return ZERO;
		const texture = convertToTexture(childNode);
		const edgeMode = propValues.edges ?? 0;
		return call(unpremultiplyAlpha, "unpremultiplyAlpha", [sampleRemappedExpr(texture, call(perspectiveUV, "perspectiveUV", [
			uniforms.center,
			uniforms.pan,
			uniforms.tilt,
			uniforms.fov,
			uniforms.zoom,
			uniforms.offset,
			ctx.uv
		]), edgeMode)]);
	},
	uvRemap: ({ uv, mask, uniforms, propValues }) => {
		const edgeMode = propValues.edges ?? 0;
		return composeEdgeRemapExpr(call(perspectiveUV, "perspectiveUV", [
			uniforms.center,
			uniforms.pan,
			uniforms.tilt,
			uniforms.fov,
			uniforms.zoom,
			uniforms.offset,
			uv
		]), mask, edgeMode);
	}
};
//#endregion
//#region node_modules/shaders/dist/react/Perspective.js
var Perspective_exports = /* @__PURE__ */ __exportAll({
	ShaderComponent: () => ShaderComponent,
	default: () => Perspective_default
});
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var import_react_dom = /* @__PURE__ */ __toESM(require_react_dom(), 1);
var import_jsx_runtime = require_jsx_runtime();
function isPropDriver(value) {
	return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
}
function computeEffectiveProps(props, defaultProps$1) {
	let baseProps = { ...defaultProps$1 };
	for (const [key, value] of Object.entries(props)) if (key !== "children" && key !== "ref" && value !== void 0 && !isPropDriver(value)) baseProps[key] = value;
	return baseProps;
}
var MARKER_STYLE = { display: "contents" };
var DEFAULT_TRANSFORM = {
	offsetX: 0,
	offsetY: 0,
	rotation: 0,
	scale: 1,
	anchorX: .5,
	anchorY: .5,
	edges: "transparent"
};
var defaultProps = {
	blendMode: "normal",
	visible: true
};
try {
	if (componentDefinition && componentDefinition.props) Object.entries(componentDefinition.props).forEach(([key, config]) => {
		const propConfig = config;
		if (propConfig && typeof propConfig === "object" && "default" in propConfig) defaultProps[key] = propConfig.default;
	});
} catch (e) {
	console.warn("Error extracting default props:", e);
}
var ShaderComponent = (props) => {
	const context = (0, import_react.useContext)(ShaderContext);
	if (!context) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const { shaderParentId: parentId, shaderNodeRegister: parentRegister, shaderUniformUpdate: parentUniformUpdate, shaderMetadataUpdate: parentMetadataUpdate, shaderColorSpace } = context;
	const reactId = (0, import_react.useId)();
	const instanceId = (0, import_react.useMemo)(() => {
		return (props.id ? props.id.replace(/[^a-zA-Z0-9_]/g, "_") : null) || reactId.replace(/[^a-zA-Z0-9_]/g, "");
	}, [props.id, reactId]);
	const effectiveProps = (0, import_react.useMemo)(() => {
		return computeEffectiveProps(props, defaultProps);
	}, [props]);
	const effectiveTransform = (0, import_react.useMemo)(() => ({
		...DEFAULT_TRANSFORM,
		...props.transform
	}), [props.transform]);
	const effectiveBoundingBox = (0, import_react.useMemo)(() => resolveBoundingBox(props.boundingBox), [props.boundingBox]);
	const markerRef = (0, import_react.useRef)(null);
	const isCapturesDOM = !!componentDefinition.capturesDOM;
	const captureCanvasRef = (0, import_react.useRef)(null);
	const [domMounted, setDomMounted] = (0, import_react.useState)(false);
	const [captureSize, setCaptureSize] = (0, import_react.useState)(() => ({
		w: typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0,
		h: typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0
	}));
	(0, import_react.useEffect)(() => {
		if (!isCapturesDOM) return;
		setDomMounted(true);
		const onResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			setCaptureSize({
				w: Math.round(window.innerWidth * d),
				h: Math.round(window.innerHeight * d)
			});
		};
		window.addEventListener("resize", onResize);
		return () => window.removeEventListener("resize", onResize);
	}, [isCapturesDOM]);
	const detectedOrderRef = (0, import_react.useRef)(void 0);
	const uniformsRef = (0, import_react.useRef)(null);
	if (uniformsRef.current === null) {
		setColorSpaceMode(shaderColorSpace);
		uniformsRef.current = createGpuUniformsMap(componentDefinition, effectiveProps, instanceId);
	}
	const childContextValue = (0, import_react.useMemo)(() => {
		return {
			...context,
			shaderParentId: instanceId
		};
	}, [context, instanceId]);
	(0, import_react.useEffect)(() => {
		if (!uniformsRef.current) return;
		try {
			const metadata = {
				blendMode: props.blendMode || "normal",
				opacity: props.opacity,
				visible: props.visible === false ? false : true,
				id: props.id,
				mask: props.maskSource ? {
					source: props.maskSource,
					type: props.maskType || "alpha"
				} : void 0,
				maps: mapsFromProps,
				renderOrder: props.renderOrder ?? detectedOrderRef.current,
				transform: effectiveTransform,
				boundingBox: effectiveBoundingBox,
				flow: props.flow,
				absolute: props.absolute
			};
			parentRegister(instanceId, componentDefinition.fragment, parentId, metadata, uniformsRef.current, componentDefinition, isCapturesDOM ? captureCanvasRef.current ?? void 0 : void 0);
			return () => {
				parentRegister(instanceId, null, null, null, null);
			};
		} catch (error) {
			console.error("Error registering shader node:", error);
			return () => {};
		}
	}, [
		instanceId,
		parentId,
		parentRegister,
		domMounted
	]);
	(0, import_react.useEffect)(() => {
		if (props.renderOrder === void 0 && markerRef.current) {
			const parent = markerRef.current.parentElement;
			if (parent) {
				const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
				const position = Array.from(siblings).indexOf(markerRef.current);
				if (position >= 0) {
					detectedOrderRef.current = position;
					parentMetadataUpdate(instanceId, { renderOrder: position });
				}
			}
		}
	}, []);
	const prevPropsRef = (0, import_react.useRef)({});
	const mapsFromProps = (0, import_react.useMemo)(() => {
		const maps = {};
		for (const [key, value] of Object.entries(props)) if (key !== "children" && key !== "ref" && isPropDriver(value)) maps[key] = value;
		return Object.keys(maps).length > 0 ? maps : void 0;
	}, [props]);
	(0, import_react.useEffect)(() => {
		if (!uniformsRef.current) return;
		try {
			Object.entries(uniformsRef.current).forEach(([propName, uniformData]) => {
				if (!uniformData || typeof uniformData !== "object") return;
				if (uniformData.value !== void 0 && propName in effectiveProps) {
					const newValue = effectiveProps[propName];
					if (isPropDriver(newValue)) return;
					if (newValue !== prevPropsRef.current[propName]) {
						parentUniformUpdate(instanceId, propName, newValue);
						prevPropsRef.current[propName] = newValue;
					}
				}
			});
		} catch (error) {
			console.error("Error updating uniforms:", error);
		}
	}, [
		effectiveProps,
		instanceId,
		parentUniformUpdate
	]);
	(0, import_react.useEffect)(() => {
		try {
			parentMetadataUpdate(instanceId, {
				blendMode: props.blendMode || "normal",
				opacity: props.opacity,
				visible: props.visible === false ? false : true,
				id: props.id,
				mask: props.maskSource ? {
					source: props.maskSource,
					type: props.maskType || "alpha"
				} : void 0,
				maps: mapsFromProps,
				renderOrder: props.renderOrder ?? detectedOrderRef.current,
				transform: effectiveTransform,
				boundingBox: effectiveBoundingBox,
				flow: props.flow,
				absolute: props.absolute
			});
		} catch (error) {
			console.error("Error updating metadata:", error);
		}
	}, [
		props.blendMode,
		props.opacity,
		props.visible,
		props.maskSource,
		props.maskType,
		props.renderOrder,
		props.id,
		mapsFromProps,
		effectiveTransform,
		effectiveBoundingBox,
		props.flow,
		props.absolute,
		instanceId,
		parentMetadataUpdate
	]);
	if (props.ref && typeof props.ref === "function") try {
		props.ref(null);
	} catch (e) {}
	const captureCanvas = isCapturesDOM && domMounted ? (0, import_react_dom.createPortal)(/* @__PURE__ */ (0, import_jsx_runtime.jsx)("canvas", {
		ref: captureCanvasRef,
		"layoutsubtree": "",
		width: captureSize.w,
		height: captureSize.h,
		style: {
			position: "fixed",
			inset: 0,
			width: "100vw",
			height: "100vh",
			zIndex: -9999
		},
		children: props.children
	}), document.body) : null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ShaderContext.Provider, {
		value: childContextValue,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			ref: markerRef,
			style: MARKER_STYLE,
			"data-shader-id": instanceId,
			children: !isCapturesDOM && props.children
		}), captureCanvas]
	});
};
var Perspective_default = ShaderComponent;
//#endregion
export { Perspective_exports as n, Perspective_default as t };
