import { n as __exportAll, r as __toESM } from "./rolldown-runtime-B-1-B7_t.js";
import { t as require_react } from "./react.js";
import { C as ZERO, Cn as std_exports, Fn as abs, Gn as fract, P as call, Pn as select, Sn as src_default, Ut as resolveBoundingBox, Vn as dot, Yn as mix, Zn as pow, _r as vec3f, cr as data_exports, d as transformBoolean, f as transformColor, ir as smoothstep, it as floatE, kn as fwidth, l as setColorSpaceMode, n as ShaderContext, qn as max, s as createGpuUniformsMap, vn as vec4, vr as vec4f, yr as f32 } from "./Shader-BPX4e-H3.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { t as require_react_dom } from "./react-dom.js";
//#region node_modules/shaders/dist/core/ContourLines-Cg2Dlmcr.js
var transformSource = (value) => value === "alpha" ? 1 : 0;
var transformColorMode = (value) => value === "custom" ? 1 : 0;
var sourceModeOf = (raw) => typeof raw === "number" ? raw : raw === "alpha" ? 1 : 0;
var colorModeOf = (raw) => typeof raw === "number" ? raw : raw === "custom" ? 1 : 0;
var contourLuma = src_default.fn([vec4f], f32)(/* @__PURE__ */ (($) => (globalThis.__TYPEGPU_META__ ??= /* @__PURE__ */ new WeakMap()).set($.f = ((color) => {
	"use gpu";
	return dot(vec3f(color.x, color.y, color.z), vec3f(.2126, .7152, .0722));
}), {
	v: 1,
	name: void 0,
	ast: {
		"params": [{
			"type": "i",
			"name": "color"
		}],
		"body": [0, [[10, [
			6,
			[
				7,
				"std",
				"dot"
			],
			[[
				6,
				[
					7,
					"d",
					"vec3f"
				],
				[
					[
						7,
						"color",
						"x"
					],
					[
						7,
						"color",
						"y"
					],
					[
						7,
						"color",
						"z"
					]
				]
			], [
				6,
				[
					7,
					"d",
					"vec3f"
				],
				[
					[5, "0.2126"],
					[5, "0.7152"],
					[5, "0.0722"]
				]
			]]
		]]]],
		"externalNames": ["std", "d"]
	},
	externals: () => ({
		std: std_exports,
		d: data_exports
	})
}) && $.f)({}));
var contourLineMask = src_default.fn([
	f32,
	f32,
	f32,
	f32,
	f32,
	f32
], f32)(/* @__PURE__ */ (($) => (globalThis.__TYPEGPU_META__ ??= /* @__PURE__ */ new WeakMap()).set($.f = ((sourceValue, levels, lineWidth, softness, gamma, invert) => {
	"use gpu";
	const sv = pow(select(sourceValue, __tsover_sub(1, sourceValue), invert > .5), gamma);
	const scaledValue = __tsover_add(__tsover_mul(sv, levels), .5);
	const fw = max(fwidth(scaledValue), 1e-4);
	const distFromBoundary = abs(__tsover_sub(fract(__tsover_add(scaledValue, .5)), .5));
	const normalizedDist = __tsover_div(distFromBoundary, fw);
	const transitionFactor = __tsover_add(__tsover_mul(softness, .99), .01);
	const innerEdge = __tsover_mul(lineWidth, __tsover_sub(1, transitionFactor));
	return __tsover_sub(1, smoothstep(innerEdge, lineWidth, normalizedDist));
}), {
	v: 1,
	name: void 0,
	ast: {
		"params": [
			{
				"type": "i",
				"name": "sourceValue"
			},
			{
				"type": "i",
				"name": "levels"
			},
			{
				"type": "i",
				"name": "lineWidth"
			},
			{
				"type": "i",
				"name": "softness"
			},
			{
				"type": "i",
				"name": "gamma"
			},
			{
				"type": "i",
				"name": "invert"
			}
		],
		"body": [0, [
			[
				13,
				"invertedValue",
				[
					6,
					[
						7,
						"std",
						"select"
					],
					[
						"sourceValue",
						[
							1,
							[5, "1"],
							"-",
							"sourceValue"
						],
						[
							1,
							"invert",
							">",
							[5, "0.5"]
						]
					]
				]
			],
			[
				13,
				"sv",
				[
					6,
					[
						7,
						"std",
						"pow"
					],
					["invertedValue", "gamma"]
				]
			],
			[
				13,
				"scaledValue",
				[
					1,
					[
						1,
						"sv",
						"*",
						"levels"
					],
					"+",
					[5, "0.5"]
				]
			],
			[
				13,
				"fw",
				[
					6,
					[
						7,
						"std",
						"max"
					],
					[[
						6,
						[
							7,
							"std",
							"fwidth"
						],
						["scaledValue"]
					], [5, "0.0001"]]
				]
			],
			[
				13,
				"distFromBoundary",
				[
					6,
					[
						7,
						"std",
						"abs"
					],
					[[
						1,
						[
							6,
							[
								7,
								"std",
								"fract"
							],
							[[
								1,
								"scaledValue",
								"+",
								[5, "0.5"]
							]]
						],
						"-",
						[5, "0.5"]
					]]
				]
			],
			[
				13,
				"normalizedDist",
				[
					1,
					"distFromBoundary",
					"/",
					"fw"
				]
			],
			[
				13,
				"transitionFactor",
				[
					1,
					[
						1,
						"softness",
						"*",
						[5, "0.99"]
					],
					"+",
					[5, "0.01"]
				]
			],
			[
				13,
				"innerEdge",
				[
					1,
					"lineWidth",
					"*",
					[
						1,
						[5, "1"],
						"-",
						"transitionFactor"
					]
				]
			],
			[10, [
				1,
				[5, "1"],
				"-",
				[
					6,
					[
						7,
						"std",
						"smoothstep"
					],
					[
						"innerEdge",
						"lineWidth",
						"normalizedDist"
					]
				]
			]]
		]],
		"externalNames": ["std"]
	},
	externals: () => ({ std: std_exports })
}) && $.f)({}));
var contourCompose = src_default.fn([
	vec4f,
	vec4f,
	f32
], vec4f)(/* @__PURE__ */ (($) => (globalThis.__TYPEGPU_META__ ??= /* @__PURE__ */ new WeakMap()).set($.f = ((bg, line, mask) => {
	"use gpu";
	const rgb = mix(vec3f(bg.x, bg.y, bg.z), vec3f(line.x, line.y, line.z), vec3f(mask));
	const alpha = mix(bg.w, line.w, mask);
	return vec4f(rgb.x, rgb.y, rgb.z, alpha);
}), {
	v: 1,
	name: void 0,
	ast: {
		"params": [
			{
				"type": "i",
				"name": "bg"
			},
			{
				"type": "i",
				"name": "line"
			},
			{
				"type": "i",
				"name": "mask"
			}
		],
		"body": [0, [
			[
				13,
				"rgb",
				[
					6,
					[
						7,
						"std",
						"mix"
					],
					[
						[
							6,
							[
								7,
								"d",
								"vec3f"
							],
							[
								[
									7,
									"bg",
									"x"
								],
								[
									7,
									"bg",
									"y"
								],
								[
									7,
									"bg",
									"z"
								]
							]
						],
						[
							6,
							[
								7,
								"d",
								"vec3f"
							],
							[
								[
									7,
									"line",
									"x"
								],
								[
									7,
									"line",
									"y"
								],
								[
									7,
									"line",
									"z"
								]
							]
						],
						[
							6,
							[
								7,
								"d",
								"vec3f"
							],
							["mask"]
						]
					]
				]
			],
			[
				13,
				"alpha",
				[
					6,
					[
						7,
						"std",
						"mix"
					],
					[
						[
							7,
							"bg",
							"w"
						],
						[
							7,
							"line",
							"w"
						],
						"mask"
					]
				]
			],
			[10, [
				6,
				[
					7,
					"d",
					"vec4f"
				],
				[
					[
						7,
						"rgb",
						"x"
					],
					[
						7,
						"rgb",
						"y"
					],
					[
						7,
						"rgb",
						"z"
					],
					"alpha"
				]
			]]
		]],
		"externalNames": ["std", "d"]
	},
	externals: () => ({
		std: std_exports,
		d: data_exports
	})
}) && $.f)({}));
var componentDefinition = {
	name: "ContourLines",
	boundingBoxDeclaration: { aspectRatio: null },
	category: "Stylize",
	description: "Draw topographical contour lines based on luminance or alpha",
	requiresChild: true,
	blendWithChildren: false,
	props: {
		levels: {
			default: 5,
			description: "Number of contour levels",
			ui: {
				type: ["range", "map"],
				min: 2,
				max: 30,
				step: 1,
				label: "Levels",
				group: "Effect"
			}
		},
		lineWidth: {
			default: 2,
			description: "Width of the contour lines in pixels",
			ui: {
				type: ["range", "map"],
				min: .5,
				max: 5,
				step: .1,
				label: "Line Width",
				group: "Effect"
			}
		},
		softness: {
			default: 0,
			description: "Edge softness of the lines (0 = sharp, 1 = soft)",
			ui: {
				type: "range",
				min: 0,
				max: 1,
				step: .01,
				label: "Softness",
				group: "Effect"
			}
		},
		gamma: {
			default: .5,
			description: "Contour distribution. <1 clusters in bright, >1 clusters in dark",
			ui: {
				type: ["range", "map"],
				min: .1,
				max: 2,
				step: .01,
				label: "Gamma",
				group: "Effect"
			}
		},
		invert: {
			default: false,
			description: "Invert the source values",
			transform: transformBoolean,
			ui: {
				type: "checkbox",
				label: "Invert",
				group: "Effect"
			}
		},
		source: {
			default: "luminance",
			description: "Use luminance or alpha channel for contours",
			transform: transformSource,
			compileTime: true,
			ui: {
				type: "select",
				options: [{
					label: "Luminance",
					value: "luminance"
				}, {
					label: "Alpha",
					value: "alpha"
				}],
				label: "Source",
				group: "Effect"
			}
		},
		colorMode: {
			default: "source",
			description: "Use source image colors or custom colors",
			transform: transformColorMode,
			compileTime: true,
			ui: {
				type: "select",
				options: [{
					label: "Source",
					value: "source"
				}, {
					label: "Custom",
					value: "custom"
				}],
				label: "Color Mode",
				group: "Colors"
			}
		},
		lineColor: {
			default: "#000000",
			description: "Color of the contour lines (custom mode)",
			transform: transformColor,
			ui: {
				type: "color",
				label: "Line Color",
				group: "Colors",
				condition: { colorMode: "custom" }
			}
		},
		backgroundColor: {
			default: "transparent",
			description: "Background color (custom mode)",
			transform: transformColor,
			ui: {
				type: "color",
				label: "Background Color",
				group: "Colors",
				condition: { colorMode: "custom" }
			}
		}
	},
	fragment: ({ childNode, uniforms, propValues }) => {
		if (!childNode) return ZERO;
		const lineMask = call(contourLineMask, "contourLineMask", [
			sourceModeOf(propValues.source) === 1 ? childNode.member("a") : call(contourLuma, "contourLuma", [childNode]),
			uniforms.levels,
			uniforms.lineWidth,
			uniforms.softness,
			uniforms.gamma,
			uniforms.invert
		]);
		let bg;
		let line;
		if (colorModeOf(propValues.colorMode) === 1) {
			bg = uniforms.backgroundColor;
			line = uniforms.lineColor;
		} else {
			bg = ZERO;
			line = vec4(childNode.member("rgb"), floatE(1));
		}
		return call(contourCompose, "contourCompose", [
			bg,
			line,
			lineMask
		]);
	}
};
//#endregion
//#region node_modules/shaders/dist/react/ContourLines.js
var ContourLines_exports = /* @__PURE__ */ __exportAll({
	ShaderComponent: () => ShaderComponent,
	default: () => ContourLines_default
});
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var import_react_dom = /* @__PURE__ */ __toESM(require_react_dom(), 1);
var import_jsx_runtime = require_jsx_runtime();
function isPropDriver(value) {
	return typeof value === "object" && value !== null && "type" in value && (value.type === "map" || value.type === "mouse" || value.type === "mouse-position" || value.type === "auto-animate");
}
function computeEffectiveProps(props, defaultProps$1) {
	let baseProps = { ...defaultProps$1 };
	for (const [key, value] of Object.entries(props)) if (key !== "children" && key !== "ref" && value !== void 0 && !isPropDriver(value)) baseProps[key] = value;
	return baseProps;
}
var MARKER_STYLE = { display: "contents" };
var DEFAULT_TRANSFORM = {
	offsetX: 0,
	offsetY: 0,
	rotation: 0,
	scale: 1,
	anchorX: .5,
	anchorY: .5,
	edges: "transparent"
};
var defaultProps = {
	blendMode: "normal",
	visible: true
};
try {
	if (componentDefinition && componentDefinition.props) Object.entries(componentDefinition.props).forEach(([key, config]) => {
		const propConfig = config;
		if (propConfig && typeof propConfig === "object" && "default" in propConfig) defaultProps[key] = propConfig.default;
	});
} catch (e) {
	console.warn("Error extracting default props:", e);
}
var ShaderComponent = (props) => {
	const context = (0, import_react.useContext)(ShaderContext);
	if (!context) throw new Error("Shader components must be used inside an <Shader> component or another shader component");
	const { shaderParentId: parentId, shaderNodeRegister: parentRegister, shaderUniformUpdate: parentUniformUpdate, shaderMetadataUpdate: parentMetadataUpdate, shaderColorSpace } = context;
	const reactId = (0, import_react.useId)();
	const instanceId = (0, import_react.useMemo)(() => {
		return (props.id ? props.id.replace(/[^a-zA-Z0-9_]/g, "_") : null) || reactId.replace(/[^a-zA-Z0-9_]/g, "");
	}, [props.id, reactId]);
	const effectiveProps = (0, import_react.useMemo)(() => {
		return computeEffectiveProps(props, defaultProps);
	}, [props]);
	const effectiveTransform = (0, import_react.useMemo)(() => ({
		...DEFAULT_TRANSFORM,
		...props.transform
	}), [props.transform]);
	const effectiveBoundingBox = (0, import_react.useMemo)(() => resolveBoundingBox(props.boundingBox), [props.boundingBox]);
	const markerRef = (0, import_react.useRef)(null);
	const isCapturesDOM = !!componentDefinition.capturesDOM;
	const captureCanvasRef = (0, import_react.useRef)(null);
	const [domMounted, setDomMounted] = (0, import_react.useState)(false);
	const [captureSize, setCaptureSize] = (0, import_react.useState)(() => ({
		w: typeof window !== "undefined" ? Math.round(window.innerWidth * Math.min(window.devicePixelRatio, 2)) : 0,
		h: typeof window !== "undefined" ? Math.round(window.innerHeight * Math.min(window.devicePixelRatio, 2)) : 0
	}));
	(0, import_react.useEffect)(() => {
		if (!isCapturesDOM) return;
		setDomMounted(true);
		const onResize = () => {
			const d = Math.min(window.devicePixelRatio, 2);
			setCaptureSize({
				w: Math.round(window.innerWidth * d),
				h: Math.round(window.innerHeight * d)
			});
		};
		window.addEventListener("resize", onResize);
		return () => window.removeEventListener("resize", onResize);
	}, [isCapturesDOM]);
	const detectedOrderRef = (0, import_react.useRef)(void 0);
	const uniformsRef = (0, import_react.useRef)(null);
	if (uniformsRef.current === null) {
		setColorSpaceMode(shaderColorSpace);
		uniformsRef.current = createGpuUniformsMap(componentDefinition, effectiveProps, instanceId);
	}
	const childContextValue = (0, import_react.useMemo)(() => {
		return {
			...context,
			shaderParentId: instanceId
		};
	}, [context, instanceId]);
	(0, import_react.useEffect)(() => {
		if (!uniformsRef.current) return;
		try {
			const metadata = {
				blendMode: props.blendMode || "normal",
				opacity: props.opacity,
				visible: props.visible === false ? false : true,
				id: props.id,
				mask: props.maskSource ? {
					source: props.maskSource,
					type: props.maskType || "alpha"
				} : void 0,
				maps: mapsFromProps,
				renderOrder: props.renderOrder ?? detectedOrderRef.current,
				transform: effectiveTransform,
				boundingBox: effectiveBoundingBox,
				flow: props.flow,
				absolute: props.absolute
			};
			parentRegister(instanceId, componentDefinition.fragment, parentId, metadata, uniformsRef.current, componentDefinition, isCapturesDOM ? captureCanvasRef.current ?? void 0 : void 0);
			return () => {
				parentRegister(instanceId, null, null, null, null);
			};
		} catch (error) {
			console.error("Error registering shader node:", error);
			return () => {};
		}
	}, [
		instanceId,
		parentId,
		parentRegister,
		domMounted
	]);
	(0, import_react.useEffect)(() => {
		if (props.renderOrder === void 0 && markerRef.current) {
			const parent = markerRef.current.parentElement;
			if (parent) {
				const siblings = parent.querySelectorAll(":scope > [data-shader-id]");
				const position = Array.from(siblings).indexOf(markerRef.current);
				if (position >= 0) {
					detectedOrderRef.current = position;
					parentMetadataUpdate(instanceId, { renderOrder: position });
				}
			}
		}
	}, []);
	const prevPropsRef = (0, import_react.useRef)({});
	const mapsFromProps = (0, import_react.useMemo)(() => {
		const maps = {};
		for (const [key, value] of Object.entries(props)) if (key !== "children" && key !== "ref" && isPropDriver(value)) maps[key] = value;
		return Object.keys(maps).length > 0 ? maps : void 0;
	}, [props]);
	(0, import_react.useEffect)(() => {
		if (!uniformsRef.current) return;
		try {
			Object.entries(uniformsRef.current).forEach(([propName, uniformData]) => {
				if (!uniformData || typeof uniformData !== "object") return;
				if (uniformData.value !== void 0 && propName in effectiveProps) {
					const newValue = effectiveProps[propName];
					if (isPropDriver(newValue)) return;
					if (newValue !== prevPropsRef.current[propName]) {
						parentUniformUpdate(instanceId, propName, newValue);
						prevPropsRef.current[propName] = newValue;
					}
				}
			});
		} catch (error) {
			console.error("Error updating uniforms:", error);
		}
	}, [
		effectiveProps,
		instanceId,
		parentUniformUpdate
	]);
	(0, import_react.useEffect)(() => {
		try {
			parentMetadataUpdate(instanceId, {
				blendMode: props.blendMode || "normal",
				opacity: props.opacity,
				visible: props.visible === false ? false : true,
				id: props.id,
				mask: props.maskSource ? {
					source: props.maskSource,
					type: props.maskType || "alpha"
				} : void 0,
				maps: mapsFromProps,
				renderOrder: props.renderOrder ?? detectedOrderRef.current,
				transform: effectiveTransform,
				boundingBox: effectiveBoundingBox,
				flow: props.flow,
				absolute: props.absolute
			});
		} catch (error) {
			console.error("Error updating metadata:", error);
		}
	}, [
		props.blendMode,
		props.opacity,
		props.visible,
		props.maskSource,
		props.maskType,
		props.renderOrder,
		props.id,
		mapsFromProps,
		effectiveTransform,
		effectiveBoundingBox,
		props.flow,
		props.absolute,
		instanceId,
		parentMetadataUpdate
	]);
	if (props.ref && typeof props.ref === "function") try {
		props.ref(null);
	} catch (e) {}
	const captureCanvas = isCapturesDOM && domMounted ? (0, import_react_dom.createPortal)(/* @__PURE__ */ (0, import_jsx_runtime.jsx)("canvas", {
		ref: captureCanvasRef,
		"layoutsubtree": "",
		width: captureSize.w,
		height: captureSize.h,
		style: {
			position: "fixed",
			inset: 0,
			width: "100vw",
			height: "100vh",
			zIndex: -9999
		},
		children: props.children
	}), document.body) : null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ShaderContext.Provider, {
		value: childContextValue,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			ref: markerRef,
			style: MARKER_STYLE,
			"data-shader-id": instanceId,
			children: !isCapturesDOM && props.children
		}), captureCanvas]
	});
};
var ContourLines_default = ShaderComponent;
//#endregion
export { ContourLines_exports as n, ContourLines_default as t };
